---
phase: quick
plan: 260424-hn5
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tools/descriptions.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "SERVER_INSTRUCTIONS tells Claude to retry with smaller limit instead of reading from file when response is too long"
  artifacts:
    - path: "src/tools/descriptions.ts"
      provides: "Updated SERVER_INSTRUCTIONS with pagination retry guidance"
      contains: "retry.*smaller"
  key_links: []
---

<objective>
Add guidance to SERVER_INSTRUCTIONS that when a tool response is too long and Claude Code suggests reading the result from a file, the agent should IGNORE that suggestion and instead retry the call with a smaller `startLine`/`lineCount` or `limit` parameter.

Purpose: Reading MCP tool responses from files does not work well. Agents need to know to paginate instead.
Output: Updated SERVER_INSTRUCTIONS in descriptions.ts
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/tools/descriptions.ts (lines 13-105 for SERVER_INSTRUCTIONS)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add response pagination guidance to SERVER_INSTRUCTIONS</name>
  <files>src/tools/descriptions.ts</files>
  <action>
Add a new section to SERVER_INSTRUCTIONS (before the closing backtick, after the `configure_filters` section) titled `## Large Responses`.

The section should convey:
- Several tools support pagination via `startLine`/`lineCount` (read_source, read_member) or `limit`/`offset` (navigation tools like find_definition, find_references, find_implementations, search_symbols).
- If a tool response is too large and the MCP client suggests reading the result from a file instead, **ignore that suggestion** — reading tool call results from files does not work reliably.
- Instead, retry the same tool call with a smaller line range or lower limit to get a shorter response.
- For read_source/read_member: use `startLine` and `lineCount` to request a specific line range. The response metadata (`totalLineCount`, `truncated`) tells you the full size.
- For navigation tools: use `limit` (and `offset` to page through results).

Keep the tone consistent with existing sections — concise, imperative, practical. No more than ~8 lines of template literal text.
  </action>
  <verify>
    <automated>cd /Users/LoganDark/Documents/Projects/FabricModMCP && pnpm build && pnpm test</automated>
  </verify>
  <done>SERVER_INSTRUCTIONS contains a "Large Responses" section with clear guidance to retry with pagination parameters instead of reading from files. Build and tests pass.</done>
</task>

</tasks>

<verification>
- grep for "Large Responses" in descriptions.ts confirms section exists
- grep for "retry" or "smaller" confirms the key guidance is present
- pnpm build succeeds
- pnpm test passes
</verification>

<success_criteria>
SERVER_INSTRUCTIONS includes actionable guidance about handling oversized responses via pagination retry, not file reads.
</success_criteria>

<output>
After completion, create `.planning/quick/260424-hn5-in-the-server-instructions-be-clear-that/260424-hn5-SUMMARY.md`
</output>
