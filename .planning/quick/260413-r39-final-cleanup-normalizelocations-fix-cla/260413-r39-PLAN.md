---
phase: quick
plan: 260413-r39
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tools/find-references.ts
  - src/browsing/entry-index.ts
  - tests/helpers/factories.ts
  - tests/tools/type-hierarchy.test.ts
  - tests/tools/search-classes.test.ts
  - tests/tools/list-classes.test.ts
  - tests/tools/search-symbols.test.ts
  - tests/tools/find-implementations.test.ts
  - tests/tools/get-symbol-info.test.ts
  - tests/tools/list-members.test.ts
  - tests/tools/find-references.test.ts
  - tests/tools/find-definition.test.ts
  - tests/tools/locate-in-source.test.ts
  - tests/tools/read-source.test.ts
  - tests/tools/list-packages.test.ts
  - tests/tools/get-project-metadata.test.ts
  - tests/tools/set-default-project.test.ts
  - tests/tools/list-projects.test.ts
  - tests/tools/load-project.test.ts
  - tests/tools/unload-project.test.ts
  - tests/tools/echo.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "find-references.ts uses normalizeLocations() like other LSP tools"
    - "No ClassInfo name collision between entry-index.ts and types.ts"
    - "All 18 test files import parseEnvelope from shared factory"
    - "All tests pass unchanged (327 tests)"
  artifacts:
    - path: "tests/helpers/factories.ts"
      provides: "Shared parseEnvelope and makeFakeProject test factories"
  key_links:
    - from: "tests/tools/*.test.ts"
      to: "tests/helpers/factories.ts"
      via: "import { parseEnvelope, makeFakeProject }"
      pattern: "from.*helpers/factories"
---

<objective>
Three independent cleanups: fix normalizeLocations usage in find-references.ts, rename ClassInfo to ClassIndexEntry in entry-index.ts, and extract duplicated test factories to a shared helper.

Purpose: Eliminate code duplication and a name collision with zero behavior changes.
Output: Cleaner codebase, all 327 tests still passing.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/tools/find-references.ts
@src/tools/tool-helpers.ts
@src/browsing/entry-index.ts
@src/browsing/types.ts
@tests/helpers/client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix normalizeLocations + rename ClassIndexEntry</name>
  <files>src/tools/find-references.ts, src/browsing/entry-index.ts</files>
  <action>
Two quick fixes in production code:

1. **find-references.ts line ~70**: Replace the verbose inline type annotation:
```ts
const locations: Array<{ uri: string; range: { start: { line: number; character: number }; end: { line: number; character: number } } }> = refResult ?? [];
```
With:
```ts
const locations = normalizeLocations(refResult);
```
Add `normalizeLocations` to the existing import from `./tool-helpers.js` on line 9.

2. **entry-index.ts**: Rename `ClassInfo` interface (line 33) to `ClassIndexEntry`. Also rename `FlatClassInfo` (line 38) to `FlatClassIndexEntry`. Update all usages within entry-index.ts:
   - Line 113: `getClasses` return type `ClassInfo[]` -> `ClassIndexEntry[]`
   - Line 117: `const result: ClassInfo[]` -> `const result: ClassIndexEntry[]`
   - Line 131: `getAllClasses` return type `FlatClassInfo[]` -> `FlatClassIndexEntry[]`
   - Line 132: `const result: FlatClassInfo[]` -> `const result: FlatClassIndexEntry[]`
   
   These types are NOT imported by any other file (verified: only entry-index-cache.ts imports from entry-index, and it only imports `EntryIndex` class). The types are used internally within EntryIndex methods and inferred by callers via method return types, so no other files need updating.
  </action>
  <verify>
    <automated>cd /Users/LoganDark/Documents/Projects/FabricModMCP && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>find-references.ts uses normalizeLocations(). ClassInfo renamed to ClassIndexEntry in entry-index.ts. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Extract test factories and update all test files</name>
  <files>tests/helpers/factories.ts, tests/tools/*.test.ts (all 18 files)</files>
  <action>
Create `tests/helpers/factories.ts` with two shared helpers:

1. **parseEnvelope** — identical across all 18 files:
```ts
import type { TestPair } from './client.js';

export function parseEnvelope(result: Awaited<ReturnType<TestPair['client']['callTool']>>): any {
	return (result as any).structuredContent;
}
```

2. **makeFakeProject** — a comprehensive version that merges overrides. Study the variations across test files:
   - Most files use `(overrides: Partial<LoadedProject> = {}): LoadedProject` pattern with dependencyJars Map, jdtls session, filterConfig
   - Some simpler files (list-projects, unload-project, set-default-project) use `(name: string)` signature
   - The shared version should use the `Partial<LoadedProject>` pattern with sensible defaults (name: 'test', rootPath: '/fake/test', basic gradleConfig, basic fabricMod, minecraft dep in dependencyJars, modSourceDir, null jdtls, null filterConfig)
   
   Read each test file's makeFakeProject to build the comprehensive default, then use spread/Object.assign for overrides. The key is that `dependencyJars` should be a `new Map()` with a default minecraft entry, overrideable. Use `DeepPartial` or handle Map merging sensibly.

   Export signature:
```ts
export function makeFakeProject(overrides?: Partial<LoadedProject>): LoadedProject
```

Then update ALL 18 test files:
- Add import: `import { parseEnvelope } from '../helpers/factories.js';` (and `makeFakeProject` where used)
- Remove local `parseEnvelope` function definition
- Remove local `makeFakeProject` function definition (for files that use the overrides pattern)
- For files with simple `makeFakeProject(name: string)` signature (list-projects.test.ts, unload-project.test.ts, set-default-project.test.ts), either adapt calls to use `makeFakeProject({ name })` or keep a local wrapper — use judgment for minimal churn
- For echo.test.ts which only has parseEnvelope (no makeFakeProject), just extract parseEnvelope

Files to update (all in tests/tools/):
echo.test.ts, find-definition.test.ts, find-implementations.test.ts, find-references.test.ts, get-project-metadata.test.ts, get-symbol-info.test.ts, list-classes.test.ts, list-members.test.ts, list-packages.test.ts, list-projects.test.ts, load-project.test.ts, locate-in-source.test.ts, read-source.test.ts, search-classes.test.ts, search-symbols.test.ts, set-default-project.test.ts, type-hierarchy.test.ts, unload-project.test.ts

Use tab indentation in all files.
  </action>
  <verify>
    <automated>cd /Users/LoganDark/Documents/Projects/FabricModMCP && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>All 327 tests pass. No test file defines its own parseEnvelope. makeFakeProject shared where the overrides pattern was used.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/LoganDark/Documents/Projects/FabricModMCP && npx tsc --noEmit && npx vitest run 2>&1 | tail -10
```
All tests pass, no type errors, no local parseEnvelope definitions remain in test files.
</verification>

<success_criteria>
- normalizeLocations used in find-references.ts (matching find-definition.ts and find-implementations.ts pattern)
- ClassInfo renamed to ClassIndexEntry in entry-index.ts with no name collision
- tests/helpers/factories.ts exists with parseEnvelope and makeFakeProject
- All 18 test files import from shared factories
- All 327 tests pass
</success_criteria>

<output>
After completion, create `.planning/quick/260413-r39-final-cleanup-normalizelocations-fix-cla/260413-r39-SUMMARY.md`
</output>
