---
phase: 22-verbosity-audit
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/tools/find-references.test.ts
  - tests/tools/find-definition.test.ts
  - tests/tools/find-implementations.test.ts
  - tests/tools/locate-in-source.test.ts
autonomous: true
gap_closure: true
requirements: [VERB-03]

must_haves:
  truths:
    - "find_references returns context, entryPath, and provenanceChains when details: { lineContent: true } is passed"
    - "find_definition returns context, entryPath, and provenanceChains when details: { lineContent: true } is passed"
    - "find_implementations returns context, entryPath, and provenanceChains when details: { lineContent: true } is passed"
    - "locate_in_source returns steps when details: { steps: true } is passed"
  artifacts:
    - path: "tests/tools/find-references.test.ts"
      provides: "Detail flag opt-in test for find_references"
      contains: "lineContent.*true"
    - path: "tests/tools/find-definition.test.ts"
      provides: "Detail flag opt-in test for find_definition"
      contains: "lineContent.*true"
    - path: "tests/tools/find-implementations.test.ts"
      provides: "Detail flag opt-in test for find_implementations"
      contains: "lineContent.*true"
    - path: "tests/tools/locate-in-source.test.ts"
      provides: "Detail flag opt-in test for locate_in_source"
      contains: "steps.*true"
  key_links:
    - from: "tests/tools/find-references.test.ts"
      to: "src/tools/tool-helpers.ts"
      via: "test asserts stripNavigationResult returns full data when lineContent: true"
      pattern: "context.*toBeDefined"
    - from: "tests/tools/locate-in-source.test.ts"
      to: "src/tools/tool-helpers.ts"
      via: "test asserts stripLocateResult returns full data when steps: true"
      pattern: "steps.*toBeDefined"
---

<objective>
Add missing test cases for the detail flag opt-in paths in navigation and locate tools.

Purpose: The verifier found that find_references, find_definition, find_implementations, and locate_in_source have correct implementations for their detail flags but zero test coverage on the opt-in (full data) path. This gap closure adds those tests.
Output: 4 updated test files with detail flag opt-in assertions
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-verbosity-audit/22-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add detail flag opt-in tests for navigation tools</name>
  <files>tests/tools/find-references.test.ts, tests/tools/find-definition.test.ts, tests/tools/find-implementations.test.ts</files>
  <read_first>
    tests/tools/find-references.test.ts
    tests/tools/find-definition.test.ts
    tests/tools/find-implementations.test.ts
    tests/tools/list-members.test.ts (lines 190-244 for reference pattern of detail flag test)
    src/tools/tool-helpers.ts (stripNavigationResult function)
  </read_first>
  <action>
Add one test case to each of the three navigation tool test files. Each test calls the tool with `details: { lineContent: true }` and asserts that the normally-stripped fields are present in the response.

**find-references.test.ts** — Add after the "returns reference locations with context snippets" test (after line 184). Use the same mock setup as that test (mockReferences returning two locations, mockReadFile returning different sources). Call with `details: { lineContent: true }` and assert:
- `envelope.data.results[0].context` is defined
- `envelope.data.results[0].entryPath` is defined (string containing `MinecraftClient.java`)
- `envelope.data.results[0].context.snippet` is defined (string)
- `envelope.data.results[0].context.startLine` is a number

Test name: `'returns full results with context when details: { lineContent: true } is passed'`

**find-definition.test.ts** — Add after the "returns definition with context snippet" test (after line 139). Use the same mock setup (mockDefinition returning a single location). Call with `details: { lineContent: true }` and assert:
- `envelope.data.results[0].context` is defined
- `envelope.data.results[0].entryPath` is defined
- `envelope.data.results[0].context.snippet` is defined

Test name: `'returns full results with context when details: { lineContent: true } is passed'`

**find-implementations.test.ts** — Add after the "returns NavigationResult array with provenance and context" test (after line 152). Use the same mock setup (mockEndpointSend returning one implementation location). Call with `details: { lineContent: true }` and assert:
- `envelope.data.results[0].context` is defined
- `envelope.data.results[0].entryPath` is defined
- `envelope.data.results[0].context.snippet` is defined

Test name: `'returns full results with context when details: { lineContent: true } is passed'`

Follow the existing test pattern: `test.skipIf(!toolModuleAvailable)(...)`, use `createTestPair()`, `makeFakeProject({ jdtls: ... })`, `projectStore.set('test', fake)`, wrap in try/finally with cleanup.
  </action>
  <verify>
    <automated>cd /Users/LoganDark/Documents/Projects/MinecraftDevMCP && npx vitest run tests/tools/find-references.test.ts tests/tools/find-definition.test.ts tests/tools/find-implementations.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - find-references.test.ts contains `details: { lineContent: true }` in a test
    - find-references.test.ts contains `expect(envelope.data.results[0].context).toBeDefined()`
    - find-references.test.ts contains `expect(envelope.data.results[0].entryPath).toBeDefined()`
    - find-definition.test.ts contains `details: { lineContent: true }` in a test
    - find-definition.test.ts contains `expect(envelope.data.results[0].context).toBeDefined()`
    - find-implementations.test.ts contains `details: { lineContent: true }` in a test
    - find-implementations.test.ts contains `expect(envelope.data.results[0].context).toBeDefined()`
    - All three test files pass with `npx vitest run`
  </acceptance_criteria>
  <done>All three navigation tool test files have a passing test that calls the tool with details: { lineContent: true } and asserts context, entryPath are defined in results</done>
</task>

<task type="auto">
  <name>Task 2: Add detail flag opt-in test for locate_in_source</name>
  <files>tests/tools/locate-in-source.test.ts</files>
  <read_first>
    tests/tools/locate-in-source.test.ts
    src/tools/tool-helpers.ts (stripLocateResult function)
  </read_first>
  <action>
Add one test case to locate-in-source.test.ts. Add it after the first test "returns success with results when cascade matches" (after line 141). The test calls locate_in_source with `details: { steps: true }` and asserts that the normally-stripped `steps` field is present.

Use the same setup as the first test: project 'test', jar 'minecraft', class 'net.minecraft.client.MinecraftClient', patterns `['public class MinecraftClient', 'MinecraftClient']`. Add `details: { steps: true }` to the arguments.

Assert:
- `envelope.data.results[0].steps` is defined
- `envelope.data.results[0].steps` is an array
- `envelope.data.results[0].steps.length` is greater than 0

Test name: `'returns full results with steps when details: { steps: true } is passed'`

Follow the existing test pattern: use `it(...)` (not `test(...)`), rely on `pair` and `beforeEach`/`afterEach` from the describe block.
  </action>
  <verify>
    <automated>cd /Users/LoganDark/Documents/Projects/MinecraftDevMCP && npx vitest run tests/tools/locate-in-source.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - locate-in-source.test.ts contains `details: { steps: true }` in a test
    - locate-in-source.test.ts contains `expect(envelope.data.results[0].steps).toBeDefined()`
    - locate-in-source.test.ts passes with `npx vitest run`
  </acceptance_criteria>
  <done>locate-in-source.test.ts has a passing test that calls the tool with details: { steps: true } and asserts steps is defined and non-empty in results</done>
</task>

</tasks>

<verification>
All 4 test files pass: `npx vitest run tests/tools/find-references.test.ts tests/tools/find-definition.test.ts tests/tools/find-implementations.test.ts tests/tools/locate-in-source.test.ts`

Full test suite still green: `npx vitest run`
</verification>

<success_criteria>
- 4 new test cases added (one per tool)
- Each test passes `details` flag and asserts stripped fields are restored
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/22-verbosity-audit/22-03-SUMMARY.md`
</output>
