---
phase: 34-documentation-and-instructions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tools/descriptions.ts
  - CLAUDE.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-04
  - DOC-05

must_haves:
  truths:
    - "Every JDT LS-dependent tool description states the JDT LS requirement"
    - "SERVER_INSTRUCTIONS explains JDT LS availability, response envelope, study jar workflow, scope dual-effect, refresh guidance, and configure_filters"
    - "All tool descriptions match actual parameter names, response fields, and behavior"
    - "CLAUDE.md Architecture, Conventions, and Project Structure sections contain accurate current information"
    - "No stale Phase references or removed/renamed parameter names appear in any description"
  artifacts:
    - path: "src/tools/descriptions.ts"
      provides: "Updated SERVER_INSTRUCTIONS and TOOL_DESCRIPTIONS"
      contains: "JDTLS_NOT_AVAILABLE"
    - path: "CLAUDE.md"
      provides: "Filled Architecture, Conventions, Project Structure sections"
      contains: "## Architecture"
  key_links:
    - from: "src/tools/descriptions.ts"
      to: "all tool files"
      via: "TOOL_DESCRIPTIONS import"
      pattern: "TOOL_DESCRIPTIONS"
    - from: "CLAUDE.md"
      to: "Claude Code behavior"
      via: "project instructions loaded on conversation start"
      pattern: "## Conventions"
---

<objective>
Update all documentation and instructions to accurately describe the server's final state after phases 28-33.

Purpose: Agents consuming this MCP server rely on SERVER_INSTRUCTIONS and tool descriptions for correct tool usage. CLAUDE.md guides Claude Code behavior in this repo. Stale or missing docs cause agent errors.
Output: Updated `src/tools/descriptions.ts` (SERVER_INSTRUCTIONS + all TOOL_DESCRIPTIONS) and `CLAUDE.md` with filled sections.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/AUDIT-FINDINGS.md
@.planning/phases/34-documentation-and-instructions/34-CONTEXT.md
@src/tools/descriptions.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SERVER_INSTRUCTIONS and all TOOL_DESCRIPTIONS in descriptions.ts</name>
  <files>src/tools/descriptions.ts</files>
  <action>
This task modifies `src/tools/descriptions.ts` only. Read each tool file listed in 34-CONTEXT.md canonical_refs to cross-reference actual schemas/behavior before writing descriptions.

**SERVER_INSTRUCTIONS changes (DOC-01, DOC-02, DOC-03):**

Add these new sections after the existing "## Shared Concepts" block:

1. **## JDT LS (Java Language Server)** — Explain: JDT LS provides semantic Java analysis (go-to-definition, find-references, type hierarchy, etc.). It requires Java 21+ and the JDTLS_HOME environment variable pointing to the JDT LS installation. If unavailable, 8 tools return JDTLS_NOT_AVAILABLE error code. Use get_project_info to check jdtlsAvailable per project. Tools requiring JDT LS: list_members, read_member, find_definition, find_references, find_implementations, get_symbol_info, search_symbols, type_hierarchy.

2. **## Response Envelope** — All tools return `{ ok: true, ...data }` on success or `{ ok: false, code: string, message: string, tried?: string[], suggestions?: string[] }` on error. The `code` field is a machine-readable error code (e.g., JDTLS_NOT_AVAILABLE, NOT_FOUND, AMBIGUOUS_ID). `tried` shows what was attempted; `suggestions` offers recovery actions.

3. **## Study Jars** — Workflow: add_study_jar (provide path + optional name) -> configure_study_jar (set autoInclude to control default visibility) -> list_study_jars (see all study jars and stats). Study jar names must not conflict with existing dependency IDs. Study jars are available to all browsing/search tools. Use configure_filters to fine-tune which jars appear in results.

4. **## Refresh Guidance** — When to use refresh_project / refresh_project_members: after modifying gradle.properties, build.gradle.kts, or fabric.mod.json and running ./gradlew downloadSources. Both tools re-parse build configuration files (not just re-scan jars). refresh_project refreshes all members; refresh_project_members refreshes specific ones by name.

5. **## configure_filters** — Briefly mention: use configure_filters to control which dependency jars appear in browsing/search results. Include-all mode (default) excludes matched patterns; exclude-all mode includes matched patterns.

**Fix existing SERVER_INSTRUCTIONS text:**

- Step 1: Change `(or use the "default" project)` to `(a "default" project is pre-created at startup)`
- Step 4: Change the search->read_member pipeline to: `search_classes (by name pattern) or search_symbols (by symbol name) -> list_members -> read_member`
- Scope section under "Shared Concepts": Add sentence: "When scoped, only that child's own jars (after its filters) are searched. When unscoped, all children's jars are searched (each with its own filter applied)."

**TOOL_DESCRIPTIONS changes (DOC-01 + DOC-04):**

Add "Requires JDT LS (Java 21+ and JDTLS_HOME). Returns JDTLS_NOT_AVAILABLE if unavailable." to each of these 8 tools' descriptions (insert as second sentence or at natural point):
- list_members, read_member, find_definition, find_references, find_implementations, get_symbol_info, search_symbols, type_hierarchy

Individual tool description fixes (DOC-04):
- locate_in_source: Add that `details.steps` includes a `matched` field per step showing the matched text
- list_study_jars: Remove "total entries" from the stats list — change to "stats (package count, class count)"
- create_project: Add "Initializes JDT LS workspace if available. Response includes jdtlsAvailable status."
- read_member: Add that field FQNs use a trailing colon format (e.g., `Class#field:`)
- type_hierarchy: Add "depth:0 returns the supertype chain only (no subtypes). Supertype chain is fully traversed regardless of depth."
- set_active_child: Reword to clarify it only affects bare ID namespace resolution, not jar filtering scope. Something like: "Set the active child (fabric mod) on a project. When set, bare jar IDs like 'minecraft' resolve within that child's namespace without requiring the scope parameter. Does not affect which jars are searched — use the scope parameter on individual tools for that."
- configure_filters: Change pattern example from `"net.fabricmc.*"` to `"net.fabricmc.fabric-api:*"`
- search_symbols: Add "Fields are NOT searchable via this tool" if not already present (check — it may already be there from Phase 30)
- add_study_jar: Add "The name must not conflict with an existing dependency ID."
- search_classes: Verify it references `query` parameter, not `pattern` (renamed in Phase 30). The description currently says "glob pattern" which is fine, just ensure no reference to a param named `pattern`.
- remove_project_member: Verify it references `names` parameter (renamed in Phase 30). Currently says "an array of names" which is correct.
- refresh_project: Add "Re-parses gradle.properties and build.gradle.kts to detect configuration changes."
- refresh_project_members: Add "Re-parses gradle.properties and fabric.mod.json for each specified member."
  </action>
  <verify>
    <automated>cd /Users/LoganDark/Documents/Projects/FabricModMCP && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>
- All 8 JDT LS tool descriptions contain "Requires JDT LS" and "JDTLS_NOT_AVAILABLE"
- SERVER_INSTRUCTIONS has JDT LS, Response Envelope, Study Jars, Refresh Guidance, and configure_filters sections
- Existing workflow steps corrected (default project phrasing, search pipeline, scope dual-effect)
- All 12+ individual tool description fixes applied
- No stale parameter names (pattern->query, members->names) or removed fields (totalEntries, javadoc) referenced
- TypeScript compiles and all tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Fill CLAUDE.md empty sections and fix stale references</name>
  <files>CLAUDE.md</files>
  <action>
Edit CLAUDE.md to fill empty sections and fix stale content. Be mindful of the GSD marker comments (e.g., `<!-- GSD:conventions-start -->`) — content between matching start/end markers can be edited but the markers themselves must be preserved.

**Fill Conventions section (between GSD:conventions-start and GSD:conventions-end):**
Replace the placeholder text with actual conventions:
- Tab indentation in all source files (not spaces)
- MCP tool text responses must not be JSON.stringify'd duplicates of structuredContent (no nested JSON strings)
- All tool descriptions live in `src/tools/descriptions.ts` — the TOOL_DESCRIPTIONS and SERVER_INSTRUCTIONS objects
- Shared parameter schemas in PARAMS and DETAIL_PARAMS objects in descriptions.ts
- Tool response envelope: `{ ok: true, ...data }` on success, `{ ok: false, code, message }` on error (see `src/types/envelope.ts`)
- Domain logic in `src/browsing/`, `src/project/`, `src/jdtls/` — tools in `src/tools/` are thin wrappers
- Tests use vitest, located alongside source or in `tests/` directory

**Fill Architecture section (between GSD:architecture-start and GSD:architecture-end):**
Replace the placeholder with layered architecture description:
- **Layer 1 — MCP Server** (`src/server.ts`, `src/index.ts`): Registers tools, handles MCP protocol via stdio
- **Layer 2 — Tools** (`src/tools/*.ts`): Thin handlers that validate params (via Zod), call domain logic, format responses. Each tool imports its description from `descriptions.ts`.
- **Layer 3 — Domain** (`src/browsing/`, `src/project/`, `src/jdtls/`): Business logic. Browsing: class parsing, member extraction, search, cascading regex. Project: Gradle parsing, jar reading, dependency discovery, namespace resolution. JDT LS: LSP client, workspace sync, symbol navigation.
- **Layer 4 — State** (`src/state/project-store.ts`): Singleton ProjectStore holding all project state, jar handles, JDT LS sessions
- **Layer 5 — Types** (`src/types/`): Shared type definitions, response envelope

**Fill Project Structure section (in the Technology Stack GSD block):**
Replace the empty `## Project Structure` with a directory tree:
```
src/
  index.ts          — Entry point
  server.ts         — MCP server setup and tool registration
  browsing/         — Class parsing, member extraction, search, cascading regex
  cli/              — CLI argument parsing
  errors/           — Domain error types and validation
  jdtls/            — JDT LS client, workspace sync, startup
  logging/          — Logger
  project/          — Gradle parsing, jar reading, dependency discovery
  state/            — ProjectStore singleton
  tools/            — MCP tool handlers and descriptions
  types/            — Shared types and response envelope
tests/              — Test files
```

**Fix stale references in Technology Stack section:**
- Alternatives table row: Change `JDT LS (Phase 2)` to just `JDT LS` and remove "Phase 1 uses cascading regex for search. JDT LS adds semantic find-definition/find-references in Phase 2. This is an additive approach, not a replacement." Replace with: "Cascading regex provides text-based search. JDT LS adds semantic navigation (find-definition, find-references, type hierarchy). Both are used — additive, not replacement."
- MCP SDK row: Remove "v2 anticipated Q1 2026 but v1.x is production-recommended and will receive patches for 6+ months after v2 ships. Start on v1.x; migrate to v2 when stable." Replace with just: "Official SDK. Full MCP spec compliance. Supports stdio transport (what Claude Code uses). Production-ready v1.x line."
- ts-lsp-client: Change confidence from MEDIUM to HIGH
- Multi-Project Support bullet: Change "JDT LS workspace (Phase 2)" to "JDT LS workspace"

**Fill empty headers or remove them:**
- `## Runtime Dependencies` — Fill with: list from package.json dependencies (list the 6 runtime deps with one-line purpose each)
- `## Installation` — Remove the orphaned `# Core` and `# Dev dependencies` lines. Replace with: `pnpm install` to install, `pnpm start` to run, `pnpm build` to bundle.
- `## Key Technical Details` subsections (Jar Reading Strategy, Gradle Properties Parsing Strategy) — Add brief descriptions: Jar Reading uses node-stream-zip for O(1) random access by entry path from source jars. Gradle parsing reads gradle.properties as key=value pairs + regex extraction from build.gradle.kts.
  </action>
  <verify>
    <automated>cd /Users/LoganDark/Documents/Projects/FabricModMCP && grep -c "not yet" CLAUDE.md && grep -c "Phase 2" CLAUDE.md && grep -c "MEDIUM" CLAUDE.md</automated>
  </verify>
  <done>
- Conventions section contains tab indentation rule, no-nested-JSON rule, descriptions.ts pattern, envelope pattern
- Architecture section describes the 5-layer structure with file paths
- Project Structure section has directory tree
- Zero occurrences of "not yet established", "not yet mapped" placeholder text
- Zero occurrences of "Phase 2" in stale contexts (Alternatives table, Multi-Project Support)
- ts-lsp-client confidence is HIGH
- Runtime Dependencies, Installation, Key Technical Details sections have content (not empty headers)
- MCP SDK version timeline prediction removed
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `npx vitest run` — all tests pass (descriptions.ts is imported by every tool, compilation errors would break tests)
2. `grep -c "JDTLS_NOT_AVAILABLE" src/tools/descriptions.ts` returns 8+ (one per JDT LS tool)
3. `grep -c "not yet" CLAUDE.md` returns 0
4. `grep "Phase 2" CLAUDE.md` returns no stale references
5. `grep "total entries" src/tools/descriptions.ts` returns 0
6. `grep "net.fabricmc.\*" src/tools/descriptions.ts` returns 0 (old pattern example removed)
</verification>

<success_criteria>
- SERVER_INSTRUCTIONS has 5 new sections (JDT LS, Response Envelope, Study Jars, Refresh Guidance, configure_filters) and 3 existing text fixes
- All 8 JDT LS tools have requirement notice in their descriptions
- All 12+ individual tool description fixes from the audit are applied
- CLAUDE.md has no empty/placeholder sections and no stale Phase references
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/34-documentation-and-instructions/34-01-SUMMARY.md`
</output>
