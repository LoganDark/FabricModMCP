---
phase: 23-type-foundation-and-projectstore
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [tests/cli/args.test.ts]
autonomous: true
requirements: [CONT-01, CONT-02, CONT-03, CONT-05, CONT-06]
gap_closure: true

must_haves:
  truths:
    - "All tests pass with zero failures (npx vitest run exits 0)"
    - "tests/cli/args.test.ts tests only currently-supported CLI behavior"
    - "No references to args.projects or --project flag remain in test files"
  artifacts:
    - path: "tests/cli/args.test.ts"
      provides: "CLI argument parsing tests for --verbose, --log-level, LOG_LEVEL env"
      contains: "parseCli"
  key_links:
    - from: "tests/cli/args.test.ts"
      to: "src/cli/args.ts"
      via: "imports parseCli"
      pattern: "import.*parseCli.*from.*args"
---

<objective>
Fix the 3 failing tests in tests/cli/args.test.ts that reference the removed --project CLI flag.

Purpose: Close the single verification gap blocking Phase 23 completion — "All 592+ tests pass with zero regressions" currently fails because tests/cli/args.test.ts was not updated when --project was removed from src/cli/args.ts.

Output: Updated test file covering current CLI behavior; full test suite passing.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-type-foundation-and-projectstore/23-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace removed --project tests with current CLI flag tests</name>
  <files>tests/cli/args.test.ts</files>
  <read_first>
    - src/cli/args.ts
    - tests/cli/args.test.ts
  </read_first>
  <action>
Replace the entire contents of tests/cli/args.test.ts. The current file has 3 tests that all reference `args.projects` and the `--project` flag, which were removed from src/cli/args.ts in this phase.

Write new tests covering the currently-supported CLI behavior in src/cli/args.ts:

1. `parseCli([]) returns default logLevel 'info'` - call parseCli([]), expect result.logLevel to equal 'info'
2. `--verbose flag sets logLevel to 'debug'` - call parseCli(['--verbose']), expect result.logLevel to equal 'debug'
3. `-v short flag sets logLevel to 'debug'` - call parseCli(['-v']), expect result.logLevel to equal 'debug'
4. `--log-level sets logLevel` - call parseCli(['--log-level', 'warn']), expect result.logLevel to equal 'warn'
5. `--verbose overrides --log-level` - call parseCli(['--log-level', 'warn', '--verbose']), expect result.logLevel to equal 'debug'
6. `invalid --log-level falls back to default` - call parseCli(['--log-level', 'invalid']), expect result.logLevel to equal 'info'
7. `unknown flag throws` - expect parseCli(['--project', '/path']) to throw (confirms --project is truly removed)

Use tab indentation. Import { describe, it, expect } from 'vitest' and { parseCli } from '../../src/cli/args.js'.

Do NOT reference `args.projects` anywhere. The CliArgs interface only has `logLevel: LogLevel`.
  </action>
  <verify>
    <automated>cd /Users/LoganDark/Documents/Projects/FabricModMCP && npx vitest run tests/cli/args.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "projects" tests/cli/args.test.ts returns 0 (no references to removed field)
    - grep -c "--project" tests/cli/args.test.ts returns exactly 1 (only in the "unknown flag throws" test)
    - npx vitest run tests/cli/args.test.ts exits with code 0
    - npx vitest run exits with code 0 (full suite passes)
  </acceptance_criteria>
  <done>All tests in tests/cli/args.test.ts pass. No references to args.projects. Full test suite (600+) passes with zero failures.</done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/cli/args.test.ts` — all new CLI tests pass
2. `npx vitest run` — full suite passes with zero failures
3. `grep -r "args\.projects" tests/` — no remaining references to removed field
</verification>

<success_criteria>
- All 7 new CLI tests pass
- Zero test failures across the entire suite
- No references to `args.projects` or `--project` flag behavior remain in tests (except the "unknown flag throws" test confirming removal)
</success_criteria>

<output>
After completion, create `.planning/phases/23-type-foundation-and-projectstore/23-04-SUMMARY.md`
</output>
