---
phase: 23-type-foundation-and-projectstore
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/project/types.ts
  - src/project/compat.ts
  - tests/project/compat.test.ts
  - tests/project/types.test.ts
autonomous: true
requirements: [CONT-01, CONT-02, CONT-03, CONT-06]

must_haves:
  truths:
    - "Project interface has only name, children Map, and optional jdtls -- no rootPath, gradleConfig, sourcesJar, fabricMod, dependencyJars, filterConfig, or studyJars"
    - "FabricModChild interface has kind 'fabric-mod' and owns rootPath, gradleConfig, sourcesJar, fabricMod, dependencyJars, filterConfig"
    - "StudyJarChild interface has kind 'study-jar' and wraps existing StudyJar fields"
    - "ProjectChild is a discriminated union of FabricModChild | StudyJarChild"
    - "LoadedProject is a type alias for Project"
    - "Compat accessors resolve from sole fabric mod child, throw DomainError for zero or multiple mods"
    - "getStudyJars returns Map of study-jar children from project.children"
    - "Type guards correctly narrow ProjectChild to FabricModChild or StudyJarChild by kind field"
  artifacts:
    - path: "src/project/types.ts"
      provides: "Project, FabricModChild, StudyJarChild, ProjectChild types and LoadedProject alias"
      contains: "export type LoadedProject = Project"
    - path: "src/project/compat.ts"
      provides: "Compat accessor functions for backward compatibility"
      exports: ["getSoleFabricMod", "getGradleConfig", "getSourcesJar", "getFabricMod", "getDependencyJars", "getFilterConfig", "getRootPath", "getStudyJars"]
    - path: "tests/project/compat.test.ts"
      provides: "Tests for compat accessor functions"
      min_lines: 50
    - path: "tests/project/types.test.ts"
      provides: "Tests for type hierarchy correctness and discriminated union narrowing"
      min_lines: 30
  key_links:
    - from: "src/project/compat.ts"
      to: "src/project/types.ts"
      via: "imports Project, FabricModChild, StudyJarChild"
      pattern: "import.*Project.*from.*types"
    - from: "src/project/compat.ts"
      to: "src/errors/domain-error.ts"
      via: "throws DomainError for zero/multiple mods"
      pattern: "DomainError"
---

<objective>
Define the new type hierarchy (Project, FabricModChild, StudyJarChild, ProjectChild) and build the compat accessor layer that bridges old field access patterns to the new child-based structure.

Purpose: This is the foundation for the entire v1.4 rearchitecture. All subsequent plans depend on these types existing and the compat layer being tested.
Output: New type definitions in types.ts, compat accessor functions in compat.ts, comprehensive tests for the compat layer, and type correctness tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-type-foundation-and-projectstore/23-CONTEXT.md
@.planning/phases/23-type-foundation-and-projectstore/23-RESEARCH.md

<interfaces>
<!-- Current types being replaced/extended -->

From src/project/types.ts:
```typescript
export interface StudyJar {
	name: string;
	jarPath: string;
	mtime: number;
	size: number;
	autoInclude: boolean;
	stats: StudyJarStats;
}

export interface LoadedProject {
	name: string;
	rootPath: string;
	gradleConfig: GradleConfig;
	sourcesJar: ResolvedJar;
	fabricMod: FabricModJson;
	dependencyJars: Map<string, DependencyEntry>;
	filterConfig: FilterConfig;
	studyJars: Map<string, StudyJar>;
	jdtls?: JdtLsSession;
}
```

From src/errors/domain-error.ts:
```typescript
export class DomainError extends Error {
	constructor(
		public readonly code: string,
		message: string,
		public readonly tried: string[] = [],
		public readonly suggestions: string[] = [],
	) { ... }
}
```

From src/jdtls/types.ts:
```typescript
export interface JdtLsSession {
	available: boolean;
	failureReason?: string;
	tempDir: string;
	dataDir: string;
	jarIdToDirName: Map<string, string>;
	client?: LspClient;
	endpoint?: JSONRPCEndpoint;
	process?: ChildProcess;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite types.ts with new type hierarchy</name>
  <files>src/project/types.ts</files>
  <read_first>
    - src/project/types.ts (current types being restructured)
    - src/jdtls/types.ts (JdtLsSession interface that stays on Project)
  </read_first>
  <action>
Rewrite `src/project/types.ts` to define the new container type hierarchy. Keep all existing types that are NOT being restructured (MappingEra, DependencyCoordinate, GradleConfig, FabricModJson, ResolvedJar, JarCategory, DependencyEntry, FilterConfig, StudyJarStats, StudyJar). Add these new types:

```typescript
export interface FabricModChild {
	kind: 'fabric-mod';
	name: string;
	rootPath: string;
	gradleConfig: GradleConfig;
	sourcesJar: ResolvedJar;
	fabricMod: FabricModJson;
	dependencyJars: Map<string, DependencyEntry>;
	filterConfig: FilterConfig;
}

export interface StudyJarChild {
	kind: 'study-jar';
	name: string;
	jarPath: string;
	mtime: number;
	size: number;
	autoInclude: boolean;
	stats: StudyJarStats;
}

export type ProjectChild = FabricModChild | StudyJarChild;

export interface Project {
	name: string;
	children: Map<string, ProjectChild>;
	jdtls?: JdtLsSession;
}

export type LoadedProject = Project;
```

CRITICAL: The old `LoadedProject` interface (with rootPath, gradleConfig, etc.) is REPLACED by the `Project` interface. `LoadedProject` becomes a type alias for `Project`. This will cause compile errors throughout the codebase -- that is intentional and will be fixed in plans 02 and 03.

Keep the `StudyJar` interface as-is (it is still used internally). `StudyJarChild` is a parallel type that adds `kind` and `name` fields.

Use tab indentation. Export all new types.
  </action>
  <verify>
    <automated>grep -c "export interface FabricModChild" src/project/types.ts && grep -c "export interface StudyJarChild" src/project/types.ts && grep -c "export type ProjectChild" src/project/types.ts && grep -c "export interface Project" src/project/types.ts && grep -c "export type LoadedProject = Project" src/project/types.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/project/types.ts contains `export interface FabricModChild` with `kind: 'fabric-mod'`
    - src/project/types.ts contains `export interface StudyJarChild` with `kind: 'study-jar'`
    - src/project/types.ts contains `export type ProjectChild = FabricModChild | StudyJarChild`
    - src/project/types.ts contains `export interface Project` with `name: string`, `children: Map<string, ProjectChild>`, `jdtls?: JdtLsSession`
    - src/project/types.ts contains `export type LoadedProject = Project`
    - src/project/types.ts does NOT contain the old `export interface LoadedProject` (the one with rootPath, gradleConfig fields)
    - All existing types (GradleConfig, FabricModJson, ResolvedJar, DependencyEntry, FilterConfig, StudyJar, StudyJarStats, etc.) are still exported
    - File uses tab indentation
  </acceptance_criteria>
  <done>New type hierarchy defined. Project is a pure container. FabricModChild owns all mod-specific fields. StudyJarChild wraps StudyJar with kind discriminant. LoadedProject is an alias for Project.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create compat.ts accessor functions with tests</name>
  <files>src/project/compat.ts, tests/project/compat.test.ts</files>
  <read_first>
    - src/project/types.ts (just written in Task 1 -- read to get exact type signatures)
    - src/errors/domain-error.ts (DomainError constructor signature)
    - .planning/phases/23-type-foundation-and-projectstore/23-RESEARCH.md (Pattern 2: Compat Accessor Functions example code)
  </read_first>
  <behavior>
    - getSoleFabricMod(project) with one fabric mod returns that mod
    - getSoleFabricMod(project) with zero children throws DomainError code 'NO_FABRIC_MOD'
    - getSoleFabricMod(project) with two fabric mods throws DomainError code 'MULTIPLE_FABRIC_MODS'
    - getSoleFabricMod(project) with one study jar and one fabric mod returns the fabric mod (ignores study jars)
    - getGradleConfig(project) delegates to getSoleFabricMod(project).gradleConfig
    - getSourcesJar(project) delegates to getSoleFabricMod(project).sourcesJar
    - getFabricMod(project) delegates to getSoleFabricMod(project).fabricMod
    - getDependencyJars(project) delegates to getSoleFabricMod(project).dependencyJars
    - getFilterConfig(project) delegates to getSoleFabricMod(project).filterConfig
    - getRootPath(project) delegates to getSoleFabricMod(project).rootPath
    - getStudyJars(project) returns Map of only study-jar kind children from project.children
    - getStudyJars(project) with no study jars returns empty Map
    - getStudyJars(project) with mixed children returns only study jars
  </behavior>
  <action>
Create `src/project/compat.ts` with these exported functions:

1. `getSoleFabricMod(project: Project): FabricModChild` -- iterates `project.children.values()`, collects children with `kind === 'fabric-mod'`. If 0 found, throw `new DomainError('NO_FABRIC_MOD', "No fabric mod loaded in project '${project.name}'", [project.name], ['Load a fabric mod using the load_project tool'])`. If >1 found, throw `new DomainError('MULTIPLE_FABRIC_MODS', "Multiple fabric mods in project '${project.name}' -- specify which one", mods.map(m => m.name), ['This operation requires exactly one fabric mod'])`. Otherwise return the single mod.

2. `getGradleConfig(project: Project)` -- returns `getSoleFabricMod(project).gradleConfig`
3. `getSourcesJar(project: Project)` -- returns `getSoleFabricMod(project).sourcesJar`
4. `getFabricMod(project: Project)` -- returns `getSoleFabricMod(project).fabricMod`
5. `getDependencyJars(project: Project)` -- returns `getSoleFabricMod(project).dependencyJars`
6. `getFilterConfig(project: Project)` -- returns `getSoleFabricMod(project).filterConfig`
7. `getRootPath(project: Project)` -- returns `getSoleFabricMod(project).rootPath`
8. `getStudyJars(project: Project): Map<string, StudyJarChild>` -- iterates `project.children`, collects `kind === 'study-jar'` entries into a new Map.

Import `DomainError` from `'../errors/domain-error.js'`. Import `Project`, `FabricModChild`, `StudyJarChild` from `'./types.js'`.

Then create `tests/project/compat.test.ts` following TDD RED-GREEN pattern. Write tests FIRST covering all behaviors listed above, using mock Project objects with `children: new Map(...)` containing test FabricModChild and StudyJarChild instances. Use tab indentation.
  </action>
  <verify>
    <automated>npx vitest run tests/project/compat.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - src/project/compat.ts exists and exports: getSoleFabricMod, getGradleConfig, getSourcesJar, getFabricMod, getDependencyJars, getFilterConfig, getRootPath, getStudyJars
    - tests/project/compat.test.ts exists with at least 10 test cases
    - Tests cover: zero mods error, multiple mods error, single mod returns it, mixed children (study jar ignored), all accessor delegation, getStudyJars filtering
    - DomainError codes used: 'NO_FABRIC_MOD' and 'MULTIPLE_FABRIC_MODS'
    - All compat tests pass
    - Files use tab indentation
  </acceptance_criteria>
  <done>Compat accessor layer exists with comprehensive tests. getSoleFabricMod correctly resolves sole mod or throws. All property accessors delegate. getStudyJars filters by kind.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create types.test.ts for type hierarchy correctness</name>
  <files>tests/project/types.test.ts</files>
  <read_first>
    - src/project/types.ts (new type definitions from Task 1)
  </read_first>
  <behavior>
    - FabricModChild with kind 'fabric-mod' narrows correctly in switch/if statements
    - StudyJarChild with kind 'study-jar' narrows correctly in switch/if statements
    - ProjectChild discriminated union narrows to FabricModChild when kind === 'fabric-mod'
    - ProjectChild discriminated union narrows to StudyJarChild when kind === 'study-jar'
    - Project.children Map accepts both FabricModChild and StudyJarChild
    - LoadedProject is assignable to/from Project (type alias correctness)
    - FabricModChild has all required fields: kind, name, rootPath, gradleConfig, sourcesJar, fabricMod, dependencyJars, filterConfig
    - StudyJarChild has all required fields: kind, name, jarPath, mtime, size, autoInclude, stats
  </behavior>
  <action>
Create `tests/project/types.test.ts` with runtime tests that verify the discriminated union works correctly at runtime. Use mock objects that conform to FabricModChild and StudyJarChild interfaces, put them in a children Map, and verify:

1. Iterating children and switching on `kind` correctly identifies each child type
2. Type guard functions (inline `child.kind === 'fabric-mod'`) allow accessing kind-specific fields
3. A Project with mixed children (one fabric mod, one study jar) can be iterated and each child's fields accessed after narrowing
4. LoadedProject alias works -- a variable typed as LoadedProject can be passed where Project is expected and vice versa
5. Empty Project (children = empty Map) is valid

Use `expect()` assertions on the runtime values, not just type-level checks. This ensures the type shapes are correct at runtime. Use tab indentation.
  </action>
  <verify>
    <automated>npx vitest run tests/project/types.test.ts -x</automated>
  </verify>
  <done>Type hierarchy tests pass. Discriminated union narrowing works at runtime. All required fields are present on each child type.</done>
</task>

</tasks>

<verification>
- `grep -c "export interface FabricModChild" src/project/types.ts` returns 1
- `grep -c "export type LoadedProject = Project" src/project/types.ts` returns 1
- `grep -c "export function getSoleFabricMod" src/project/compat.ts` returns 1
- `grep -c "export function getStudyJars" src/project/compat.ts` returns 1
- `npx vitest run tests/project/compat.test.ts -x` passes all tests
- `npx vitest run tests/project/types.test.ts -x` passes all tests
</verification>

<success_criteria>
- New type hierarchy defined in types.ts with Project, FabricModChild, StudyJarChild, ProjectChild, and LoadedProject alias
- Compat accessor layer in compat.ts with 8 exported functions
- Compat tests pass covering all accessor behaviors and error cases
- Type hierarchy tests pass verifying discriminated union correctness
- No existing tests are modified (they will fail due to type changes -- that is expected and will be fixed in Plan 02/03)
</success_criteria>

<output>
After completion, create `.planning/phases/23-type-foundation-and-projectstore/23-01-SUMMARY.md`
</output>
