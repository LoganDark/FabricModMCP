---
phase: 25.1-rework-tools-and-tests-for-native-architecture-without-compatibility-shims
verified: 2026-04-15T22:45:00Z
status: passed
score: 11/11 must-haves verified
gaps: []
---

# Phase 25.1: Tool Rework for Native Architecture Verification Report

**Phase Goal:** Rework tools and tests to be entirely native to the new architecture rather than using any compatibility shims. Cleanly separate Project creation, addition of fabric mod children or study jar children, and operations within an entire project or within any set of its children.
**Verified:** 2026-04-15T22:45:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `defaultChild` is renamed to `activeChild` everywhere | VERIFIED | `Project.activeChild` in types.ts line 98; namespace-resolver.ts line 29 uses `project.activeChild`; zero grep hits for `defaultChild` in src/ or tests/ |
| 2 | `defaultProject` is renamed to `activeProject` in ProjectStore and tests | VERIFIED | `private activeProject` in project-store.ts line 7; `setActive()` line 54; `getActive()` line 66; zero grep hits for `defaultProject` in src/ or tests/ |
| 3 | `LoadedProject` type alias is removed | VERIFIED | Not present in types.ts; zero grep hits for `LoadedProject` in src/ or tests/ |
| 4 | `ProjectStore.delete()` no longer blocks deletion of any project | VERIFIED | project-store.ts lines 42-48 — no guard, deletes unconditionally, clears activeProject if it matches |
| 5 | `compat.ts` is deleted and no source file imports from it | VERIFIED | `src/project/compat.ts` confirmed deleted; zero grep hits for `from.*compat` in src/ or tests/ |
| 6 | All old tool files are deleted | VERIFIED | `load-project.ts`, `unload-project.ts`, `get-project-metadata.ts`, `refresh-dependencies.ts`, `set-default-project.ts`, `remove-study-jar.ts` all confirmed deleted |
| 7 | All old test files for deleted tools are deleted | VERIFIED | `compat.test.ts`, `load-project.test.ts`, `unload-project.test.ts`, `get-project-metadata.test.ts`, `refresh-dependencies.test.ts`, `set-default-project.test.ts`, `remove-study-jar.test.ts` all confirmed deleted |
| 8 | Five lifecycle tools (create_project, add_fabric_mod, remove_project, remove_project_member, set_active_child) are implemented | VERIFIED | All five files exist, export correct registration functions, use native Project types, zero compat imports |
| 9 | Six info/refresh/utility tools (get_project_info, get_member_info, set_active_project, refresh_project, refresh_project_members, list_projects rework) are implemented | VERIFIED | All six files exist, export correct registration functions, no compat imports; list_projects returns simplified output |
| 10 | All browsing tools use direct child access via `getRootPathForScope` instead of compat functions | VERIFIED | Zero hits for `getRootPath\b` (old compat import) in src/; `getRootPathForScope` helper present in tool-helpers.ts lines 62-76; all 11 browsing tools import from tool-helpers.js |
| 11 | Full test suite passes | VERIFIED | `npx vitest run` exits 0 — 642 tests passing across 62 test files |

**Score:** 11/11 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/project/types.ts` | Project with `activeChild`, no `LoadedProject` | VERIFIED | Line 98: `activeChild?: string` on Project interface; no LoadedProject alias |
| `src/state/project-store.ts` | ProjectStore with `activeProject`, no CANNOT_DELETE_DEFAULT | VERIFIED | Line 7: `private activeProject`; delete() has no guard; setActive/getActive methods |
| `src/project/namespace-resolver.ts` | Uses `project.activeChild` | VERIFIED | Line 29: `scope ?? project.activeChild ?? inferSoleChildName(project)` |
| `src/tools/tool-helpers.ts` | Uses `Project` type, has `getRootPathForScope` | VERIFIED | Line 12: `import type { ..., Project } from '../project/types.js'`; lines 62-76: `getRootPathForScope` function |
| `src/tools/create-project.ts` | Creates empty project container | VERIFIED | Registers `create_project` tool; creates `{ name, children: new Map() }`; exports `registerCreateProjectTool` |
| `src/tools/add-fabric-mod.ts` | Loads fabric mod, no compat imports | VERIFIED | Registers `add_fabric_mod`; calls `loadFabricMod(path)`, handles collision, registers jars; exports `registerAddFabricModTool`; no compat import |
| `src/tools/remove-project.ts` | Removes project with cleanup | VERIFIED | Registers `remove_project`; shuts down JDT LS, closes jars, calls `projectStore.delete()`; exports `registerRemoveProjectTool` |
| `src/tools/remove-project-member.ts` | Removes members with cleanup, no compat | VERIFIED | Registers `remove_project_member`; validates then removes, handles both fabric-mod and study-jar kinds, clears activeChild; exports `registerRemoveProjectMemberTool`; no compat import |
| `src/tools/set-active-child.ts` | Sets activeChild, validates fabric-mod kind | VERIFIED | Registers `set_active_child`; validates kind !== 'fabric-mod' returns INVALID_CHILD_TYPE; sets `loadedProject.activeChild = child`; exports `registerSetActiveChildTool` |
| `src/tools/get-project-info.ts` | Project overview with member summaries | VERIFIED | Registers `get_project_info`; iterates children by kind; returns members array with name, kind, metadata; exports `registerGetProjectInfoTool` |
| `src/tools/get-member-info.ts` | Detailed member info, no compat | VERIFIED | Registers `get_member_info`; returns projectInfo/modInfo/jarInventory for fabric-mod, studyJarInfo for study-jar; no compat import; exports `registerGetMemberInfoTool` |
| `src/tools/set-active-project.ts` | Calls projectStore.setActive() | VERIFIED | Registers `set_active_project`; calls `projectStore.setActive(project)`; returns `activeProject: project`; exports `registerSetActiveProjectTool` |
| `src/tools/refresh-project.ts` | Refreshes all fabric mod children | VERIFIED | Registers `refresh_project`; iterates all fabric-mod children; calls discoverDependencies; returns error if no mods; no compat import; exports `registerRefreshProjectTool` |
| `src/tools/refresh-project-members.ts` | Named members refresh, empty array ok | VERIFIED | Registers `refresh_project_members`; empty array returns `{ refreshed: [], message: 'Nothing changed' }`; validates kinds before refreshing; no compat import |
| `src/tools/list-projects.ts` | Simplified output (name, memberCount, activeChild, isActive) | VERIFIED | Returns exactly `{ name, memberCount, activeChild, isActive }` per project; no compat imports; calls `projectStore.getActive()` |
| `src/tools/index.ts` | Registers all new tools, not old | VERIFIED | Imports and calls all 11 new lifecycle/info/refresh tool registrations; zero references to `registerLoadProjectTool` or other old registrations |
| `src/tools/descriptions.ts` | New SERVER_INSTRUCTIONS, new TOOL_DESCRIPTIONS | VERIFIED | SERVER_INSTRUCTIONS references `create_project`/`add_fabric_mod` workflow; TOOL_DESCRIPTIONS has entries for all new tools; zero entries for `load_project`, `unload_project`, `set_default_project`, `remove_study_jar`, `get_project_metadata`, `refresh_dependencies` |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `src/project/namespace-resolver.ts` | `src/project/types.ts` | `project.activeChild` reference | WIRED | Line 29: `scope ?? project.activeChild ?? inferSoleChildName(project)` |
| `src/state/project-store.ts` | Error messages | `set_active_project` suggestion | WIRED | Lines 112-113: AMBIGUOUS_PROJECT error references `set_active_project` |
| `src/tools/tool-helpers.ts` | `src/project/types.ts` | Direct `Project` type, no compat | WIRED | Line 12: `import type { ..., Project } from '../project/types.js'`; no compat import |
| `src/tools/index.ts` | All new tool files | `register*Tool(server)` calls | WIRED | 11 new tool registration calls present; old tool registrations absent |
| `src/tools/add-fabric-mod.ts` | `src/project/loader.ts` | `loadFabricMod` call | WIRED | Line 6: `import { loadFabricMod } from '../project/loader.js'`; line 30: `await loadFabricMod(path)` |
| `src/tools/remove-project-member.ts` | children.delete | Direct child removal | WIRED | Line 115: `loadedProject.children.delete(name)` with jar cleanup preceding it |
| `src/tools/get-project-info.ts` | project.children | Iterates children by kind | WIRED | Lines 25-42: for-loop over `loadedProject.children` building members array |
| `src/tools/refresh-project.ts` | `dependency-discovery.ts` | `discoverDependencies` call | WIRED | Line 4: import; line 62: `await discoverDependencies(...)` for each mod |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| REWORK-01 | 25.1-01 | Rename defaultChild/defaultProject to activeChild/activeProject; remove LoadedProject; clean ProjectStore | SATISFIED | `activeChild`/`activeProject` present throughout; `LoadedProject` absent; `CANNOT_DELETE_DEFAULT` guard absent |
| REWORK-02 | 25.1-01 | Update tool-helpers.ts to use Project type; update descriptions.ts error messages | SATISFIED | tool-helpers.ts imports `Project` not `LoadedProject`; error messages reference `set_active_project`, `get_member_info` etc. |
| REWORK-03 | 25.1-02 | Create 5 lifecycle tools: create_project, add_fabric_mod, remove_project, remove_project_member, set_active_child | SATISFIED | All 5 tool files exist, are substantive (not stubs), wired into index.ts |
| REWORK-04 | 25.1-03 | Create 6 info/refresh tools: get_project_info, get_member_info, set_active_project, refresh_project, refresh_project_members, list_projects rework | SATISFIED | All 6 tool files exist, are substantive, wired into index.ts |
| REWORK-05 | 25.1-04 | Migrate browsing tools off compat, delete compat.ts and old tools/tests, wire index.ts, rewrite SERVER_INSTRUCTIONS | SATISFIED | compat.ts deleted, old tools/tests deleted, 11 browsing tools use `getRootPathForScope`, index.ts has 28 new registrations, SERVER_INSTRUCTIONS rewritten |

**Note on REQUIREMENTS.md:** The REWORK-01 through REWORK-05 requirement IDs are defined only in the ROADMAP.md phase entry and plan frontmatter. They do not appear in `.planning/REQUIREMENTS.md`, which tracks CONT-*, DEP-*, TOOL-*, LSP-* IDs for the v1.4 milestone. This is consistent — REWORK-* are phase-local planning IDs for this inserted phase, not top-level product requirements. No orphaned requirements were found.

### Anti-Patterns Found

No significant anti-patterns detected. Reviewed all 11 new tool files and the modified infrastructure:

- No TODO/FIXME/placeholder comments in new tool files
- No stub return patterns (`return null`, `return {}`, etc.)
- No empty handlers
- No remaining compat function calls (`getRootPath`, `getSoleFabricMod`, `getStudyJars`, etc.) in src/tools/
- No remaining `LoadedProject`, `defaultChild`, `defaultProject` references in src/ or tests/

One minor informational note: `add-fabric-mod.ts` line 72 contains `// JDT LS workspace sync deferred to Phase 26` — this is an intentional deferral documented in the SUMMARY, not a placeholder hiding missing functionality. The tool is fully functional for its stated scope.

### Human Verification Required

None — all critical behaviors are verifiable programmatically:

- Tool registration and wiring verified via file existence, content inspection, and test passage
- Naming renames verified via grep (zero hits for old names)
- Deletion of old files verified via filesystem checks
- Test suite passage verified via `npx vitest run` (642/642 passing)

The only item that inherently requires runtime is whether the full MCP server starts and responds correctly to tool calls, but that is outside the scope of this phase's goal (which is about code architecture, not runtime behavior).

### Gaps Summary

No gaps. All 11 observable truths verified. All 17 critical artifacts exist and are substantive (not stubs) and wired correctly. All 5 REWORK requirements satisfied. Full test suite green at 642 tests.

---

_Verified: 2026-04-15T22:45:00Z_
_Verifier: Claude (gsd-verifier)_
