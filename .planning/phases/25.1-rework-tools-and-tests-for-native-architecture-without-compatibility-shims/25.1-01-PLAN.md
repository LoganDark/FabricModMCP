---
phase: 25.1-rework-tools-and-tests-for-native-architecture-without-compatibility-shims
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/project/types.ts
  - src/project/namespace-resolver.ts
  - src/state/project-store.ts
  - src/index.ts
  - src/tools/tool-helpers.ts
  - src/tools/descriptions.ts
  - tests/state/project-store.test.ts
  - tests/project/namespace-resolver.test.ts
autonomous: true
requirements:
  - REWORK-01
  - REWORK-02

must_haves:
  truths:
    - "defaultChild is renamed to activeChild everywhere in source and tests"
    - "defaultProject is renamed to activeProject in ProjectStore and tests"
    - "LoadedProject type alias is removed from types.ts"
    - "All source files importing LoadedProject now import Project directly"
    - "ProjectStore.delete() no longer blocks deletion of the 'default' project"
    - "ProjectStore error messages reference new tool names (set_active_project, remove_project)"
  artifacts:
    - path: "src/project/types.ts"
      provides: "Project type with activeChild field, no LoadedProject alias"
      contains: "activeChild"
    - path: "src/state/project-store.ts"
      provides: "ProjectStore with activeProject field, no CANNOT_DELETE_DEFAULT guard"
      contains: "activeProject"
    - path: "src/tools/tool-helpers.ts"
      provides: "tool-helpers using Project type instead of LoadedProject"
      contains: "Project"
  key_links:
    - from: "src/project/namespace-resolver.ts"
      to: "src/project/types.ts"
      via: "project.activeChild reference"
      pattern: "project\\.activeChild"
    - from: "src/state/project-store.ts"
      to: "src/tools/set-active-project.ts (future)"
      via: "error message suggestions"
      pattern: "set_active_project"
---

<objective>
Rename defaultChild/defaultProject to activeChild/activeProject throughout the codebase, remove the LoadedProject type alias, clean up ProjectStore (remove CANNOT_DELETE_DEFAULT guard, update error messages to reference new tool names), and update tool-helpers.ts to use Project type.

Purpose: Establish the naming foundation that all subsequent plans depend on. Every file that references the old naming must be updated before new tools are created.
Output: All source and test files use activeChild/activeProject naming, LoadedProject alias gone, ProjectStore freely allows deleting any project.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25.1-rework-tools-and-tests-for-native-architecture-without-compatibility-shims/25.1-CONTEXT.md
@.planning/phases/25.1-rework-tools-and-tests-for-native-architecture-without-compatibility-shims/25.1-RESEARCH.md

<interfaces>
From src/project/types.ts:
```typescript
export interface Project {
	name: string;
	defaultChild?: string;  // RENAME to activeChild
	children: Map<string, ProjectChild>;
	jdtls?: JdtLsSession;
}

export type LoadedProject = Project;  // DELETE this line
```

From src/state/project-store.ts:
```typescript
private defaultProject: string | undefined;  // RENAME to activeProject
setDefault(name: string): void  // RENAME to setActive
getDefault(): string | undefined  // RENAME to getActive
```

From src/project/namespace-resolver.ts:
```typescript
const targetChild = scope ?? project.defaultChild ?? inferSoleChildName(project);
// project.defaultChild -> project.activeChild
// Error suggestion: 'Set a default child with set_default_project' -> 'Set the active child with set_active_child'
```

From src/tools/tool-helpers.ts:
```typescript
import type { JarCategory, DependencyEntry, LoadedProject } from '../project/types.js';
// LoadedProject -> Project
// All function signatures using LoadedProject -> Project
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename activeChild/activeProject in types, ProjectStore, namespace-resolver, and index.ts</name>
  <files>src/project/types.ts, src/state/project-store.ts, src/project/namespace-resolver.ts, src/index.ts</files>
  <read_first>
    - src/project/types.ts
    - src/state/project-store.ts
    - src/project/namespace-resolver.ts
    - src/index.ts
    - tests/state/project-store.test.ts
    - tests/project/namespace-resolver.test.ts
  </read_first>
  <action>
**In `src/project/types.ts`:**
- Rename `defaultChild?: string` to `activeChild?: string` on the Project interface
- DELETE the line `export type LoadedProject = Project;` entirely

**In `src/state/project-store.ts`:**
- Rename field `private defaultProject: string | undefined` to `private activeProject: string | undefined`
- Rename method `setDefault(name: string)` to `setActive(name: string)` — update body to use `this.activeProject`
- Rename method `getDefault()` to `getActive()` — update body to return `this.activeProject`
- In `delete()`: REMOVE the entire `if (name === 'default')` guard block (lines 43-49 that throw CANNOT_DELETE_DEFAULT). Keep the `this.projects.delete(name)` and the `if (this.activeProject === name)` cleanup
- In `resolveProject()`: Update `this.defaultProject` references to `this.activeProject`
- In `resolveProject()` error for NO_PROJECTS_LOADED: Change suggestion from `'Load a project first using the load_project tool'` to `'Create a project with create_project, then add a fabric mod with add_fabric_mod'`
- In `resolveProject()` error for AMBIGUOUS_PROJECT: Change suggestion from `'Provide the project name or use set_default_project to set a default'` to `'Provide the project name or use set_active_project to set the active project'`
- In `set()` error for PROJECT_NAME_COLLISION: Change suggestion from `'Choose a different name or unload the existing project'` to `'Choose a different name or use remove_project to remove the existing one'`
- In `clear()`: Update `this.defaultProject` to `this.activeProject`

**In `src/project/namespace-resolver.ts`:**
- Change `project.defaultChild` to `project.activeChild` on line 29
- In the AMBIGUOUS_JAR_ID error: Change suggestion from `'Set a default child with set_default_project'` to `'Set the active child with set_active_child'`

**In `src/index.ts`:**
- Rename the local variable `defaultProject` to `initialProject` (it's just creating the startup project, not setting a "default")
- Update `projectStore.set('default', initialProject)` accordingly
- Keep the project name as `'default'` — that's the actual name, not a concept

**In `tests/state/project-store.test.ts`:**
- Rename all `defaultProject` variable names in test code to use `activeProject` or contextual names
- Update `store.setDefault(...)` calls to `store.setActive(...)`
- Update `store.getDefault()` calls to `store.getActive()`
- REMOVE the test case for CANNOT_DELETE_DEFAULT (the one testing that deleting 'default' throws)
- Add a new test: `it('allows deleting any project including default')` that verifies `store.delete('default')` succeeds
- Update error message assertions: `set_default_project` -> `set_active_project`, `load_project` -> `create_project`/`add_fabric_mod`

**In `tests/project/namespace-resolver.test.ts`:**
- Change all `project.defaultChild` assignments to `project.activeChild`
- Update test description strings: "defaultChild" -> "activeChild"
- Update error suggestion assertion: `set_default_project` -> `set_active_child`
  </action>
  <verify>
    <automated>npx vitest run tests/state/project-store.test.ts tests/project/namespace-resolver.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/project/types.ts contains `activeChild?: string` and does NOT contain `defaultChild` or `LoadedProject`
    - src/state/project-store.ts contains `private activeProject` and does NOT contain `defaultProject` or `CANNOT_DELETE_DEFAULT`
    - src/state/project-store.ts contains `set_active_project` in error suggestions
    - src/project/namespace-resolver.ts contains `project.activeChild` and does NOT contain `project.defaultChild`
    - src/project/namespace-resolver.ts contains `set_active_child` in error suggestions
    - src/index.ts contains `initialProject` and does NOT contain `defaultProject` as a variable name
    - tests/state/project-store.test.ts calls `store.setActive` and `store.getActive`
    - tests/project/namespace-resolver.test.ts contains `project.activeChild`
    - `grep -r "defaultChild\|defaultProject\|LoadedProject\|CANNOT_DELETE_DEFAULT" src/ tests/state/ tests/project/` returns zero results
  </acceptance_criteria>
  <done>All renames propagated in types, ProjectStore, namespace-resolver, index.ts and their tests. LoadedProject alias deleted. CANNOT_DELETE_DEFAULT guard removed. All error messages reference new tool names.</done>
</task>

<task type="auto">
  <name>Task 2: Update tool-helpers.ts and descriptions.ts to use Project type and new naming</name>
  <files>src/tools/tool-helpers.ts, src/tools/descriptions.ts</files>
  <read_first>
    - src/tools/tool-helpers.ts
    - src/tools/descriptions.ts
    - src/project/types.ts (after Task 1 changes)
  </read_first>
  <action>
**In `src/tools/tool-helpers.ts`:**
- Change import `import type { JarCategory, DependencyEntry, LoadedProject } from '../project/types.js'` to `import type { JarCategory, DependencyEntry, Project } from '../project/types.js'`
- Replace ALL occurrences of `LoadedProject` in type annotations with `Project` (function signatures for `resolveProjectSafely`, `resolveClassSource`, `getDependenciesForTool`, `processNavigationLocations`, and any local variable types)
- The `getRootPath` import from compat.ts is still used in `resolveClassSource` — leave this import for now (Plan 04 will remove it when migrating browsing tools). If the import causes a compile error because compat.ts doesn't exist yet, it won't — compat.ts is not deleted until Plan 04.
- In `handleSymbolPositionError`: Change `'Check available jars with get_project_metadata'` to `'Check available jars with get_member_info or get_project_info'`
- In `handleClassSourceError`: Same change — `get_project_metadata` -> `get_member_info or get_project_info`

**In `src/tools/descriptions.ts`:**
- In `PARAMS.scope` description: Change `'Child name to scope to (e.g., fabric mod name). Bare jar IDs resolve within this child\'s namespace.'` — this is fine as-is, no changes needed
- In `SERVER_INSTRUCTIONS`: This will be fully rewritten in Plan 04 when all tools are finalized. For now, update only:
  - `"set_default_project"` -> `"set_active_project"` (if it appears in instructions text)
  - `"get_project_metadata"` -> `"get_project_info"` (if it appears)
  - Leave the rest for Plan 04's full rewrite
- Do NOT add new tool descriptions yet — Plans 02 and 03 will add them when creating the tools
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/tools/tool-helpers.ts does NOT contain `LoadedProject` — only `Project`
    - src/tools/tool-helpers.ts import line reads `import type { JarCategory, DependencyEntry, Project } from '../project/types.js'`
    - src/tools/tool-helpers.ts contains `get_member_info or get_project_info` in error suggestion strings
    - src/tools/descriptions.ts does NOT contain `set_default_project` in SERVER_INSTRUCTIONS
    - Full test suite passes (`npx vitest run` exits 0)
  </acceptance_criteria>
  <done>tool-helpers.ts uses Project type everywhere. descriptions.ts updated with active naming. Full 661-test suite passes green.</done>
</task>

</tasks>

<verification>
- `grep -rn "LoadedProject" src/` returns zero results
- `grep -rn "defaultChild\|defaultProject" src/ tests/state/ tests/project/` returns zero results
- `grep -rn "CANNOT_DELETE_DEFAULT" src/ tests/` returns zero results
- `npx vitest run` passes all 661+ tests
</verification>

<success_criteria>
All source and test files use activeChild/activeProject naming. LoadedProject alias is deleted. ProjectStore allows deleting any project. Error messages reference new tool names. Full test suite is green.
</success_criteria>

<output>
After completion, create `.planning/phases/25.1-rework-tools-and-tests-for-native-architecture-without-compatibility-shims/25.1-01-SUMMARY.md`
</output>
