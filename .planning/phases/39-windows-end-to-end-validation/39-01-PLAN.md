---
phase: 39-windows-end-to-end-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/WINDOWS-SUPPORT.md
autonomous: true
requirements:
  - UNIX-03

must_haves:
  truths:
    - "`docs/WINDOWS-SUPPORT.md` exists as a new standalone file (the `docs/` directory is created by this plan; no prior `docs/` directory exists in the repo)."
    - "The file contains a Java discovery priority chain section that lists the 5 slots VERBATIM from `src/jdtls/java-discovery.ts:8-14` (quoting the doc-comment header text with the jsdoc gutter ` * ` removed)."
    - "The file contains a JDT LS install locations section that lists the Windows 4-path chain and the Unix 3-path chain in the exact order produced by `src/platform/index.ts:70-87` `jdtlsCandidateDirs()`, with `JDTLS_HOME` documented as heading each chain (consumed by `findJdtLs` in `src/jdtls/client.ts` BEFORE iterating candidates)."
    - "The file contains a `Known limitations` section that documents the Windows 260-char path limit with the `HKLM\\SYSTEM\\CurrentControlSet\\Control\\FileSystem\\LongPathsEnabled` registry mitigation, AND a WSL2 note (WSL2 is Linux from FabricModMCP's perspective)."
    - "The file contains an installation pre-requisites section listing Java 21+, JDT LS milestone, Node.js 22+, and pnpm."
    - "The file ends with the D-18 cross-reference footer text exactly: `Source of truth for the contract: see REQUIREMENTS.md WIN-01/WIN-02/JAVA-01/JAVA-02.` followed by `Implementation: \\`src/jdtls/java-discovery.ts\\` (Java) and \\`src/jdtls/client.ts\\` \\`findJdtLs\\` (JDT LS).`"
    - "File length is between 80 and 150 lines (Claude's Discretion target from CONTEXT.md)."
    - "The full v1.5 + v1.6 vitest suite still passes after this plan (UNIX-03 regression guard — docs-only change must not regress code tests)."
  artifacts:
    - path: "docs/WINDOWS-SUPPORT.md"
      provides: "Standalone user-facing Windows documentation with verbatim priority chains + known limitations + D-18 cross-reference footer"
      contains: "Source of truth for the contract"
  key_links:
    - from: "docs/WINDOWS-SUPPORT.md"
      to: "REQUIREMENTS.md WIN-01/WIN-02/JAVA-01/JAVA-02"
      via: "verbatim D-18 footer text"
      pattern: "REQUIREMENTS\\.md WIN-01/WIN-02/JAVA-01/JAVA-02"
    - from: "docs/WINDOWS-SUPPORT.md"
      to: "src/jdtls/java-discovery.ts + src/jdtls/client.ts"
      via: "verbatim D-18 footer text"
      pattern: "src/jdtls/java-discovery\\.ts.*src/jdtls/client\\.ts"
---

<objective>
Create `docs/WINDOWS-SUPPORT.md` — the new standalone user-facing Windows documentation for v1.6. The file inlines both priority chains (Java + JDT LS) verbatim from source-of-truth comments, documents known Windows limitations (260-char path limit, WSL2 note), lists installation pre-requisites, and ends with the D-18 cross-reference footer that points readers to REQUIREMENTS.md + the implementing source files.

Purpose: Provide the canonical v1.6 Windows-user reference. The repo has no README yet (deferred to v1.7+ per D-10); this file is the v1.6 Windows-user touchpoint. The verbatim-quoting convention (D-17) and cross-reference footer (D-18) mitigate drift across the 3+ surfaces that document the same contract.

Output: One new file at `docs/WINDOWS-SUPPORT.md` (the `docs/` directory is created by this plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-windows-end-to-end-validation/39-CONTEXT.md
@.planning/phases/39-windows-end-to-end-validation/39-RESEARCH.md
@.planning/phases/39-windows-end-to-end-validation/39-PATTERNS.md
@.planning/phases/39-windows-end-to-end-validation/39-VALIDATION.md

@src/jdtls/java-discovery.ts
@src/platform/index.ts
@src/jdtls/client.ts

<interfaces>
<!-- Source-of-truth excerpts the executor must quote verbatim. Do NOT paraphrase. -->

From src/jdtls/java-discovery.ts lines 8-14 (jsdoc gutter ` * ` to be stripped):
```
 * Slot order for `discoverJava`:
 *   1. `--java-home` (module-state `configuredJavaHome`)
 *   2. `org.gradle.java.home` from `<projectRoot>/gradle.properties`
 *   3. `JAVA_HOME` env var
 *   4. `java` on PATH (libuv handles PATH lookup + PATHEXT on Windows)
 *   5. Scan common install locations from `commonJavaLocations()` with
 *      vendor-aware layout map
```

From src/platform/index.ts lines 70-87 (jdtlsCandidateDirs() body — extract chain in source order):
- Windows: %LOCALAPPDATA%\jdtls → %ProgramFiles%\jdtls → %USERPROFILE%\jdtls → %LOCALAPPDATA%\nvim-data\mason\packages\jdtls
- Linux/macOS: ~/.local/share/jdtls → /usr/local/share/jdtls → ~/jdtls
- JDTLS_HOME (consumed by src/jdtls/client.ts findJdtLs BEFORE iterating candidates) heads each OS chain.

Required D-18 footer text (verbatim, exactly two lines, at file bottom):
```
Source of truth for the contract: see REQUIREMENTS.md WIN-01/WIN-02/JAVA-01/JAVA-02.
Implementation: `src/jdtls/java-discovery.ts` (Java) and `src/jdtls/client.ts` `findJdtLs` (JDT LS).
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/WINDOWS-SUPPORT.md with verbatim priority chains and D-18 footer</name>
  <files>docs/WINDOWS-SUPPORT.md</files>
  <read_first>
    - src/jdtls/java-discovery.ts (read lines 1-20 — extract the verbatim 5-slot Java priority chain text from the jsdoc header at lines 8-14)
    - src/platform/index.ts (read lines 50-100 — extract the verbatim Windows 4-path and Unix 3-path lists from jdtlsCandidateDirs() body at lines 70-87)
    - src/jdtls/client.ts (read lines 110-150 — confirm JDTLS_HOME is consumed by findJdtLs BEFORE iterating candidates; cite in the JDT LS section)
    - .planning/phases/39-windows-end-to-end-validation/39-CONTEXT.md (D-10/D-11/D-18 — required content and footer text)
    - .planning/phases/39-windows-end-to-end-validation/39-PATTERNS.md (Pattern Assignments section — recommended top-level structure for the doc; D-18 verbatim footer string)
  </read_first>
  <action>
Create new file `docs/WINDOWS-SUPPORT.md` (this creates the `docs/` directory — it does not exist in the repo today, per `ls docs/` returning ENOENT verified in 39-RESEARCH.md). Tab indentation is irrelevant for markdown; use standard markdown conventions. Use US English. The file MUST contain these sections in this order (per D-11 and 39-PATTERNS.md recommended layout):

1. `# Windows Support` H1 title.
2. Brief intro paragraph (1 short paragraph): state that FabricModMCP supports native Windows, macOS, Linux, and WSL2; the MCP server runs on Node 22+; JDT LS spawns on Java 21+.
3. `## Installation Prerequisites` H2 with bullet list: Java 21+ (link to https://adoptium.net/), JDT LS milestone (link to https://download.eclipse.org/jdtls/milestones/), Node.js 22+ (link to https://nodejs.org/), pnpm (link to https://pnpm.io/).
4. `## Java Discovery Priority Chain` H2. Short prose intro pointing to where the chain is implemented (`src/jdtls/java-discovery.ts` `discoverJava`). Then quote the 5 slots VERBATIM from `src/jdtls/java-discovery.ts:8-14` with the leading jsdoc gutter ` * ` stripped. Use a numbered list (1.–5.).
5. `## JDT LS Install Locations` H2. Short prose intro stating that `JDTLS_HOME` env var heads each OS chain (consumed by `findJdtLs` in `src/jdtls/client.ts` before iterating candidates). Then two subsections: `### Windows` (numbered list of the 4 Windows paths from `src/platform/index.ts:70-87` in exact source order) and `### Linux / macOS` (numbered list of the 3 Unix paths in exact source order).
6. `## Known Limitations` H2 with two subsections:
   - `### 260-character path limit` — explain the Windows MAX_PATH constraint, mitigation via `HKLM\SYSTEM\CurrentControlSet\Control\FileSystem\LongPathsEnabled = 1` registry key OR Group Policy "Enable Win32 long paths" setting; link to https://learn.microsoft.com/en-us/windows/win32/fileio/maximum-file-path-limitation.
   - `### WSL2` — state that WSL2 is Linux from FabricModMCP's perspective (`process.platform === 'linux'`); cross-FS access between Windows and WSL is slow; recommend running fully inside WSL2 (project files inside WSL filesystem, MCP server running inside WSL) for users hitting Windows-native quirks.
7. Final footer block — VERBATIM D-18 text, exactly two lines, no surrounding heading, immediately after the Known Limitations section:
   ```
   Source of truth for the contract: see REQUIREMENTS.md WIN-01/WIN-02/JAVA-01/JAVA-02.
   Implementation: `src/jdtls/java-discovery.ts` (Java) and `src/jdtls/client.ts` `findJdtLs` (JDT LS).
   ```

Constraints:
- Total file length MUST land between 80 and 150 lines (per CONTEXT.md Claude's Discretion guidance; tone is technical but not exhaustive, audience is Minecraft modders + Claude Code users).
- Do NOT explain what `%LOCALAPPDATA%` or `$JDTLS_HOME` mean (audience knows).
- Do NOT create `README.md`; D-10 explicitly defers it to v1.7+.
- Do NOT add a Troubleshooting section (Open Question §4 in RESEARCH.md — Claude's Discretion; we skip it to stay inside the 80-150 line budget and inside the D-11 scope).
- Use `org.gradle.java.home` with backticks consistent with source code formatting.
- Use US English throughout per CLAUDE.md / project conventions.
  </action>
  <verify>
    <automated>test -f docs/WINDOWS-SUPPORT.md \
      && [ "$(wc -l &lt; docs/WINDOWS-SUPPORT.md)" -ge 80 ] \
      && [ "$(wc -l &lt; docs/WINDOWS-SUPPORT.md)" -le 150 ] \
      && grep -Fq 'org.gradle.java.home' docs/WINDOWS-SUPPORT.md \
      && grep -Fq 'JAVA_HOME' docs/WINDOWS-SUPPORT.md \
      && grep -Fq '--java-home' docs/WINDOWS-SUPPORT.md \
      && grep -Fq 'commonJavaLocations' docs/WINDOWS-SUPPORT.md \
      && grep -Fq 'JDTLS_HOME' docs/WINDOWS-SUPPORT.md \
      && grep -Fq '%LOCALAPPDATA%\jdtls' docs/WINDOWS-SUPPORT.md \
      && grep -Fq '%LOCALAPPDATA%\nvim-data\mason\packages\jdtls' docs/WINDOWS-SUPPORT.md \
      && grep -Fq '~/.local/share/jdtls' docs/WINDOWS-SUPPORT.md \
      && grep -Fq '/usr/local/share/jdtls' docs/WINDOWS-SUPPORT.md \
      && grep -Fq 'LongPathsEnabled' docs/WINDOWS-SUPPORT.md \
      && grep -Fq 'WSL2' docs/WINDOWS-SUPPORT.md \
      && grep -Fq 'REQUIREMENTS.md WIN-01/WIN-02/JAVA-01/JAVA-02' docs/WINDOWS-SUPPORT.md \
      && grep -Fq '`src/jdtls/java-discovery.ts` (Java) and `src/jdtls/client.ts` `findJdtLs` (JDT LS)' docs/WINDOWS-SUPPORT.md \
      && grep -Fq '%ProgramFiles%\jdtls' docs/WINDOWS-SUPPORT.md \
      && grep -Fq '%USERPROFILE%\jdtls' docs/WINDOWS-SUPPORT.md \
      && grep -Fq '~/jdtls' docs/WINDOWS-SUPPORT.md \
      && grep -Fq 'vendor-aware layout map' docs/WINDOWS-SUPPORT.md \
      && pnpm test -- run</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `docs/WINDOWS-SUPPORT.md` (the `docs/` directory is created by writing the file).
    - `wc -l docs/WINDOWS-SUPPORT.md` reports a count in the inclusive range [80, 150].
    - `grep -F` succeeds for ALL of these literal strings (Java chain slots): `--java-home`, `org.gradle.java.home`, `JAVA_HOME`, `commonJavaLocations`.
    - `grep -F` succeeds for ALL of these literal strings (JDT LS chain): `JDTLS_HOME`, `%LOCALAPPDATA%\jdtls`, `%LOCALAPPDATA%\nvim-data\mason\packages\jdtls`, `~/.local/share/jdtls`, `/usr/local/share/jdtls`.
    - `grep -F` succeeds for `LongPathsEnabled` and `WSL2`.
    - `grep -F` succeeds for the D-18 footer literal `REQUIREMENTS.md WIN-01/WIN-02/JAVA-01/JAVA-02` AND `` `src/jdtls/java-discovery.ts` (Java) and `src/jdtls/client.ts` `findJdtLs` (JDT LS) ``.
    - `pnpm test -- run` exits 0 (UNIX-03 regression guard: docs-only change must not regress existing tests).
  </acceptance_criteria>
  <done>
The new `docs/WINDOWS-SUPPORT.md` file documents the v1.6 Windows surface with verbatim priority chains, known limitations, and the cross-reference footer; vitest suite stays green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs ↔ source | Docs reference source-of-truth comments; drift between source and docs is the risk. |
| docs ↔ user actions | Doc instructions (registry edit, env var setting) execute on the user's machine. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Information Disclosure | docs/WINDOWS-SUPPORT.md priority chains | mitigate | Quote VERBATIM from `src/jdtls/java-discovery.ts:8-14` and `src/platform/index.ts:70-87`; end with D-18 cross-reference footer so a reader can verify against current source. Paraphrase introduces drift on next refactor. |
| T-39-02 | Tampering | Registry mitigation instruction (`HKLM\SYSTEM\CurrentControlSet\Control\FileSystem\LongPathsEnabled`) | accept | Standard Microsoft-documented setting; we link to Microsoft Learn. User executes on their own machine; no FabricModMCP code touches the registry. |
| T-39-03 | Repudiation | Doc accuracy claims about JDT LS chain order | mitigate | Source order from `jdtlsCandidateDirs()` is load-bearing — JDTLS_HOME documented as heading each list per `src/jdtls/client.ts:117-134` consumption order. Acceptance criteria assert all path literals via `grep -F`. |
</threat_model>

<verification>
After completion:
- File exists, length in range, all grep gates pass, vitest suite green.
- The D-18 footer is the load-bearing drift-mitigation artifact; verify it is the LAST content block in the file (not buried).
</verification>

<success_criteria>
A reader hitting `docs/WINDOWS-SUPPORT.md` cold learns: (a) the four discovery entry points for Java in priority order; (b) where the MCP server looks for JDT LS on Windows and Unix; (c) the 260-char-limit mitigation; (d) the WSL2 escape hatch; (e) where to find the source of truth if anything in the doc looks wrong.
</success_criteria>

<output>
Create `.planning/phases/39-windows-end-to-end-validation/39-01-SUMMARY.md` when done, summarizing the file created, the verbatim source ranges quoted, and confirming the D-18 footer is present.
</output>
</content>
</invoke>