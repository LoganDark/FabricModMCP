---
phase: 39-windows-end-to-end-validation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - UNIX-03

must_haves:
  truths:
    - "The Phase 39 `**Plans**:` subsection in `.planning/ROADMAP.md` (currently lines 181-185 of the original file, holding stale Phase 36 entries) lists the actual Phase 39 plans 01-05 with their objectives and requirement annotations."
    - "No `36-0X-PLAN.md` references remain in the Phase 39 section of ROADMAP.md (the copy-paste bug from RESEARCH.md Pitfall §5 is eliminated)."
    - "The replacement plan list uses the Phase 36/37/38 precedent format: header line `**Plans**: 5 plans` then `  - [ ] 39-NN-PLAN.md — <description> (<req IDs or label>)` per plan, exactly two-space indent, dash, space, checkbox, space, filename, em-dash with surrounding spaces, description, parenthesized annotation."
    - "All other content in `.planning/ROADMAP.md` is unchanged — the change is localized to the Phase 39 Plans subsection."
    - "The full v1.5 + v1.6 vitest suite still passes after this plan (UNIX-03 regression guard — ROADMAP edit must not regress code tests)."
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Modified — Phase 39 Plans subsection rewritten with the actual Phase 39 plan list, replacing the stale Phase 36 copy-paste"
      contains: "39-01-PLAN.md"
  key_links:
    - from: ".planning/ROADMAP.md Phase 39 Plans subsection"
      to: "Phase 39 plan files"
      via: "plan filename references"
      pattern: "39-0[1-5]-PLAN\\.md"
---

<objective>
Rewrite the `**Plans**:` subsection inside the Phase 39 block of `.planning/ROADMAP.md` to list the actual Phase 39 plans 01-05, replacing the stale copy-paste of Phase 36's plan list (RESEARCH.md Pitfall §5 — VERIFIED via ROADMAP read 2026-05-24, lines 181-185 in current file hold `36-0X-PLAN.md` entries).

Purpose: Without this fix, ROADMAP.md is self-contradictory after Phase 39 plans land — the Phase 39 section says "Phase 36 plans" while the actual Phase 39 plan files exist on disk. Verifier (`/gsd:verify-phase`) and reading humans need a consistent ROADMAP. Mechanical localized edit; no other section of ROADMAP.md changes.

Output: One modified file — `.planning/ROADMAP.md` — with the Phase 39 `**Plans**:` subsection rewritten.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-windows-end-to-end-validation/39-CONTEXT.md
@.planning/phases/39-windows-end-to-end-validation/39-RESEARCH.md
@.planning/phases/39-windows-end-to-end-validation/39-PATTERNS.md

<interfaces>
<!-- Exact stale content to replace and target format. -->

Current stale Phase 39 Plans subsection in .planning/ROADMAP.md (lines ~181-185 of the original file — confirm exact line range with grep before editing):
```
**Plans**: 5 plans (4 original + 1 gap closure)
  - [x] 36-01-PLAN.md — Create `src/platform/uri.ts` pure helper module + tests (WIN-03 / UNIX-02 foundation)
  - [x] 36-02-PLAN.md — Forward sweep (7 sites) + reverse sweep (1 site) across `client.ts`, `workspace-sync.ts`, `remove-project-member.ts`, `tool-helpers.ts` (WIN-03)
  - [x] 36-03-PLAN.md — `uri-mapper.ts` drive-letter case-fold (`prefixMatches` state machine) + internal `toFileUri` migration + Windows-mocked tests (WIN-05 / UNIX-02)
  - [ ] 36-04-PLAN.md — `workspace-sync.ts` hardening: ZIP split-and-spread + traversal guard + `rm` retry options + tests (WIN-04 / WIN-06 / WIN-07)
```

These five lines belong to the Phase 39 block (the H3 `### Phase 39: Windows End-to-End Validation` heading begins at line 171 of the original file). They MUST be replaced with the actual Phase 39 plan list.

Format precedent (from `.planning/ROADMAP.md` line 118-120 Phase 35, 133-138 Phase 36, 151-156 Phase 37, 168-169 Phase 38):
- Header line: `**Plans**: N plans` (no `M original + K gap closure` annotation until/unless gap closure triggers).
- Each plan: `  - [ ] NN-NN-PLAN.md — <one-line description> (<requirement IDs or label>)`. Exactly two spaces of indent, dash, space, checkbox, space, plan filename, em-dash with surrounding spaces, description, parenthesized annotation. Checkboxes start `[ ]` (incomplete).

Phase 39 plan list to write in:
1. `39-01-PLAN.md` — Create `docs/WINDOWS-SUPPORT.md` with verbatim priority chains + known limitations + D-18 cross-reference footer (UNIX-03 / validation surface for WIN-* / JAVA-*)
2. `39-02-PLAN.md` — Insert `### Platform Support` subsection into `CLAUDE.md ## Technology Stack` with verbatim priority chains + D-18 footer (UNIX-03 / validation surface for WIN-* / JAVA-*)
3. `39-03-PLAN.md` — Rewrite ROADMAP.md Phase 39 Plans subsection (replace stale Phase 36 copy-paste) (UNIX-03 housekeeping)
4. `39-04-PLAN.md` — Human-executed 4-row Java-discovery matrix on Windows + evidence capture in `39-VERIFICATION.md` (validation: WIN-01..WIN-07, JAVA-01..JAVA-05, UNIX-02)
5. `39-05-PLAN.md` — UNIX-03 regression sweep: full vitest suite green on macOS (and Linux if accessible) (UNIX-03)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite ROADMAP.md Phase 39 Plans subsection</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (read lines 165-201 — confirm Phase 39 block boundaries, locate the stale `**Plans**:` subsection exact line numbers, verify Phase 38 single-plan block at lines ~168-169 as the immediately-preceding format precedent)
    - .planning/phases/39-windows-end-to-end-validation/39-RESEARCH.md (Pitfall §5 — copy-paste-bug context)
    - .planning/phases/39-windows-end-to-end-validation/39-PATTERNS.md (Pattern Assignments §"`.planning/ROADMAP.md` Phase 39 Plans subsection" — header-line format + per-plan-line format with two-space indent + em-dash with surrounding spaces + checkbox state)
  </read_first>
  <action>
Edit `.planning/ROADMAP.md`. Locate the Phase 39 block (begins at `### Phase 39: Windows End-to-End Validation` heading near line 171). Within that block, locate the existing `**Plans**:` subsection (currently 5 lines holding `36-0X-PLAN.md` entries — confirm the exact line range with grep before the edit). Replace those 5 lines (the header line plus the 4 bullet lines) with EXACTLY this content (5 lines total — one header, five bullets):

```
**Plans**: 5 plans
  - [ ] 39-01-PLAN.md — Create `docs/WINDOWS-SUPPORT.md` with verbatim priority chains + known limitations + D-18 cross-reference footer (UNIX-03 / validation surface for WIN-* / JAVA-*)
  - [ ] 39-02-PLAN.md — Insert `### Platform Support` subsection into `CLAUDE.md` `## Technology Stack` with verbatim priority chains + D-18 footer (UNIX-03 / validation surface for WIN-* / JAVA-*)
  - [ ] 39-03-PLAN.md — Rewrite `.planning/ROADMAP.md` Phase 39 Plans subsection (replace stale Phase 36 copy-paste) (UNIX-03 housekeeping)
  - [ ] 39-04-PLAN.md — Human-executed 4-row Java-discovery matrix on Windows + evidence capture in `39-VERIFICATION.md` (validation: WIN-01..WIN-07, JAVA-01..JAVA-05, UNIX-02)
  - [ ] 39-05-PLAN.md — UNIX-03 regression sweep: full vitest suite green on macOS (and Linux if accessible) (UNIX-03)
```

Constraints:
- Header line MUST be `**Plans**: 5 plans` (no `4 original + 1 gap closure` — Phase 39 has 5 original plans; the `M original + K gap closure` notation is reserved for follow-up commits if D-13 gap-closure plans get added later).
- Each bullet line uses TWO spaces of indent, then `- [ ] ` (dash, space, checkbox, space), then plan filename, then ` — ` (space, em-dash U+2014, space — match Phase 36/37/38 precedent exactly), then description, then parenthesized annotation.
- All five checkboxes start as `[ ]` (incomplete). The verifier will flip them to `[x]` post-verification per Phase 36-38 precedent.
- Do NOT modify any other content in `.planning/ROADMAP.md` — the change is strictly localized to the Phase 39 `**Plans**:` subsection.
- Do NOT modify the Phase 39 `**Goal**:`, `**Depends on**:`, `**Requirements**:`, or `**Success Criteria**:` lines (those were updated in the CONTEXT.md commit per D-12 and must remain intact).
- After the edit, grep `^### Phase 3[5-9]:` in ROADMAP.md MUST return exactly 5 matches (one per phase heading).
- After the edit, grep `36-0[1-4]-PLAN\.md` inside the Phase 39 block (between the Phase 39 H3 heading and the next H2 heading `## Progress`) MUST return zero matches.
  </action>
  <verify>
    <automated>awk '/^### Phase 39:/,/^## Progress$/' .planning/ROADMAP.md | grep -qE '^\*\*Plans\*\*: 5 plans$' \
      &amp;&amp; awk '/^### Phase 39:/,/^## Progress$/' .planning/ROADMAP.md | grep -cE '^  - \[ \] 39-0[1-5]-PLAN\.md — ' | grep -qE '^5$' \
      &amp;&amp; ! awk '/^### Phase 39:/,/^## Progress$/' .planning/ROADMAP.md | grep -qE '36-0[1-4]-PLAN\.md' \
      &amp;&amp; [ "$(grep -cE '^### Phase 3[5-9]:' .planning/ROADMAP.md)" -eq 5 ] \
      &amp;&amp; pnpm test -- run</automated>
  </verify>
  <acceptance_criteria>
    - Inside the Phase 39 block (between `### Phase 39:` H3 heading and the next `## Progress` H2 heading), `awk` + `grep` confirms exactly one `**Plans**: 5 plans` header line.
    - Inside the same Phase 39 block, exactly 5 lines match the regex `^  - \[ \] 39-0[1-5]-PLAN\.md — ` (one per plan, all `[ ]` unchecked, exact two-space indent + em-dash format).
    - Inside the Phase 39 block, zero matches for `36-0[1-4]-PLAN\.md` (stale copy-paste eliminated).
    - The five `### Phase 3[5-9]:` H3 headings are preserved (count = 5).
    - `pnpm test -- run` exits 0 (UNIX-03 regression guard — ROADMAP edit must not regress code tests).
  </acceptance_criteria>
  <done>
ROADMAP.md Phase 39 Plans subsection lists the actual Phase 39 plans 01-05 in the precedent format; no stale Phase 36 references remain; vitest suite stays green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ROADMAP.md ↔ plan files on disk | ROADMAP claims must agree with files in `.planning/phases/39-*/` to avoid self-contradiction. |
| ROADMAP.md ↔ verifier (`/gsd:verify-phase`) | Verifier reads the Plans subsection to enumerate what to check. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-06 | Repudiation | ROADMAP claims about Phase 39 plans | mitigate | Acceptance criteria assert exactly 5 `39-0[1-5]-PLAN.md` bullet lines AND zero `36-0[1-4]-PLAN.md` references inside the Phase 39 block. The awk-bounded grep prevents leaking edits into adjacent phase blocks. |
| T-39-07 | Tampering | Inadvertent modification of adjacent phase blocks (Phase 38 or Progress table) | mitigate | Acceptance criterion `grep -cE '^### Phase 3[5-9]:' .planning/ROADMAP.md eq 5` ensures all 5 phase H3 headings preserved; localized edit limited to Phase 39's Plans subsection. |
</threat_model>

<verification>
After completion:
- Phase 39 block in ROADMAP.md lists actual Phase 39 plans 01-05 with correct format.
- No `36-0X-PLAN.md` references remain in the Phase 39 block.
- All other ROADMAP content preserved.
- vitest suite still green.
</verification>

<success_criteria>
A reader navigating to the Phase 39 section in ROADMAP.md sees the five actual plans for this phase. The verifier can enumerate plan filenames from the ROADMAP and cross-check against on-disk files without confusion.
</success_criteria>

<output>
Create `.planning/phases/39-windows-end-to-end-validation/39-03-SUMMARY.md` when done, summarizing the before/after of the Phase 39 Plans subsection and confirming surrounding content unchanged.
</output>
</content>
</invoke>