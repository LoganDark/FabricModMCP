---
phase: 39-windows-end-to-end-validation
plan: 05
type: execute
wave: 3
depends_on:
  - 39-01
  - 39-02
  - 39-03
  - 39-04
files_modified:
  - .planning/phases/39-windows-end-to-end-validation/39-VERIFICATION.md
autonomous: true
requirements:
  - UNIX-03

must_haves:
  truths:
    - "`pnpm test -- run` exits 0 on the developer's macOS host (M4 Max running macOS 25.5.0 per CLAUDE.md user environment) with zero new failures and zero new skips compared to the Phase 38 baseline (UNIX-03)."
    - "If a Linux host is accessible to the developer at execution time, `pnpm test -- run` also exits 0 on that Linux host with zero new failures and zero new skips; otherwise the `## UNIX-03 Regression Sweep` section in `39-VERIFICATION.md` explicitly states `Linux not verified in this phase` per RESEARCH.md Open Question §3 fallback (D-15 spirit — genuinely-environmental edge case explicit-document escape valve)."
    - "The UNIX-03 result block in `39-VERIFICATION.md` records: command run, exit code, total test count, pass/fail/skip counts, and a comparison to the Phase 38 baseline (the latter expressed as `matches Phase 38 baseline` OR `differs from Phase 38 baseline by N tests — investigated and resolved: <one-line>` OR `differs — opened gap-closure plan 39-NN`)."
    - "No `tests/**` files are modified by this plan (UNIX-03 requires existing tests pass UNCHANGED — CONTEXT.md `Files this phase MUST NOT modify` includes `tests/**`)."
    - "No `src/**` files are modified by this plan (CONTEXT.md `Files this phase MUST NOT modify` includes `src/**` — only gap-closure plans triggered by failures may touch `src/**` per D-13)."
  artifacts:
    - path: ".planning/phases/39-windows-end-to-end-validation/39-VERIFICATION.md"
      provides: "Verification doc updated with the `## UNIX-03 Regression Sweep` section recording exit codes, test counts, and host info"
      contains: "UNIX-03 Regression Sweep"
  key_links:
    - from: "39-VERIFICATION.md ## UNIX-03 Regression Sweep"
      to: "vitest reporter output (test counts)"
      via: "default vitest reporter summary lines"
      pattern: "Test Files|Tests"
---

<objective>
Run the full vitest suite on macOS (and Linux if accessible) to satisfy the UNIX-03 regression guard — proving that none of the Windows-targeted changes in Phases 35-38 (and the docs/CLAUDE.md/ROADMAP edits in Plans 39-01/02/03) regressed Unix behavior. Append the result block to `.planning/phases/39-windows-end-to-end-validation/39-VERIFICATION.md` so the maintainer's matrix evidence (Plan 04) and the UNIX-03 sweep evidence (this plan) live in the same verification artifact.

Purpose: UNIX-03 is THE explicit phase requirement for Phase 39 per REQUIREMENTS.md and per the `requirements` field of every Phase 39 plan. Phases 35-38 already added Windows-mocked describes via `setPlatform + vi.resetModules + dynamic import`; this sweep confirms the macOS path through those tests AND through every other test still goes green. Independent of Plan 04's Windows matrix — they validate orthogonal concerns (Windows production behavior vs. Unix regression preservation).

Output: `.planning/phases/39-windows-end-to-end-validation/39-VERIFICATION.md` updated (creating it if Plan 04 has not yet run, or appending the `## UNIX-03 Regression Sweep` section if it has) with macOS exit code + counts + host info; Linux block populated if accessible, otherwise explicit "not verified" note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-windows-end-to-end-validation/39-CONTEXT.md
@.planning/phases/39-windows-end-to-end-validation/39-RESEARCH.md
@.planning/phases/39-windows-end-to-end-validation/39-VALIDATION.md
@.planning/phases/38-jdt-ls-discovery-on-windows/38-VERIFICATION.md
@vitest.config.ts
@package.json

<interfaces>
<!-- Commands to run + result-block template. -->

vitest config (verified by RESEARCH.md):
- `include: ['tests/**/*.test.ts']`
- `testTimeout: 10000` (10s per test)
- ~72 `.test.ts` files (count verified in RESEARCH.md)

Commands:
- Full suite run: `pnpm test -- run` (forwards `run` to vitest CLI to force single-pass non-watch mode)
- macOS host info: `uname -srm`, `node --version`, `pnpm --version`

Phase 38 baseline (the prior verification record this sweep compares against):
- File: `.planning/phases/38-jdt-ls-discovery-on-windows/38-VERIFICATION.md`
- Locate the baseline test count in that file's "Behavioral Spot-Checks" or "Probe Execution" section. If 38-VERIFICATION.md does not record a precise pass/fail/skip count, use the count from the first `pnpm test -- run` of this plan as the new baseline AND document that fact in the result block.

Required result-block shape to append to `39-VERIFICATION.md` under a new `## UNIX-03 Regression Sweep` H2 — append AFTER the `## Failures` section from Plan 04 if present, or at the end of the document otherwise:

```
## UNIX-03 Regression Sweep

### macOS run

- Host: <output of `uname -srm`>
- Node.js: <output of `node --version`>
- pnpm: <output of `pnpm --version`>
- Command: `pnpm test -- run`
- Exit code: <0|N>
- Test files: <vitest summary "Test Files" line>
- Tests: <vitest summary "Tests" line, including pass/fail/skip counts>
- Baseline comparison: <"matches Phase 38 baseline" | "differs from Phase 38 baseline by N tests — investigated and resolved: <one-line>" | "differs — opened gap-closure plan 39-NN">

### Linux run

- Status: <"verified — see block below" | "not verified in this phase (no Linux host accessible at execution time, per RESEARCH.md Open Questions §3 fallback)">

(If verified, append same fields as macOS run.)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full vitest suite on macOS, record result in 39-VERIFICATION.md</name>
  <files>.planning/phases/39-windows-end-to-end-validation/39-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/39-windows-end-to-end-validation/39-VERIFICATION.md (determine whether the file already exists from Plan 04 — if yes, append; if no, create it with standard verifier frontmatter + this section)
    - .planning/phases/38-jdt-ls-discovery-on-windows/38-VERIFICATION.md (extract the Phase 38 baseline test count from the spot-check / probe-execution tables)
    - vitest.config.ts (confirm `include` and `testTimeout`)
    - package.json (confirm `test` script resolves to `vitest`)
    - .planning/REQUIREMENTS.md (UNIX-03 wording — "All v1.5 tests pass unchanged after the refactor (regression guard)")
  </read_first>
  <action>
Run the full vitest suite on the developer's macOS host (the M4 Max running macOS 25.5.0):

Command: `pnpm test -- run`

Capture:
- Exit code (must be 0 for UNIX-03 satisfaction).
- vitest summary lines: the `Test Files` line and the `Tests` line at the end of the reporter output (containing pass/fail/skip counts).
- Host info: `uname -srm`, `node --version`, `pnpm --version`.

Compare the test count to the Phase 38 baseline read from `38-VERIFICATION.md`:
- Match → record `matches Phase 38 baseline`.
- Differs but all-pass → record `differs from Phase 38 baseline by N tests — investigated and resolved: <one-line explanation, e.g. 'Phase 38 added M new tests via 38-01-PLAN; net delta accounts for them'>`.
- Differs with new failures or skips → record `differs — opened gap-closure plan 39-NN` AND open a gap-closure plan via `/gsd:plan-phase 39 --gaps` to investigate and fix the regression. Do NOT silently move on.

If a Linux host is accessible to the developer at execution time (developer's call — if no Linux host is reachable, skip this sub-step):
- SSH or otherwise execute `pnpm test -- run` on the Linux host. Capture the same exit code + summary + host info. Record under the Linux run block.

If no Linux host is accessible:
- Record `Status: not verified in this phase (no Linux host accessible at execution time, per RESEARCH.md Open Questions §3 fallback)` in the Linux run block.

Edit `.planning/phases/39-windows-end-to-end-validation/39-VERIFICATION.md`:
- If the file does NOT exist (Plan 04 ran later, or the maintainer ran plans out of order): create it with standard verifier frontmatter (`phase: 39-windows-end-to-end-validation`, `verified: <ISO-8601 timestamp>`, `status: pending`, `score: <truths verified>/<total truths>`, `overrides_applied: 0`), a brief `# Phase 39 — Verification Report` H1, and the `## UNIX-03 Regression Sweep` section per the `<interfaces>` template.
- If the file already exists (Plan 04 ran first): APPEND the `## UNIX-03 Regression Sweep` H2 section at the end of the document (or immediately after the `## Failures` section if present). Do NOT modify the Environment / Matrix / Failures blocks written by Plan 04.

Constraints:
- Do NOT modify any file under `tests/**` (CONTEXT.md "Files this phase MUST NOT modify").
- Do NOT modify any file under `src/**` (only triggered gap-closure plans may touch `src/**` per D-13).
- Use US English.
- If the suite is RED, do NOT silently move on. Open a gap-closure plan immediately; do not flip the maintainer's matrix-evidence checkboxes to green-by-omission.
  </action>
  <verify>
    <automated>pnpm test -- run &amp;&amp; test -f .planning/phases/39-windows-end-to-end-validation/39-VERIFICATION.md &amp;&amp; grep -qE '^## UNIX-03 Regression Sweep$' .planning/phases/39-windows-end-to-end-validation/39-VERIFICATION.md &amp;&amp; grep -qE '^### macOS run$' .planning/phases/39-windows-end-to-end-validation/39-VERIFICATION.md &amp;&amp; grep -qE '^- Exit code: 0$' .planning/phases/39-windows-end-to-end-validation/39-VERIFICATION.md &amp;&amp; awk '/^## UNIX-03 Regression Sweep$/,0' .planning/phases/39-windows-end-to-end-validation/39-VERIFICATION.md | grep -qE 'Test Files|Tests'</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- run` exits 0 on macOS (this is the primary UNIX-03 gate).
    - `.planning/phases/39-windows-end-to-end-validation/39-VERIFICATION.md` exists after this plan runs.
    - The file contains a `## UNIX-03 Regression Sweep` H2 section.
    - The file contains a `### macOS run` H3 inside that section.
    - The file contains the literal line `- Exit code: 0` (proving the macOS run was recorded as green).
    - The file contains either `Test Files` or `Tests` literal substrings inside the UNIX-03 Regression Sweep section (proving vitest summary output was captured).
    - The file contains a `### Linux run` H3 with EITHER full block fields OR the literal "not verified in this phase" phrase.
    - The file contains a `Baseline comparison:` line for the macOS run with one of the three documented values.
    - No `src/**` files modified by this plan (check via `git status` after the plan completes — only `.planning/phases/39-windows-end-to-end-validation/39-VERIFICATION.md` should appear in the working-tree diff).
    - No `tests/**` files modified by this plan (same check).
  </acceptance_criteria>
  <done>
UNIX-03 regression guard satisfied: full vitest suite passes green on macOS (and Linux if accessible); evidence captured in `39-VERIFICATION.md` `## UNIX-03 Regression Sweep` section; no Unix-side regressions introduced by Phases 35-38 or by Plans 39-01/02/03.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test suite (existing) ↔ Unix code paths | The test suite is the regression-guard surface for UNIX-01 / UNIX-02 / UNIX-03; if any Phase 35-38 Windows fix introduced Unix drift, vitest catches it here. |
| Verification doc ↔ baseline | The Phase 38 baseline is the comparison reference for "tests pass unchanged". |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-12 | Repudiation | UNIX-03 evidence in verification doc | mitigate | Acceptance criteria require literal `- Exit code: 0` AND vitest summary substrings AND a Baseline comparison line. A failed-but-claimed-green run cannot satisfy these assertions. |
| T-39-13 | Tampering | Silent regression introduced between Phase 38 verification and Phase 39 sweep | mitigate | The baseline-comparison field forces an explicit reckoning — either count matches, or the delta is explained with a one-line rationale, or a gap-closure plan opens. No "differs — unexplained" path. |
| T-39-14 | Information Disclosure | Misreporting Linux status | mitigate | Acceptance criterion requires either a populated Linux block OR the explicit literal "not verified in this phase" phrase. No silent omission. |
</threat_model>

<verification>
- macOS `pnpm test -- run` exits 0.
- `39-VERIFICATION.md` contains `## UNIX-03 Regression Sweep` with macOS exit code 0, vitest summary, baseline comparison, and Linux block (verified or explicit not-verified).
- No `src/**` or `tests/**` files modified.
</verification>

<success_criteria>
UNIX-03 is satisfied for v1.6: the full vitest suite is green on macOS (the developer's primary host), and the Linux story is either verified-green or explicitly documented as not-verified-in-this-phase with the D-15-spirit rationale. The verification doc consolidates Plan 04's Windows matrix evidence and Plan 05's Unix regression evidence in one place for the milestone-completion checkpoint.
</success_criteria>

<output>
Create `.planning/phases/39-windows-end-to-end-validation/39-05-SUMMARY.md` when done, summarizing the macOS run result, the Linux status, the baseline comparison outcome, and any gap-closure plans opened.
</output>
</content>
</invoke>