---
phase: 39-windows-end-to-end-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true
requirements:
  - UNIX-03

must_haves:
  truths:
    - "`CLAUDE.md` contains a new `### Platform Support` subsection inside the existing `## Technology Stack` block (NOT a top-level `## Platform Support` per D-16)."
    - "The subsection is inserted between the existing `### Build & Development` block (ends at the `@types/node` table row) and the existing `### Supporting Libraries` block."
    - "The subsection inlines the Java priority chain VERBATIM (all 5 slots) from `src/jdtls/java-discovery.ts:8-14` AND the JDT LS install location chains for Windows (4 paths) and Linux/macOS (3 paths) VERBATIM from `src/platform/index.ts:70-87`, with `JDTLS_HOME` documented as heading each OS chain."
    - "The subsection ends with the D-18 cross-reference footer text exactly as in `docs/WINDOWS-SUPPORT.md`: `Source of truth for the contract: see REQUIREMENTS.md WIN-01/WIN-02/JAVA-01/JAVA-02.` followed by `Implementation: \\`src/jdtls/java-discovery.ts\\` (Java) and \\`src/jdtls/client.ts\\` \\`findJdtLs\\` (JDT LS).`"
    - "All other existing subsections in `## Technology Stack` (Language & Runtime, MCP Framework, Schema Validation, Jar/ZIP File Reading, Java Language Server Integration, Gradle Project Parsing, Build & Development, Supporting Libraries) remain UNCHANGED — `grep -c` of their existing H3 headers returns the same count after the edit."
    - "The full v1.5 + v1.6 vitest suite still passes after this plan (UNIX-03 regression guard — CLAUDE.md edit must not regress code tests)."
  artifacts:
    - path: "CLAUDE.md"
      provides: "Modified — new ### Platform Support subsection inside ## Technology Stack with verbatim priority chains + D-18 footer"
      contains: "### Platform Support"
  key_links:
    - from: "CLAUDE.md ### Platform Support"
      to: "REQUIREMENTS.md WIN-01/WIN-02/JAVA-01/JAVA-02"
      via: "verbatim D-18 footer text"
      pattern: "REQUIREMENTS\\.md WIN-01/WIN-02/JAVA-01/JAVA-02"
    - from: "CLAUDE.md ### Platform Support"
      to: "src/jdtls/java-discovery.ts + src/jdtls/client.ts"
      via: "verbatim D-18 footer text"
      pattern: "src/jdtls/java-discovery\\.ts.*src/jdtls/client\\.ts"
---

<objective>
Insert a new `### Platform Support` subsection into `CLAUDE.md` inside the existing `## Technology Stack` block (D-16), sibling to `### Language & Runtime`, `### MCP Framework`, …, `### Build & Development`, `### Supporting Libraries`. The subsection inlines both priority chains (Java + JDT LS) verbatim per D-17, ends with the D-18 cross-reference footer per D-18, and matches the dual surface published by `docs/WINDOWS-SUPPORT.md` so the two locations stay consistent.

Purpose: CLAUDE.md is loaded into every Claude session. Inlining the priority chains makes the contract discoverable to every future Claude instance working on this project (cost: ~30-50 lines per session, accepted per D-16). The D-18 footer keeps the inlined content tethered to its source-of-truth implementation.

Output: One modified file — `CLAUDE.md` — with a single new subsection inserted at the documented anchor point.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-windows-end-to-end-validation/39-CONTEXT.md
@.planning/phases/39-windows-end-to-end-validation/39-RESEARCH.md
@.planning/phases/39-windows-end-to-end-validation/39-PATTERNS.md
@.planning/phases/39-windows-end-to-end-validation/39-VALIDATION.md

@CLAUDE.md
@src/jdtls/java-discovery.ts
@src/platform/index.ts
@src/jdtls/client.ts

<interfaces>
<!-- Insertion anchor in CLAUDE.md, identified by surrounding existing subsection headers. -->

Insertion point (per 39-PATTERNS.md Pattern Assignments §"CLAUDE.md ### Platform Support subsection"):
- The new `### Platform Support` subsection inserts IMMEDIATELY AFTER the existing `### Build & Development` block (whose last line is the `@types/node | 22.x | Node.js type definitions | Match Node.js 22 LTS runtime. | HIGH |` table row) and IMMEDIATELY BEFORE the existing `### Supporting Libraries` block (whose first line is `### Supporting Libraries`).
- Both surrounding subsections remain unchanged.

Source-of-truth excerpts to quote verbatim (identical to what Plan 01 quotes in `docs/WINDOWS-SUPPORT.md` so the two surfaces stay consistent):

From src/jdtls/java-discovery.ts lines 8-14 (Java priority chain, 5 slots, jsdoc gutter stripped):
  1. `--java-home` (module-state `configuredJavaHome`)
  2. `org.gradle.java.home` from `<projectRoot>/gradle.properties`
  3. `JAVA_HOME` env var
  4. `java` on PATH (libuv handles PATH lookup + PATHEXT on Windows)
  5. Scan common install locations from `commonJavaLocations()` with vendor-aware layout map

From src/platform/index.ts lines 70-87 (JDT LS chains in source order):
- Windows: `%LOCALAPPDATA%\jdtls`, `%ProgramFiles%\jdtls`, `%USERPROFILE%\jdtls`, `%LOCALAPPDATA%\nvim-data\mason\packages\jdtls`
- Linux/macOS: `~/.local/share/jdtls`, `/usr/local/share/jdtls`, `~/jdtls`
- JDTLS_HOME heads each OS chain (consumed by `src/jdtls/client.ts` `findJdtLs` at lines 117-134 BEFORE iterating candidates).

Required D-18 footer (verbatim, exactly two lines, at end of the new subsection BEFORE the next `### Supporting Libraries` heading):
```
Source of truth for the contract: see REQUIREMENTS.md WIN-01/WIN-02/JAVA-01/JAVA-02.
Implementation: `src/jdtls/java-discovery.ts` (Java) and `src/jdtls/client.ts` `findJdtLs` (JDT LS).
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert ### Platform Support subsection into CLAUDE.md ## Technology Stack</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (read lines 60-80 — confirm the `### Build & Development` block ends at the `@types/node` table row and the `### Supporting Libraries` block begins immediately after; this is the insertion anchor)
    - src/jdtls/java-discovery.ts (read lines 1-20 — verbatim Java chain text from doc-comment header)
    - src/platform/index.ts (read lines 50-100 — verbatim Windows + Unix JDT LS path lists from jdtlsCandidateDirs() body)
    - src/jdtls/client.ts (read lines 110-150 — confirm JDTLS_HOME consumption order)
    - .planning/phases/39-windows-end-to-end-validation/39-CONTEXT.md (D-16/D-17/D-18 — placement constraint, verbatim quote requirement, footer text)
    - .planning/phases/39-windows-end-to-end-validation/39-PATTERNS.md (Pattern Assignments §"CLAUDE.md ### Platform Support subsection" — recommended layout uses bold-prose intro lines `**Windows:**` / `**Linux / macOS:**` to match sibling subsection style; recommendation is NO `####` sub-subsection headers — use bold-prose intro lines instead)
    - docs/WINDOWS-SUPPORT.md (if Plan 01 already shipped — confirm the verbatim quote and footer match so the two surfaces stay consistent; OK if Plan 01 has not yet executed since both plans quote the same source)
  </read_first>
  <action>
Edit `CLAUDE.md` to insert a new `### Platform Support` H3 subsection between the existing `### Build & Development` block (which ends with the `@types/node` table row) and the existing `### Supporting Libraries` block. Do NOT modify any other content in `CLAUDE.md`.

Subsection content (in this order):

1. `### Platform Support` H3 header (3 hashes — sibling to `### Build & Development`, NOT a top-level `## Platform Support` per D-16).
2. Short prose intro paragraph (1-2 sentences): state that FabricModMCP runs on Linux, macOS, native Windows, and WSL2; the MCP server runs as Node 22+; JDT LS spawns on Java 21+. State that the two priority chains below are the authoritative discovery contract — see footer for source-of-truth pointer.
3. Bold-prose label `**Java Discovery Priority Chain**` followed by a numbered list (1.–5.) with the 5 slots VERBATIM from `src/jdtls/java-discovery.ts:8-14` (jsdoc gutter stripped). Use the same bullet text Plan 01 uses in `docs/WINDOWS-SUPPORT.md`.
4. Bold-prose label `**JDT LS Install Locations (probed in priority order)**` with a one-sentence intro stating `JDTLS_HOME` env var heads each OS list (consumed by `findJdtLs` BEFORE iterating per-OS candidates). Then bold-prose labels `**Windows:**` and `**Linux / macOS:**`, each followed by a numbered list of the paths in source order from `src/platform/index.ts:70-87`. Use bold-prose intro lines NOT `####` sub-subsection headers — sibling subsections do not use `####` (per PATTERNS.md recommendation).
5. Final two lines — VERBATIM D-18 footer text:
   ```
   Source of truth for the contract: see REQUIREMENTS.md WIN-01/WIN-02/JAVA-01/JAVA-02.
   Implementation: `src/jdtls/java-discovery.ts` (Java) and `src/jdtls/client.ts` `findJdtLs` (JDT LS).
   ```

Constraints:
- The new subsection lands INSIDE `## Technology Stack`, between `### Build & Development` and `### Supporting Libraries`. Do not move other subsections; do not create a new top-level section.
- Use US English (project convention per CLAUDE.md / global rules).
- Tabs are not relevant in markdown; markdown indentation rules apply to list items only.
- Total subsection length ~30-50 lines (per D-16 expected cost).
- The verbatim Java chain text MUST match Plan 01's `docs/WINDOWS-SUPPORT.md` Java chain text byte-for-byte (modulo the surrounding markdown structure). Same for JDT LS chains.
- Do NOT change `## Recommended Stack` header, `## Alternatives Considered`, `## Runtime Dependencies`, `## Installation`, or any other top-level section.
- Do NOT modify the surrounding subsections (`### Build & Development` table row content; `### Supporting Libraries` table). Confirm by counting H3 headers before and after.
  </action>
  <verify>
    <automated>grep -nE '^### Platform Support$' CLAUDE.md \
      &amp;&amp; awk '/^## Technology Stack$/,/^## Alternatives Considered$/' CLAUDE.md | grep -qE '^### Platform Support$' \
      &amp;&amp; grep -Fq '--java-home' CLAUDE.md \
      &amp;&amp; grep -Fq 'org.gradle.java.home' CLAUDE.md \
      &amp;&amp; grep -Fq 'commonJavaLocations' CLAUDE.md \
      &amp;&amp; grep -Fq 'JDTLS_HOME' CLAUDE.md \
      &amp;&amp; grep -Fq '%LOCALAPPDATA%\jdtls' CLAUDE.md \
      &amp;&amp; grep -Fq '%LOCALAPPDATA%\nvim-data\mason\packages\jdtls' CLAUDE.md \
      &amp;&amp; grep -Fq '~/.local/share/jdtls' CLAUDE.md \
      &amp;&amp; grep -Fq 'REQUIREMENTS.md WIN-01/WIN-02/JAVA-01/JAVA-02' CLAUDE.md \
      &amp;&amp; grep -Fq '`src/jdtls/java-discovery.ts` (Java) and `src/jdtls/client.ts` `findJdtLs` (JDT LS)' CLAUDE.md \
      &amp;&amp; [ "$(grep -cE '^### (Language &amp; Runtime|MCP Framework|Schema Validation|Jar/ZIP File Reading|Java Language Server Integration|Gradle Project Parsing|Build &amp; Development|Supporting Libraries)$' CLAUDE.md)" -eq 8 ] \
      &amp;&amp; grep -Fq '%ProgramFiles%\jdtls' CLAUDE.md \
      &amp;&amp; grep -Fq '%USERPROFILE%\jdtls' CLAUDE.md \
      &amp;&amp; grep -Fq '/usr/local/share/jdtls' CLAUDE.md \
      &amp;&amp; grep -Fq '~/jdtls' CLAUDE.md \
      &amp;&amp; pnpm test -- run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '^### Platform Support$' CLAUDE.md` returns at least one match.
    - The `### Platform Support` heading appears INSIDE the `## Technology Stack` block (i.e., between `## Technology Stack` and the next `## Alternatives Considered` H2). The awk-bounded grep gate enforces this.
    - All 8 existing sibling H3 subsection headers (`Language & Runtime`, `MCP Framework`, `Schema Validation`, `Jar/ZIP File Reading`, `Java Language Server Integration`, `Gradle Project Parsing`, `Build & Development`, `Supporting Libraries`) STILL appear in CLAUDE.md exactly once each (regression check — count = 8).
    - `grep -F` succeeds for ALL Java chain literals: `--java-home`, `org.gradle.java.home`, `commonJavaLocations`.
    - `grep -F` succeeds for ALL JDT LS chain literals: `JDTLS_HOME`, `%LOCALAPPDATA%\jdtls`, `%LOCALAPPDATA%\nvim-data\mason\packages\jdtls`, `~/.local/share/jdtls`.
    - `grep -F` succeeds for the D-18 footer literals `REQUIREMENTS.md WIN-01/WIN-02/JAVA-01/JAVA-02` AND `` `src/jdtls/java-discovery.ts` (Java) and `src/jdtls/client.ts` `findJdtLs` (JDT LS) ``.
    - `pnpm test -- run` exits 0 (UNIX-03 regression guard).
  </acceptance_criteria>
  <done>
CLAUDE.md has exactly one new `### Platform Support` subsection inside `## Technology Stack` with verbatim Java + JDT LS chains and the D-18 cross-reference footer; all eight prior sibling subsections preserved unchanged; vitest suite stays green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLAUDE.md ↔ session context | CLAUDE.md is auto-loaded into every Claude session; content drift propagates to every future Claude instance. |
| CLAUDE.md ↔ docs/WINDOWS-SUPPORT.md | Two surfaces of the same contract — must agree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-04 | Information Disclosure | CLAUDE.md ### Platform Support priority chains | mitigate | Quote VERBATIM from `src/jdtls/java-discovery.ts:8-14` and `src/platform/index.ts:70-87`; end subsection with D-18 cross-reference footer identical to docs/WINDOWS-SUPPORT.md so a reader can verify against current source. Plans 01 and 02 both quote the same source-of-truth lines so divergence is impossible if both follow this constraint. |
| T-39-05 | Tampering | Inadvertent modification of surrounding subsections | mitigate | Acceptance criterion counts the 8 existing sibling H3 headers exactly; any accidental rename or deletion fails the gate. The awk-bounded grep ensures the new subsection lands in the right H2 block. |
</threat_model>

<verification>
After completion:
- `### Platform Support` is present, inside `## Technology Stack`, with all chain literals and D-18 footer.
- All 8 existing sibling subsections preserved (grep count = 8).
- vitest suite still green.
</verification>

<success_criteria>
Any future Claude session loading this project's CLAUDE.md sees the Platform Support contract in-context without needing to grep source code. The D-18 footer prevents the inlined content from becoming stale silently.
</success_criteria>

<output>
Create `.planning/phases/39-windows-end-to-end-validation/39-02-SUMMARY.md` when done, summarizing the inserted subsection contents and confirming the surrounding subsections are unchanged.
</output>
</content>
</invoke>