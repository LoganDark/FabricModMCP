---
phase: 39-windows-end-to-end-validation
plan: 04
type: execute
wave: 2
depends_on:
  - 39-01
  - 39-02
  - 39-03
files_modified:
  - .planning/phases/39-windows-end-to-end-validation/39-VERIFICATION.md
autonomous: false
requirements:
  - UNIX-03

must_haves:
  truths:
    - "A test fixture exists at `C:\\Users\\LoganDark\\Downloads\\fabric-mod` (or another out-of-repo path documented in the verification doc) — a Fabric mod that imports at least one Minecraft class (per amended D-01: original two-mod fixture replaced by a single-mod fixture; see `<plan_deviation>` block below)."
    - "`.planning/phases/39-windows-end-to-end-validation/39-VERIFICATION.md` exists and contains an `## Environment` block listing Windows build, shell, JDK installs (with absolute paths and versions), JDT LS install path + version, Node.js version, FabricModMCP commit SHA, and the test mod's absolute path + commit SHA (or `not a git repo` note) per D-08."
    - "The verification doc contains a `## Matrix` section with exactly four checkbox rows, one per Java-discovery entry point (`--java-home`, `org.gradle.java.home`, `JAVA_HOME`, PATH-only) per D-02; each ticked row records `javaPath = <absolute path>`, `find_definition returned N=<n> results`, `find_references returned N=<n> results`, with cross-jar navigation noted for at least one row (per D-04 / D-06; original cross-mod variant replaced)."
    - "Each ticked row's `javaPath` value is captured from one of three evidence sources, with the source documented inline: (a) PowerShell `Get-CimInstance Win32_Process | Where-Object { $_.CommandLine -like '*org.eclipse.equinox.launcher*' } | Select-Object CommandLine` invoked while the row's `find_definition` is in flight; (b) Windows Task Manager / Process Hacker observation of the JDT LS child's argv0; (c) MCP server stderr if a future gap-closure plan adds a `logger.info('Spawning JDT LS', { javaPath, jdtlsHome })` line per RESEARCH.md Open Question §1."
    - "The 4 rows resolve to distinct `javaPath` values OR (single-JDK fallback) the verification doc explicitly states only one JDK is installed and documents this per D-04 fallback clause."
    - "Inter-row cleanup (per D-03 + RESEARCH.md Pitfall §2) was performed between EACH row: (a) MCP server fully exited (resets module-state `configuredJavaHome`), (b) any stray `java.exe` JDT LS processes killed (`Stop-Process` matching `org.eclipse.equinox.launcher`), (c) `gradle --stop`, (d) `<testModRoot>/.gradle/loom-cache/` deleted, (e) precondition env vars set for the next row (clear `JAVA_HOME` / unset `--java-home` flag / remove `org.gradle.java.home` from `gradle.properties` as appropriate for the next row's entry point)."
    - "Either no row failed (clean run) OR every failed row has its full stdout/stderr captured inline in the `## Failures` section of the verification doc per D-07, AND a corresponding `39-NN-PLAN.md` gap-closure plan exists per D-13 (or an explicit Phase 40 escalation note per D-14)."
    - "Cross-jar navigation is exercised in at least one row: `find_definition` from a test-mod source line referencing a Minecraft class (e.g. `net.minecraft.client.MinecraftClient`, `net.minecraft.util.Identifier`, `net.minecraft.block.Block`, or any Minecraft class the test mod actually imports) returns a location inside the merged Minecraft sources jar in the test mod's Loom cache; `find_references` from that Minecraft class declaration returns at least one result in the test mod's source files."
  artifacts:
    - path: ".planning/phases/39-windows-end-to-end-validation/39-VERIFICATION.md"
      provides: "Phase verification doc containing Environment block, 4-row Matrix block, optional Failures block, with all checkboxes ticked"
      contains: "## Matrix"
    - path: "C:/Users/LoganDark/Downloads/fabric-mod"
      provides: "User's existing Fabric mod with ≥1 Minecraft class import — single-mod fixture per amended D-01"
      contains: "build.gradle.kts"
  key_links:
    - from: "test mod source file"
      to: "Minecraft class declaration in merged Minecraft sources jar"
      via: "Java import statement"
      pattern: "import net\\.minecraft\\."
    - from: "39-VERIFICATION.md ## Matrix"
      to: "javaPath evidence per row"
      via: "PowerShell Get-CimInstance Win32_Process output (or Task Manager observation)"
      pattern: "javaPath\\s*="
---

<plan_deviation>
**Amended 2026-05-25 (chat decision):** The original two-mod fixture (`fabric-example-mod` + hand-authored sibling mod) is REPLACED with a single-mod fixture using the user's existing test mod at `C:\Users\LoganDark\Downloads\fabric-mod`. The "cross-mod" navigation requirement is replaced with "cross-jar" navigation: from the test mod's source files INTO the merged Minecraft sources jar in Loom's cache, and from a Minecraft class declaration BACK OUT to the test mod's source. This exercises the same JDT LS cross-jar resolution path that the original sibling-mod setup was meant to validate.

**Translations** — wherever the plan body, `<interfaces>` block, or `<tasks>` block references the original two-mod fixture or sibling-mod concepts, read the right-hand column instead:

| Original | Replaced with |
|----------|---------------|
| `~/dev/fmm-phase39-fixture/` (fixture root) | `C:\Users\LoganDark\Downloads\fabric-mod` (test mod root — also serves as `create_project` root) |
| `~/dev/fmm-phase39-fixture/example-mod` (cloned fabric-example-mod) | `C:\Users\LoganDark\Downloads\fabric-mod` (the single test mod) |
| `~/dev/fmm-phase39-fixture/sibling-mod` (hand-authored sibling) | (removed — no sibling mod is created) |
| `SiblingMod.java` source from `<interfaces>` block | (skipped — use whatever Minecraft class is already imported by the test mod; identify with `grep -r "^import net\.minecraft\." src/main/java/` inside the test mod) |
| Step 1 fixture setup (clone fabric-example-mod + scaffold sibling) | Verify the test mod builds on this Windows host (`./gradlew build` from `C:\Users\LoganDark\Downloads\fabric-mod`). Then identify one Minecraft class the mod imports — record the chosen class name and the source file it appears in, both in the Environment block of `39-VERIFICATION.md`. |
| Happy path step 2 (`add_fabric_mod` example-mod) | `add_fabric_mod` with `C:\Users\LoganDark\Downloads\fabric-mod` |
| Happy path step 3 (`add_fabric_mod` sibling-mod) | (skipped — only one `add_fabric_mod` call) |
| Happy path step 4 (`find_definition` from sibling → ExampleMod) | `find_definition` from a test-mod source line referencing the chosen Minecraft class. Expect a location inside `C:\Users\LoganDark\Downloads\fabric-mod\.gradle\loom-cache\minecraftMaven\...\minecraft-merged-*-sources.jar` (or the equivalent Loom global cache fallback). |
| Happy path step 5 (`find_references` from ExampleMod → sibling) | `find_references` from the chosen Minecraft class's declaration inside the merged sources jar. Expect at least one hit inside `C:\Users\LoganDark\Downloads\fabric-mod\src\main\java\...`. |
| Inter-row cleanup references to `sibling-mod/.gradle/loom-cache/` | (only one mod's loom-cache to delete: `C:\Users\LoganDark\Downloads\fabric-mod\.gradle\loom-cache\`) |
| Row 2 `org.gradle.java.home` setup (modify two gradle.properties files) | Modify only `C:\Users\LoganDark\Downloads\fabric-mod\gradle.properties` |
| `cross-mod` annotation on matrix row bullets | `cross-jar` (semantically equivalent — same JDT LS cross-jar navigation path) |
| Step 4 cross-mod proof (sibling ↔ example-mod) | Cross-jar proof: at least one row demonstrates BOTH directions — `find_definition` from test mod source INTO the Minecraft merged sources jar, AND `find_references` from a Minecraft class declaration OUT INTO the test mod's source files |

The frontmatter `must_haves.truths`, `artifacts`, and `key_links` have been rewritten in line with the above; they (not the unmodified body) are the verifier's authoritative contract. The `<objective>`, `<interfaces>`, `<tasks>`, `<threat_model>`, and `<verification>` body sections below are preserved verbatim for context — apply the translations table when executing them.
</plan_deviation>

<objective>
Execute the Windows end-to-end validation matrix: build the out-of-repo test fixture, run the 4-row Java-discovery matrix on a real Windows machine per D-02 / D-03, and capture all evidence in `39-VERIFICATION.md` per D-06 / D-07 / D-08. This plan is NOT agent-executable — the executor agent does NOT run the matrix; the maintainer (a human, on a real Windows machine) does. The plan's single task is a `checkpoint:human-verify` gate.

Purpose: This is the load-bearing validation step for milestone v1.6. Phases 35-38 shipped Windows fixes behind unit tests with mocked `process.platform = 'win32'`. This plan validates the fixes work in production on a real Windows host under all four Java-discovery entry points. The matrix detects regressions that mocks cannot catch (long-path overflows, AV interference, real PATH semantics, drive-letter casing in real `Location.uri` strings).

Output: `.planning/phases/39-windows-end-to-end-validation/39-VERIFICATION.md` with all four matrix checkboxes ticked, the environment block populated, and any failure rows recorded with full stdout/stderr.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-windows-end-to-end-validation/39-CONTEXT.md
@.planning/phases/39-windows-end-to-end-validation/39-RESEARCH.md
@.planning/phases/39-windows-end-to-end-validation/39-PATTERNS.md
@.planning/phases/39-windows-end-to-end-validation/39-VALIDATION.md

@docs/WINDOWS-SUPPORT.md
@CLAUDE.md
@src/jdtls/java-discovery.ts
@src/jdtls/client.ts
@src/platform/index.ts
@src/jdtls/startup.ts

<interfaces>
<!-- Matrix recipe + sibling-mod source the maintainer needs. -->

Sibling-mod source (per RESEARCH.md Code Examples §1 — VERIFIED Fabric convention since 1.19):

File path: `~/dev/fmm-phase39-fixture/sibling-mod/src/main/java/com/example/sibling/SiblingMod.java`

```java
package com.example.sibling;

import net.fabricmc.api.ModInitializer;
import net.fabricmc.example.ExampleMod;   // cross-mod import — JDT LS resolves this

public class SiblingMod implements ModInitializer {
	@Override
	public void onInitialize() {
		System.out.println("SiblingMod loaded; ExampleMod LOGGER name: "
			+ ExampleMod.LOGGER.getName());
	}
}
```

Fallback if `ExampleMod.LOGGER` is renamed/private in the cloned master (RESEARCH.md Tertiary source confidence): substitute any public symbol from ExampleMod, e.g. `new ExampleMod()` or `ExampleMod.class.getName()`.

Inter-row cleanup recipe (per D-03 + RESEARCH.md Pitfall §2):
1. Fully exit MCP server (Ctrl+C in Claude Code's MCP-server pane). This resets module-state `configuredJavaHome` set by `--java-home`.
2. `Stop-Process -Name java -Force -ErrorAction SilentlyContinue` (or `Get-CimInstance Win32_Process | Where-Object { $_.CommandLine -like '*org.eclipse.equinox.launcher*' } | ForEach-Object { Stop-Process -Id $_.ProcessId -Force }` for surgical removal).
3. `gradle --stop` (from anywhere — kills all Gradle daemons across the user account).
4. `Remove-Item -Recurse -Force ~/dev/fmm-phase39-fixture/example-mod/.gradle/loom-cache/` AND `Remove-Item -Recurse -Force ~/dev/fmm-phase39-fixture/sibling-mod/.gradle/loom-cache/` (if either exists).
5. Pre-condition env vars for the next row's entry point:
   - Row 1 (`--java-home`): unset `JAVA_HOME` (`$env:JAVA_HOME = $null` or `Remove-Item Env:JAVA_HOME`), remove `org.gradle.java.home` from `gradle.properties`; start MCP server with `--java-home <absolute path to JDK A's home>`.
   - Row 2 (`org.gradle.java.home`): unset `JAVA_HOME`, do NOT pass `--java-home`; add `org.gradle.java.home=<absolute path to JDK B's home>` to `~/dev/fmm-phase39-fixture/example-mod/gradle.properties` (and sibling-mod's `gradle.properties`); start MCP server with no JDK-related flags.
   - Row 3 (`JAVA_HOME`): set `$env:JAVA_HOME = '<absolute path to JDK A's home>'`, do NOT pass `--java-home`; remove `org.gradle.java.home` from `gradle.properties`; start MCP server.
   - Row 4 (PATH only): unset `JAVA_HOME`, do NOT pass `--java-home`, remove `org.gradle.java.home`; ensure exactly one Java is reachable on `PATH` (`where.exe java` returns one result); start MCP server.

Happy path (executed identically per row — per D-02):
1. `create_project` on `~/dev/fmm-phase39-fixture/`
2. `add_fabric_mod` with `~/dev/fmm-phase39-fixture/example-mod`
3. `add_fabric_mod` with `~/dev/fmm-phase39-fixture/sibling-mod`
4. `find_definition` from a sibling-mod source line that references `ExampleMod` — expect cross-mod result inside example-mod (or in the merged Loom source jar if Loom's mapping treats it that way).
5. `find_references` from the `ExampleMod` class declaration in example-mod — expect at least one result inside sibling-mod.

Evidence-capture method per row (default = no `src/**` modification — Open Question §1 deferred to gap-closure per planner decision below):

PowerShell command to capture the JDT LS child's `javaPath` while the row's `find_definition` is in flight (or any time the JDT LS process is alive):

```powershell
Get-CimInstance Win32_Process | Where-Object { $_.CommandLine -like '*org.eclipse.equinox.launcher*' } | Select-Object -ExpandProperty CommandLine
```

The first token of the returned `CommandLine` is the resolved `javaPath`. Record it verbatim in the matrix row. If the command returns no rows, JDT LS exited before the snapshot — start the MCP server, issue the `find_definition`, and immediately re-run the PowerShell snapshot in a separate terminal (JDT LS stays alive for the session). If repeatedly empty across rows, escalate to a gap-closure plan that adds the `logger.info('Spawning JDT LS', { javaPath, jdtlsHome })` line in `src/jdtls/client.ts` per RESEARCH.md Open Question §1 / Common Pitfalls §1 / 39-PATTERNS.md §"src/jdtls/client.ts — optional one-line logger.info addition".

Planner decision (this plan owns it): The `src/**` `logger.info` addition is DEFERRED to a gap-closure plan triggered only if the PowerShell `Get-CimInstance` recipe fails to produce `javaPath` evidence per row. This keeps Phase 39 a pure docs+validation phase (per CONTEXT.md "Files this phase MUST NOT modify: anything under src/**" — gap-closure carve-out per D-13). If the PowerShell recipe works, no `src/**` change is needed. If it fails, the maintainer opens `39-NN-PLAN.md` adding the one-line `logger.info` per the carved-out gap-closure rule; the new gap-closure plan owns its own legitimacy gates and tests.

Required `39-VERIFICATION.md` block templates (per RESEARCH.md Architecture Patterns §"Pattern 3" — combined with the verifier's standard layout from `38-VERIFICATION.md`):

```markdown
## Environment

- Windows: <e.g. 11 24H2 Build 26100>
- Shell: <e.g. PowerShell 7.4.6>
- JDK installs:
  - <e.g. Adoptium Temurin 21.0.5 at C:\Program Files\Eclipse Adoptium\jdk-21.0.5.11-hotspot\>
  - <if available — second distinct JDK, otherwise note "only one JDK installed">
- JDT LS: <version> at <path resolved by findJdtLs>
- Node.js: <version>
- FabricModMCP: commit <SHA>
- fabric-example-mod: commit <SHA at clone time>

## Matrix

- [ ] **Row 1 — `--java-home`**: javaPath=`<absolute path>`, find_definition N=<n>, find_references N=<n> (cross-mod sibling-mod → example-mod: <yes/no>); evidence-source=<Get-CimInstance | Task Manager | logger.info>
- [ ] **Row 2 — `org.gradle.java.home`**: javaPath=`<absolute path>`, find_definition N=<n>, find_references N=<n>; evidence-source=<…>
- [ ] **Row 3 — `JAVA_HOME`**: javaPath=`<absolute path>`, find_definition N=<n>, find_references N=<n>; evidence-source=<…>
- [ ] **Row 4 — PATH only**: javaPath=`<absolute path>`, find_definition N=<n>, find_references N=<n>; evidence-source=<…>

## Failures

(none — or per-row stdout/stderr blocks pasted inline per D-07)
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Execute Windows 4-row Java-discovery matrix and populate 39-VERIFICATION.md</name>
  <what-built>
Plans 39-01 (docs/WINDOWS-SUPPORT.md), 39-02 (CLAUDE.md ### Platform Support), and 39-03 (ROADMAP Phase 39 plan-list cleanup) are complete. The publishable Windows surface is documented. This task is the live validation that the documented contract actually holds on a real Windows host.
  </what-built>
  <read_first>
    - .planning/phases/39-windows-end-to-end-validation/39-CONTEXT.md (D-01 fixture spec, D-02 matrix spec, D-03 inter-row cleanup recipe, D-04 javaPath evidence requirement, D-06/D-07/D-08 evidence format, D-13/D-14/D-15 failure-handling protocol)
    - .planning/phases/39-windows-end-to-end-validation/39-RESEARCH.md (Common Pitfalls §1 javaPath observability, §2 slot leakage, §3 per-project workspace re-sync subtlety, §4 long-path limit, Code Examples §1 sibling-mod source)
    - .planning/phases/39-windows-end-to-end-validation/39-PATTERNS.md (Pattern Assignments §"39-VERIFICATION.md" — Environment + Matrix + Failures block templates)
    - .planning/phases/39-windows-end-to-end-validation/39-VALIDATION.md (Manual-Only Verifications table — per-row instructions)
    - docs/WINDOWS-SUPPORT.md (written by Plan 39-01 — confirms which JDT LS install paths are documented; matrix preconditions assume JDT LS is at one of those paths OR JDTLS_HOME is set)
    - CLAUDE.md ### Platform Support (written by Plan 39-02 — same contract for session-context discoverability)
  </read_first>
  <how-to-verify>
Prerequisites (assert ALL before starting Row 1):
  1. Real Windows machine accessible (not WSL — native Windows; WSL2 routes around the Windows-native code paths this matrix is meant to validate).
  2. Node.js 22+ installed; `node --version` confirms.
  3. pnpm installed; `pnpm --version` confirms.
  4. Git for Windows installed; `git --version` confirms.
  5. JDT LS milestone installed at one of `%LOCALAPPDATA%\jdtls`, `%ProgramFiles%\jdtls`, `%USERPROFILE%\jdtls`, or `%LOCALAPPDATA%\nvim-data\mason\packages\jdtls` — OR `JDTLS_HOME` set to a directory containing `plugins/org.eclipse.equinox.launcher_*.jar`.
  6. At least one JDK 21+ installed; ideally TWO distinct JDK 21+ installs to make D-04's distinct-`javaPath` proof meaningful. If only one JDK, document explicitly in the Environment block per D-04 fallback.
  7. FabricModMCP repo cloned to the Windows machine; `pnpm install` completed.

Step 1 — Fixture setup (one-time):
  a. `mkdir ~/dev/fmm-phase39-fixture/` (or any out-of-repo path; record the chosen path in the Environment block).
  b. `cd ~/dev/fmm-phase39-fixture && git clone https://github.com/FabricMC/fabric-example-mod.git example-mod` — record the cloned commit SHA in the Environment block.
  c. `cd example-mod && ./gradlew build` to verify upstream builds clean on this host BEFORE involving FabricModMCP. If the Gradle build fails, fix the upstream-build issue first (likely a Java version mismatch or missing toolchain) — this is a fixture problem, not a FabricModMCP problem.
  d. Create sibling-mod scaffold: copy `example-mod/gradle.properties` and `example-mod/build.gradle.kts` into `~/dev/fmm-phase39-fixture/sibling-mod/`; change `mod_id` to `sibling_mod` (and any other identity fields like `archives_base_name`); create `~/dev/fmm-phase39-fixture/sibling-mod/src/main/java/com/example/sibling/SiblingMod.java` with the source provided in this plan's `<interfaces>` block.
  e. Per A6 in RESEARCH.md, the sibling does NOT need a Gradle dependency on example-mod for the matrix to work — FabricModMCP's per-project JDT LS workspace concatenates classpaths across children. If `./gradlew build` on sibling-mod fails because `net.fabricmc.example.ExampleMod` is unresolved at compile time, EITHER add `dependencies { implementation(project(":example-mod")) }` (heavier) OR skip compiling sibling-mod and let JDT LS resolve the cross-mod reference at MCP-server runtime via the per-project workspace.

Step 2 — Execute matrix (Rows 1-4):

For each row, perform the FULL inter-row cleanup recipe (D-03 + RESEARCH.md Pitfall §2 expansion):
  - Fully exit MCP server (Ctrl+C in MCP-server pane — this resets module-state `configuredJavaHome`).
  - `Stop-Process -Name java -Force -ErrorAction SilentlyContinue` (or use the `Get-CimInstance` filter to target only JDT LS launchers if other Java processes are running).
  - `gradle --stop`.
  - Delete `~/dev/fmm-phase39-fixture/example-mod/.gradle/loom-cache/` and `~/dev/fmm-phase39-fixture/sibling-mod/.gradle/loom-cache/` if present.
  - Set/unset env vars and `gradle.properties` entries per the row's entry point (see `<interfaces>` block).

Then for each row, run the happy path:
  1. Start MCP server with the row's discovery precondition.
  2. `create_project` on `~/dev/fmm-phase39-fixture/` (or the root containing both example-mod and sibling-mod).
  3. `add_fabric_mod` example-mod.
  4. `add_fabric_mod` sibling-mod.
  5. In a SEPARATE PowerShell terminal (so the MCP-server stdio session isn't disturbed), capture the JDT LS spawn line: `Get-CimInstance Win32_Process | Where-Object { $_.CommandLine -like '*org.eclipse.equinox.launcher*' } | Select-Object -ExpandProperty CommandLine`. The first token in the returned string is the resolved `javaPath`. Record verbatim.
  6. `find_definition` on a sibling-mod source line referencing `ExampleMod` (e.g. on the `ExampleMod.LOGGER` reference in SiblingMod.java) — record the result count N. Assert N ≥ 1.
  7. `find_references` on the `ExampleMod` class declaration in `example-mod/src/main/java/net/fabricmc/example/ExampleMod.java` — record the result count N. Assert at least one result is located inside sibling-mod (proves cross-mod navigation works).
  8. Tick the row's checkbox in `39-VERIFICATION.md`; fill in `javaPath`, `find_definition N=`, `find_references N=`, and `cross-mod` annotation per the matrix template in `<interfaces>`.

Per-row failure handling (D-07):
  - If a row fails (server start fails, `add_fabric_mod` throws, `find_definition` returns empty, `find_references` returns empty, or `javaPath` cannot be captured): paste the full stdout/stderr inline in the `## Failures` section of `39-VERIFICATION.md` under a per-row heading.
  - Then decide per D-13/D-14/D-15:
    - **Small fix (gap-closure inside Phase 39):** open `39-NN-PLAN.md` (next sequential plan number after 05) using `/gsd:plan-phase 39 --gaps`. Implement the fix. Re-run the failed row.
    - **Big finding (needs its own discuss-phase):** escalate to Phase 40 via `/gsd:phase add 40 …` then `/gsd:discuss-phase 40`. v1.6 ships only when both phases close.
    - **Genuinely environmental (AV interaction, OneDrive lock):** add to `docs/WINDOWS-SUPPORT.md` "Known limitations" with workaround per D-15 exception.
  - "Document-as-known-issue defer to v1.7" is NOT acceptable for FabricModMCP-code-fixable bugs per D-15.

Step 3 — Slot-independence sanity check (per D-04):
  Inspect the four `javaPath` values. They MUST resolve to distinct paths OR (single-JDK fallback) the Environment block explicitly states only one JDK is installed. If two rows that should hit different slots return the same `javaPath`, suspect slot leakage (RESEARCH.md Pitfall §2): re-do inter-row cleanup including MCP server restart, retry the affected row.

Step 4 — Cross-mod proof (per CONTEXT.md success criterion 2):
  At least one row's `find_definition` from sibling-mod to example-mod MUST return a non-empty result; at least one row's `find_references` from example-mod MUST include a sibling-mod hit. If only `find_definition` works but `find_references` returns empty, suspect RESEARCH.md Pitfall §3 (per-project workspace re-sync subtlety): verify after each `add_fabric_mod` that JDT LS reports `available: true`; if degradation recovery happened, re-run the row.

Completion criteria for this task:
  - All 4 matrix checkboxes in `39-VERIFICATION.md` are ticked.
  - The Environment block is populated with concrete values (no `<…>` placeholders).
  - Either no failure rows OR every failure has full stdout/stderr in `## Failures` AND a corresponding gap-closure / Phase 40 escalation reference.
  - At least one row demonstrates cross-mod navigation in BOTH directions (find_definition AND find_references).
  - The `javaPath` values across the 4 rows are distinct OR the single-JDK fallback is documented.
  </how-to-verify>
  <resume-signal>
Type `approved` once `39-VERIFICATION.md` matrix is complete and all sub-criteria above hold. Or describe specific issues encountered: e.g. `failure: row 2 cannot resolve org.gradle.java.home — opened 39-06-PLAN.md gap closure`, or `escalation: long-path crash needs Phase 40 architectural discuss`, or `javaPath capture impossible via Get-CimInstance — opened gap closure to add logger.info per RESEARCH.md Open Question §1`.
  </resume-signal>
  <acceptance_criteria>
    - File `.planning/phases/39-windows-end-to-end-validation/39-VERIFICATION.md` exists.
    - The file contains an `## Environment` H2 with non-placeholder values for Windows build, shell, ≥1 JDK install path with version, JDT LS install path with version, Node.js version, FabricModMCP commit SHA, fabric-example-mod commit SHA.
    - The file contains a `## Matrix` H2 with exactly 4 bullet lines, one per matrix row, each starting with `- [x]` (ticked) and matching the pattern `^- \[x\] \*\*Row [1-4] — \`[^`]+\`\*\*: javaPath=` and containing the literal substrings `find_definition N=` and `find_references N=`.
    - The four recorded `javaPath` values are distinct (or the Environment block contains the literal phrase "only one JDK installed" documenting the fallback per D-04).
    - At least one matrix row's bullet line contains the literal phrase `cross-jar` (proving cross-jar navigation to the Minecraft merged sources jar was exercised — substitution for the original `cross-mod` per the `<plan_deviation>` block).
    - If any failure was encountered: `## Failures` H2 exists in the file with at least one stdout/stderr block; AND either a gap-closure plan file `39-06-PLAN.md` (or higher) exists in the phase directory OR the verification doc explicitly notes Phase 40 escalation.
    - The human reviewer (maintainer) has approved the gate by typing the resume signal.
  </acceptance_criteria>
  <done>
Windows matrix executed end-to-end; evidence captured per D-06/D-07/D-08; all checkboxes ticked; any failures triaged per D-13/D-14/D-15; cross-mod navigation proven in both directions for at least one row.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Windows host ↔ FabricModMCP server | Real-environment failures (long paths, AV, drive-letter casing) cross into FabricModMCP's process boundary. |
| Test fixture (out-of-repo) ↔ FabricModMCP | Loom cache + Gradle daemon state crosses between matrix rows; D-03 cleanup is the trust boundary. |
| Maintainer's evidence record ↔ verification doc | The matrix's evidentiary value depends on honest, slot-independent capture per D-04. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-08 | Repudiation | `javaPath` evidence per matrix row | mitigate | PowerShell `Get-CimInstance` captures argv[0] of the JDT LS child process. Each row records the evidence source (Get-CimInstance vs Task Manager vs logger.info). Distinct-`javaPath` sanity check across the 4 rows catches slot-leakage regressions (Pitfall §2). |
| T-39-09 | Tampering | Slot leakage between matrix rows (module-state `configuredJavaHome` persists if MCP server not restarted) | mitigate | Inter-row cleanup recipe in `<how-to-verify>` mandates fully exiting the MCP server between rows. Acceptance criterion requires distinct `javaPath` values (or single-JDK fallback documented). |
| T-39-10 | Denial of Service | Long-path 260-char limit during workspace extraction | accept | Documented in `docs/WINDOWS-SUPPORT.md` Known Limitations with `LongPathsEnabled` registry mitigation per D-11.3. If the matrix hits this, the maintainer enables long-path support and re-runs — or escalates to Phase 40 if a code-level fix is needed (D-14). |
| T-39-11 | Information Disclosure | False-positive matrix run (cached workspace from prior JDK pinning serves requests under new JDK's spawn line) | mitigate | D-03 cleanup deletes `<projectRoot>/.gradle/loom-cache/` between rows. RESEARCH.md Pitfall §2 + §3 surfaced as triage checklist items. |
| T-39-SC | Tampering | Package installs in this plan | accept | This plan installs zero packages. The pnpm install step happens during Step 1 prerequisites (already-pinned `package.json`); no `pnpm add` is invoked. Package Legitimacy Gate is N/A for Phase 39 per RESEARCH.md `## Package Legitimacy Audit`. If a gap-closure plan needs a new dep, that plan runs its own legitimacy gate. |
</threat_model>

<verification>
- 39-VERIFICATION.md exists with Environment + Matrix + (optional) Failures blocks.
- All 4 matrix checkboxes ticked.
- `javaPath` values distinct across rows (or single-JDK fallback documented).
- Cross-mod navigation proven in at least one row.
- Any failures triaged per D-13/D-14/D-15 with corresponding gap-closure / Phase 40 / docs-known-limitation artifact.
</verification>

<success_criteria>
The v1.6 milestone's core claim — "FabricModMCP works on Windows" — is empirically validated on a real Windows host under all four Java-discovery entry points, with evidence sufficient to detect future regressions of the slot-independence and cross-mod navigation contracts.
</success_criteria>

<output>
Create `.planning/phases/39-windows-end-to-end-validation/39-04-SUMMARY.md` when done, summarizing the matrix outcomes, any gap-closure plans opened, and the final `39-VERIFICATION.md` checkbox state.
</output>
</content>
</invoke>