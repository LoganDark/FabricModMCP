---
phase: 13-study-jar-management-tools
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - tests/tools/add-study-jar.test.ts
  - tests/tools/remove-study-jar.test.ts
  - tests/tools/list-study-jars.test.ts
  - tests/tools/configure-study-jar.test.ts
autonomous: true
requirements: [STUDY-01, STUDY-02, STUDY-03, STUDY-04]

must_haves:
  truths:
    - "add_study_jar succeeds with valid jar path and returns stats"
    - "add_study_jar fails with clear error on invalid path, non-ZIP, or duplicate name"
    - "remove_study_jar removes jars and clears cached data"
    - "remove_study_jar fails on first nonexistent name with no partial removal"
    - "list_study_jars returns all jars with names, paths, auto-include status"
    - "list_study_jars returns empty array when no study jars exist"
    - "configure_study_jar toggles auto-include flag"
    - "configure_study_jar fails on first nonexistent name with no partial update"
  artifacts:
    - path: "tests/tools/add-study-jar.test.ts"
      provides: "Integration tests for add_study_jar tool"
      min_lines: 40
    - path: "tests/tools/remove-study-jar.test.ts"
      provides: "Integration tests for remove_study_jar tool"
      min_lines: 40
    - path: "tests/tools/list-study-jars.test.ts"
      provides: "Integration tests for list_study_jars tool"
      min_lines: 30
    - path: "tests/tools/configure-study-jar.test.ts"
      provides: "Integration tests for configure_study_jar tool"
      min_lines: 30
  key_links:
    - from: "tests/tools/add-study-jar.test.ts"
      to: "src/tools/add-study-jar.ts"
      via: "callTool with name 'add_study_jar'"
      pattern: "name: 'add_study_jar'"
    - from: "tests/tools/remove-study-jar.test.ts"
      to: "src/tools/remove-study-jar.ts"
      via: "callTool with name 'remove_study_jar'"
      pattern: "name: 'remove_study_jar'"
---

<objective>
Create integration tests for all four study jar management tools, verifying both success paths and error handling.

Purpose: Ensure the tools work correctly end-to-end via MCP protocol, including error cases (invalid paths, duplicate names, nonexistent jars, batch fail-fast behavior).

Output: Four test files covering all requirement behaviors from the RESEARCH.md test map.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-study-jar-management-tools/13-CONTEXT.md
@.planning/phases/13-study-jar-management-tools/13-RESEARCH.md
@.planning/phases/13-study-jar-management-tools/13-01-SUMMARY.md

<interfaces>
<!-- Test infrastructure the executor needs -->

From tests/helpers/client.ts:
```typescript
export interface TestPair {
	client: Client;
	server: McpServer;
	cleanup: () => Promise<void>;
}
export async function createTestPair(): Promise<TestPair>;
```

From tests/helpers/factories.ts:
```typescript
export function parseEnvelope(result: ...): any;
export function makeFakeProject(overrides: Partial<LoadedProject> = {}): LoadedProject;
```

From src/state/project-store.ts:
```typescript
// Singleton project store
projectStore.set(name: string, project: LoadedProject): void;
projectStore.clear(): void;
```

From tests/project/study-jar.test.ts (test ZIP creation pattern):
```typescript
// Creates a valid JAR/ZIP with com/example/Foo.java and com/example/Bar.java
async function createTestZip(outputPath: string): Promise<void> {
	const contentDir = join(testDir, 'content-' + Math.random().toString(36).slice(2));
	await mkdir(join(contentDir, 'com', 'example'), { recursive: true });
	await writeFile(join(contentDir, 'com', 'example', 'Foo.java'), 'package com.example;\n\npublic class Foo {\n}\n');
	await writeFile(join(contentDir, 'com', 'example', 'Bar.java'), 'package com.example;\n\npublic class Bar {\n}\n');
	execSync(`cd "${contentDir}" && zip -r "${outputPath}" .`);
}
```

From src/project/types.ts:
```typescript
export interface StudyJar {
	name: string;
	jarPath: string;
	autoInclude: boolean;
	stats: StudyJarStats;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tests for add_study_jar and list_study_jars</name>
  <files>tests/tools/add-study-jar.test.ts, tests/tools/list-study-jars.test.ts</files>
  <read_first>
    - tests/helpers/client.ts (createTestPair pattern)
    - tests/helpers/factories.ts (parseEnvelope, makeFakeProject)
    - tests/project/study-jar.test.ts (createTestZip pattern and test fixture setup)
    - tests/tools/echo.test.ts (simple tool test pattern)
    - src/tools/add-study-jar.ts (actual implementation to test against)
    - src/tools/list-study-jars.ts (actual implementation to test against)
    - src/state/project-store.ts (projectStore.set, projectStore.clear)
  </read_first>
  <action>
Create two test files. Both need a shared pattern: `createTestPair()` for MCP client, `makeFakeProject()` registered via `projectStore.set()`, and a temporary JAR file created with `createTestZip()` (using `execSync('zip ...')` pattern from `tests/project/study-jar.test.ts`).

**File: `tests/tools/add-study-jar.test.ts`**

Test cases:
1. **adds a study jar with explicit name** — call `add_study_jar` with `{ path: testJarPath, name: 'my-lib' }`, assert envelope `success === true`, `data.name === 'my-lib'`, `data.stats.classCount === 2`, `data.stats.packageCount === 1`, `data.autoInclude === false`
2. **adds a study jar with auto-derived name** — call with `{ path: testJarPath }` (no name), assert `data.name` is derived from filename (e.g., `'test-lib-1.0'` for `test-lib-1.0-sources.jar`)
3. **returns error for nonexistent path** — call with `{ path: '/nonexistent/foo.jar' }`, assert envelope `success === false`
4. **returns error for non-ZIP file** — create a plain text file, call with its path, assert `success === false`
5. **returns error for duplicate name** — add a jar, then try to add another with the same name, assert `success === false`
6. **jar appears in list_study_jars after add** — add a jar, then call `list_study_jars`, assert the jar appears in `data.jars` array

Setup: `beforeAll` creates temp dir + test ZIP files. `beforeEach` creates test pair + registers fake project. `afterEach` clears project store + cleans up test pair. `afterAll` removes temp dir.

**File: `tests/tools/list-study-jars.test.ts`**

Test cases:
1. **returns empty array when no study jars** — call `list_study_jars`, assert `data.jars` is empty array, `data.count === 0`
2. **returns all study jars with details** — add 2 study jars via `add_study_jar`, then call `list_study_jars`, assert `data.count === 2`, each jar has `name`, `path`, `autoInclude`, `stats` properties
3. **text content is human-readable** — assert result `content[0].text` is a string containing jar count, not JSON

Same setup pattern as add test file.

Tab indentation throughout. Use `import { describe, it, expect, beforeAll, afterAll, beforeEach, afterEach } from 'vitest';`.
  </action>
  <verify>
    <automated>npx vitest run tests/tools/add-study-jar.test.ts tests/tools/list-study-jars.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `tests/tools/add-study-jar.test.ts` exists with at least 5 test cases
    - `tests/tools/add-study-jar.test.ts` contains `name: 'add_study_jar'` (callTool invocation)
    - `tests/tools/add-study-jar.test.ts` tests `success === true` for valid add
    - `tests/tools/add-study-jar.test.ts` tests `success === false` for invalid path
    - `tests/tools/add-study-jar.test.ts` tests `success === false` for duplicate name
    - `tests/tools/list-study-jars.test.ts` exists with at least 2 test cases
    - `tests/tools/list-study-jars.test.ts` contains `name: 'list_study_jars'`
    - `tests/tools/list-study-jars.test.ts` tests empty list case
    - All tests pass: `npx vitest run tests/tools/add-study-jar.test.ts tests/tools/list-study-jars.test.ts` exits 0
  </acceptance_criteria>
  <done>add_study_jar and list_study_jars tools have integration tests covering success, error, and edge cases</done>
</task>

<task type="auto">
  <name>Task 2: Tests for remove_study_jar and configure_study_jar</name>
  <files>tests/tools/remove-study-jar.test.ts, tests/tools/configure-study-jar.test.ts</files>
  <read_first>
    - tests/tools/add-study-jar.test.ts (test fixture pattern from Task 1)
    - src/tools/remove-study-jar.ts (actual implementation)
    - src/tools/configure-study-jar.ts (actual implementation)
    - tests/helpers/client.ts
    - tests/helpers/factories.ts
  </read_first>
  <action>
Create two test files. Both tests need to first add study jars (via `add_study_jar` tool call) before testing remove/configure operations.

**File: `tests/tools/remove-study-jar.test.ts`**

Test cases:
1. **removes a study jar by name** — add jar, remove it by name, assert `success === true`, `data.removed` includes the name, then call `list_study_jars` and assert jar is gone
2. **removes multiple jars in batch** — add 2 jars, remove both in one call, assert `data.removed` has both names, `data.remaining === 0`
3. **returns error for nonexistent name** — call `remove_study_jar` with `{ names: ['nonexistent'] }`, assert `success === false`, error code is `'STUDY_JAR_NOT_FOUND'`
4. **fails on first nonexistent in batch (no partial removal)** — add jar 'a', call remove with `{ names: ['a', 'nonexistent'] }`, assert `success === false`. Then call `list_study_jars` and assert jar 'a' still exists (was NOT removed)

**File: `tests/tools/configure-study-jar.test.ts`**

Test cases:
1. **toggles auto-include on** — add jar (default autoInclude=false), call `configure_study_jar` with `{ names: ['jar-name'], autoInclude: true }`, assert `success === true`, then list and assert `autoInclude === true`
2. **toggles auto-include off** — add jar, configure to true, then configure to false, assert result shows `autoInclude === false`
3. **configures multiple jars in batch** — add 2 jars, configure both to autoInclude=true, assert `data.updated` has both entries with `autoInclude === true`
4. **returns error for nonexistent name** — call with `{ names: ['nonexistent'], autoInclude: true }`, assert `success === false`
5. **fails on first nonexistent in batch (no partial update)** — add jar 'a', call configure with `{ names: ['a', 'nonexistent'], autoInclude: true }`, assert error. Then list and assert jar 'a' still has `autoInclude === false` (was NOT updated)

Same setup/teardown pattern. Tab indentation.
  </action>
  <verify>
    <automated>npx vitest run tests/tools/remove-study-jar.test.ts tests/tools/configure-study-jar.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `tests/tools/remove-study-jar.test.ts` exists with at least 4 test cases
    - `tests/tools/remove-study-jar.test.ts` contains `name: 'remove_study_jar'`
    - `tests/tools/remove-study-jar.test.ts` tests batch fail-fast (jar 'a' still exists after partial batch failure)
    - `tests/tools/configure-study-jar.test.ts` exists with at least 4 test cases
    - `tests/tools/configure-study-jar.test.ts` contains `name: 'configure_study_jar'`
    - `tests/tools/configure-study-jar.test.ts` tests `autoInclude` toggle
    - `tests/tools/configure-study-jar.test.ts` tests batch fail-fast (no partial update)
    - All tests pass: `npx vitest run tests/tools/remove-study-jar.test.ts tests/tools/configure-study-jar.test.ts` exits 0
  </acceptance_criteria>
  <done>remove_study_jar and configure_study_jar tools have integration tests covering batch operations, fail-fast behavior, and error cases</done>
</task>

</tasks>

<verification>
- All 4 test files exist and pass: `npx vitest run tests/tools/add-study-jar.test.ts tests/tools/remove-study-jar.test.ts tests/tools/list-study-jars.test.ts tests/tools/configure-study-jar.test.ts --reporter=verbose`
- Full test suite still green: `npx vitest run`
- At least 15 test cases total across the 4 files
</verification>

<success_criteria>
- Four test files exist in `tests/tools/` covering all behaviors from the RESEARCH.md test map
- Every requirement (STUDY-01 through STUDY-04) has at least 2 test cases (success + error)
- Batch fail-fast behavior explicitly tested for remove and configure tools
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/13-study-jar-management-tools/13-02-SUMMARY.md`
</output>
