---
phase: 36-path-uri-handling-audit
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - src/jdtls/client.ts
  - src/jdtls/workspace-sync.ts
  - src/tools/remove-project-member.ts
  - src/tools/tool-helpers.ts
autonomous: true
requirements: [WIN-03, WIN-04]
requirements_addressed: [WIN-03, WIN-04]

must_haves:
  truths:
    - "Every `'file://' + path` construction site in `client.ts`, `workspace-sync.ts`, and `remove-project-member.ts` is replaced by `pathToFileUri(absPath)` (three-slash form on the wire — WIN-03)."
    - "The reverse consumer in `tool-helpers.ts:350` no longer string-replaces `'file://'` — it calls `fileUriToPath(loc.uri)` which percent-decodes and handles the three-slash form, and the call is inside the existing try/catch so a `TypeError` from a malformed URI is treated like a `readFile` failure (`continue` to the next location, per RESEARCH Open Question 4 (RESOLVED) recommendation to widen the try block)."
    - "All 4 `workspace-sync.ts` forward sites (lines 103, 141, 206, 255) wrap the file path with `join(resolvedTempDir, '.classpath')` before passing to `pathToFileUri` — eliminates mixed-separator strings on Windows (Pitfall 1 / WIN-04 LSP-URI side)."
    - "WIN-04 is co-owned with Plan 04 — Plan 02 handles the LSP-URI Pitfall 1 cases (`join` + `pathToFileUri` at the 4 `workspace-sync.ts` LSP-forward sites), Plan 04 handles the ZIP-extract Pitfall 1 cases (`entryPath.split('/')` + spread into `join`) at the 2 Z1/Z2 sites."
    - "Unix v1.5 tests pass unchanged (UNIX-02 round-trip identity holds because `pathToFileUri` Unix output decodes back to the same path; the on-the-wire byte change two-slash→three-slash is the expected D-25 difference)."
    - "`gradle-parser.ts:36` and the 2 `remove-project-member.ts` `rm` calls at lines 96/104 are NOT touched (out of scope per CONTEXT.md and PATTERNS Note 3)."
  artifacts:
    - path: "src/jdtls/client.ts"
      provides: "LSP initialize handshake using `pathToFileUri(workspaceDir)` for `rootUri` and `workspaceFolders[0].uri`."
      contains: "pathToFileUri(workspaceDir)"
    - path: "src/jdtls/workspace-sync.ts"
      provides: "4 `didChangeWatchedFiles` notifications using `pathToFileUri(join(resolvedTempDir, '.classpath'))`."
      contains: "pathToFileUri(join(resolvedTempDir, '.classpath'))"
    - path: "src/tools/remove-project-member.ts"
      provides: "1 `didChangeWatchedFiles` notification using `pathToFileUri(join(resolvedTempDir, '.classpath'))`."
      contains: "pathToFileUri(join(resolvedTempDir, '.classpath'))"
    - path: "src/tools/tool-helpers.ts"
      provides: "Reverse consumer calls `fileUriToPath(loc.uri)` INSIDE the existing try/catch at line 353 — malformed URIs `continue` to the next location instead of throwing out of the loop."
      contains: "fileUriToPath(loc.uri)"
  key_links:
    - from: "src/jdtls/client.ts"
      to: "src/platform/uri.ts"
      via: "`import { pathToFileUri } from '../platform/uri.js';`"
      pattern: "from '\\.\\./platform/uri\\.js'"
    - from: "src/jdtls/workspace-sync.ts"
      to: "src/platform/uri.ts"
      via: "`import { pathToFileUri } from '../platform/uri.js';`"
      pattern: "from '\\.\\./platform/uri\\.js'"
    - from: "src/tools/remove-project-member.ts"
      to: "src/platform/uri.ts"
      via: "`import { pathToFileUri } from '../platform/uri.js';`"
      pattern: "from '\\.\\./platform/uri\\.js'"
    - from: "src/tools/tool-helpers.ts"
      to: "src/platform/uri.ts"
      via: "`import { fileUriToPath } from '../platform/uri.js';`"
      pattern: "from '\\.\\./platform/uri\\.js'"
---

<objective>
Sweep the 7 forward `'file://' + path` sites and the 1 reverse `uri.replace('file://', '')` consumer to the new `src/platform/uri.ts` helpers. Mechanical refactor — no behavioral change except for (a) the on-the-wire shape change (two-slash → three-slash) that WIN-03 requires and D-25 explicitly documents, and (b) the reverse consumer now `continue`s past malformed URIs instead of crashing (RESEARCH Open Question 4 (RESOLVED) — defensive widening of the existing try block).

Purpose: Eliminate brittle string concatenation at every LSP boundary. After this plan, the only `'file://'` literals left in `src/` are inside `src/jdtls/uri-mapper.ts` (Plan 03's scope) and `src/project/gradle-parser.ts:36` (out of scope — divergent semantics).

Output: 4 modified files; ~12 single-token swaps; 4 new imports; one try-block widening in `tool-helpers.ts`. No new tests required in this plan — coverage is provided by `tests/platform/uri.test.ts` (Plan 01) for the helper round-trip and the existing v1.5 test suite for the LSP-boundary integration (UNIX-03 regression).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/36-path-uri-handling-audit/36-CONTEXT.md
@.planning/phases/36-path-uri-handling-audit/36-RESEARCH.md
@.planning/phases/36-path-uri-handling-audit/36-PATTERNS.md
@.planning/phases/36-path-uri-handling-audit/36-VALIDATION.md
@CLAUDE.md
@src/platform/uri.ts
@src/jdtls/client.ts
@src/jdtls/workspace-sync.ts
@src/tools/remove-project-member.ts
@src/tools/tool-helpers.ts

<interfaces>
From src/platform/uri.ts (created by Plan 01):
- `export function pathToFileUri(absPath: string): string;` — wraps `pathToFileURL(p).href`. Three-slash output.
- `export function fileUriToPath(uri: string): string;` — wraps `fileURLToPath(u)`. Throws `TypeError` on malformed input.

ESM import convention (CLAUDE.md): use `.js` suffix on the import path even though the source is `.ts`.

`node:path` `join` is already imported in `workspace-sync.ts:11` and `remove-project-member.ts:13`. No `node:path` import changes in this plan.

Current shape at `src/tools/tool-helpers.ts` lines 346-358 (HEAD):
```
for (const loc of locations) {
    const mapping = uriMapper.fromFileUri(loc.uri);
    if (!mapping) continue;

    const filePath = loc.uri.replace('file://', '');  // ← line 350 — moves inside try
    let source = sourceCache.get(filePath);
    if (source === undefined) {
        try {
            source = await readFile(filePath, 'utf-8');  // ← existing try at line 353
            sourceCache.set(filePath, source);
        } catch {
            continue;
        }
    }
```
The `fileUriToPath` call must end up inside a try that catches `TypeError` and `continue`s — either by moving the `filePath` assignment into the existing try (and reading `filePath` once before the cache lookup) or by wrapping just the conversion in its own try/catch. See Task 2 action for the chosen shape.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Forward sweep — `src/jdtls/client.ts` (F1 + F2) and `src/tools/remove-project-member.ts` (F7)</name>
  <files>src/jdtls/client.ts, src/tools/remove-project-member.ts</files>
  <read_first>
    - .planning/phases/36-path-uri-handling-audit/36-PATTERNS.md §"`src/jdtls/client.ts` lines 245, 278" AND §"`src/tools/remove-project-member.ts` line 83"
    - .planning/phases/36-path-uri-handling-audit/36-RESEARCH.md §"Site List Verification → Forward `'file://' + …` sites" (lines 182-194)
    - .planning/phases/36-path-uri-handling-audit/36-CONTEXT.md D-03 (sweep scope), D-04/D-05/D-07 (Unix-shaped tool API, Windows paths at disk layer only)
    - src/jdtls/client.ts (current state — confirm lines 245 + 278 still match the pattern at HEAD before editing)
    - src/tools/remove-project-member.ts (current state — confirm line 83 still matches; do NOT touch lines 96 / 104 `rm` calls per PATTERNS Note 3)
  </read_first>
  <behavior>
    - After the edit, `src/jdtls/client.ts` line 245 reads `rootUri: pathToFileUri(workspaceDir),` and line 278 reads `workspaceFolders: [{ uri: pathToFileUri(workspaceDir), name: 'sources' }],`. The rest of the `initialize` object literal is byte-identical to HEAD.
    - After the edit, `src/tools/remove-project-member.ts` line 83 reads `uri: pathToFileUri(join(resolvedTempDir, '.classpath')),` (using the already-imported `join` from `node:path`).
    - Imports `{ pathToFileUri }` from `../platform/uri.js` are added at the top of each file alongside existing imports (preserving alphabetical-ish grouping if present, otherwise next to other project imports).
    - Lines 96 and 104 of `remove-project-member.ts` (the two `rm` calls outside D-17 scope) are NOT touched — they keep their current `{ recursive: true, force: true }` options.
    - Existing v1.5 tests pass: JDT LS still successfully receives `initialize` and `didChangeWatchedFiles` notifications. On Unix the wire shape changes from `file:///abs/path` to `file:///abs/path` (already three-slash on Unix because `'file://' + '/abs'` = `'file:///abs'`) — observationally identical. On Windows, two-slash → three-slash is the WIN-03 fix.
  </behavior>
  <action>
    Edit `src/jdtls/client.ts`: add `import { pathToFileUri } from '../platform/uri.js';` at the top of the file with the existing project imports. Replace the two `'file://' + workspaceDir` occurrences at lines 245 and 278 with `pathToFileUri(workspaceDir)`. Preserve all surrounding object-literal structure (per PATTERNS §"`src/jdtls/client.ts`" — two single-token replacements + one new import; nothing else changes).
    
    Edit `src/tools/remove-project-member.ts`: add `import { pathToFileUri } from '../platform/uri.js';` at the top with existing project imports. Replace `uri: 'file://' + resolvedTempDir + '/.classpath',` at line 83 with `uri: pathToFileUri(join(resolvedTempDir, '.classpath')),` (per PATTERNS §"`src/tools/remove-project-member.ts` line 83" — `join` is already imported at line 13). Do NOT touch the `rm` calls at lines 96 and 104 — they are explicitly outside CONTEXT.md D-17 scope per PATTERNS Note 3 and OUT OF SCOPE comment in the pattern map.
  </action>
  <verify>
    <automated>pnpm test -- tests/jdtls/client.test.ts tests/tools/remove-project-member.test.ts 2>&1 | tail -40</automated>
    <source-assertion>
      grep -c "'file://'" src/jdtls/client.ts equals 0
      grep -c "'file://'" src/tools/remove-project-member.ts equals 0
      grep -c "pathToFileUri" src/jdtls/client.ts is at least 3 (1 import + 2 uses)
      grep -c "pathToFileUri" src/tools/remove-project-member.ts is at least 2 (1 import + 1 use)
      grep -c "from '../platform/uri.js'" src/jdtls/client.ts equals 1
      grep -c "from '../platform/uri.js'" src/tools/remove-project-member.ts equals 1
      grep -nE "rm\\(.+\\{[^}]*\\bmaxRetries\\b" src/tools/remove-project-member.ts returns nothing (lines 96/104 untouched — no retry options added)
    </source-assertion>
  </verify>
  <done>
    `src/jdtls/client.ts` and `src/tools/remove-project-member.ts` no longer contain any `'file://'` literal. Both import `pathToFileUri` from `../platform/uri.js`. Lines 96/104 of `remove-project-member.ts` are byte-identical to HEAD. Targeted vitest suites pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Forward sweep — `src/jdtls/workspace-sync.ts` (F3 + F4 + F5 + F6) and reverse sweep — `src/tools/tool-helpers.ts` (R1) with widened try block</name>
  <files>src/jdtls/workspace-sync.ts, src/tools/tool-helpers.ts</files>
  <read_first>
    - .planning/phases/36-path-uri-handling-audit/36-PATTERNS.md §"`src/jdtls/workspace-sync.ts` lines 103, 141, 206, 255" AND §"`src/tools/tool-helpers.ts` line 350"
    - .planning/phases/36-path-uri-handling-audit/36-RESEARCH.md §"Common Pitfalls → Pitfall 1: `path.join(depDir, 'a/b/c')` does NOT split forward slashes on Windows" (lines 756-762)
    - .planning/phases/36-path-uri-handling-audit/36-RESEARCH.md §"Open Landmines → 2. `fileURLToPath` throws on non-`file:` scheme" (lines 1004-1008) — `TypeError` on malformed URIs is the failure mode to defend against
    - .planning/phases/36-path-uri-handling-audit/36-RESEARCH.md §"Open Questions (RESOLVED) → 4. Does `fileUriToPath` need a try/catch wrapper at `tool-helpers.ts:350`?" (lines 1093-1097) — **Recommendation: widen the try block** to include line 350. Cheap, defensive, no behavior change in the happy path. Skip the malformed URI via `continue` (consistent with the existing `if (!mapping) continue;` pattern at line 348).
    - .planning/phases/36-path-uri-handling-audit/36-CONTEXT.md D-03 (sweep scope), D-25 (two-slash → three-slash wire change is expected)
    - src/jdtls/workspace-sync.ts (confirm lines 103, 141, 206, 255 still match the byte-identical pattern at HEAD; note `join` already imported at line 11)
    - src/tools/tool-helpers.ts (confirm lines 350 + 353 still match at HEAD)
  </read_first>
  <behavior>
    - All 4 sites in `workspace-sync.ts` are replaced with the IDENTICAL transformation: `'file://' + resolvedTempDir + '/.classpath'` → `pathToFileUri(join(resolvedTempDir, '.classpath'))`. Why `join` first instead of string-concat: per Pitfall 1, `'C:\\tmp' + '/.classpath'` is a mixed-separator string `pathToFileURL` treats as one segment; `join('C:\\tmp', '.classpath')` produces a platform-native path that `pathToFileURL` correctly converts.
    - After the edit, `tool-helpers.ts` no longer contains `loc.uri.replace('file://', '')`. The `const filePath = fileUriToPath(loc.uri);` call lives INSIDE the existing try block (the one currently wrapping `readFile` at line 353), so a `TypeError` from a malformed URI is caught by the existing `catch { continue; }` and the loop advances to the next location — mirroring the `if (!mapping) continue;` short-circuit at line 348. This is the RESEARCH Open Question 4 (RESOLVED) recommendation: widen, do not introduce new error infrastructure.
    - The existing `uriMapper.fromFileUri(loc.uri)` call at `tool-helpers.ts:347` (just above the changed line) is UNTOUCHED — that is Plan 03's scope.
    - On Unix the wire shape is byte-identical to v1.5 for these sites (`'file://' + '/abs'` already produces `'file:///abs'`); on Windows the wire shape becomes the WIN-03 three-slash form.
    - The `sourceCache` lookup must still happen by `filePath` key, so the conversion must produce `filePath` before the cache `get` — meaning the widened try must wrap from the conversion through the `readFile` (or the conversion lives in its own `try { ... } catch { continue; }` immediately preceding the cache lookup).
  </behavior>
  <action>
    Edit `src/jdtls/workspace-sync.ts`: add `import { pathToFileUri } from '../platform/uri.js';` at the top alongside existing project imports. At each of the 4 sites (lines 103, 141, 206, 255), replace the literal `'file://' + resolvedTempDir + '/.classpath'` with `pathToFileUri(join(resolvedTempDir, '.classpath'))`. All 4 sites are byte-identical except for surrounding function context — the transformation is the same. `join` is already imported at line 11; no `node:path` import change in this task.
    
    Edit `src/tools/tool-helpers.ts`:
    
    1. Add `import { fileUriToPath } from '../platform/uri.js';` at the top with existing project imports.
    
    2. Replace the current shape at lines 350-358:
    
       ```
       const filePath = loc.uri.replace('file://', '');
       let source = sourceCache.get(filePath);
       if (source === undefined) {
           try {
               source = await readFile(filePath, 'utf-8');
               sourceCache.set(filePath, source);
           } catch {
               continue;
           }
       }
       ```
       
       with a shape that places the `fileUriToPath` conversion inside a try/catch whose catch is `continue`. The canonical replacement (mirroring RESEARCH Open Question 4 (RESOLVED) "widen the try block"):
       
       ```
       let filePath: string;
       try {
           filePath = fileUriToPath(loc.uri);
       } catch {
           continue;
       }
       let source = sourceCache.get(filePath);
       if (source === undefined) {
           try {
               source = await readFile(filePath, 'utf-8');
               sourceCache.set(filePath, source);
           } catch {
               continue;
           }
       }
       ```
       
       The conversion has its own short try/catch (mirrors the `if (!mapping) continue;` short-circuit pattern at line 348). This is functionally equivalent to merging the conversion into the existing try (which would also work — the executor MAY merge instead if that produces less code churn; the source-assertion below tolerates either shape as long as the conversion is enclosed in a try whose catch path is `continue`).
    
    3. Do NOT touch the `uriMapper.fromFileUri(loc.uri)` call at line 347 — that is Plan 03's scope.
  </action>
  <verify>
    <automated>pnpm test -- tests/jdtls/workspace-sync.test.ts tests/tools/tool-helpers.test.ts 2>&1 | tail -40</automated>
    <source-assertion>
      grep -c "'file://'" src/jdtls/workspace-sync.ts equals 0
      grep -c "pathToFileUri" src/jdtls/workspace-sync.ts is at least 5 (1 import + 4 uses)
      grep -c "from '../platform/uri.js'" src/jdtls/workspace-sync.ts equals 1
      grep -c "pathToFileUri(join(resolvedTempDir, '.classpath'))" src/jdtls/workspace-sync.ts equals 4
      grep -c "loc.uri.replace('file://', '')" src/tools/tool-helpers.ts equals 0
      grep -c "fileUriToPath(loc.uri)" src/tools/tool-helpers.ts equals 1
      grep -c "from '../platform/uri.js'" src/tools/tool-helpers.ts equals 1
      Shell: `awk '/fileUriToPath\(loc\.uri\)/{found=1} found && /catch[^A-Za-z]/{print "ENCLOSED"; exit}' src/tools/tool-helpers.ts` prints "ENCLOSED" (the `fileUriToPath` call is followed downstream by a `catch` block within the same loop iteration — confirms it lives inside try/catch scope).
    </source-assertion>
  </verify>
  <done>
    `src/jdtls/workspace-sync.ts` contains no `'file://'` literals and 4 occurrences of `pathToFileUri(join(resolvedTempDir, '.classpath'))`. `src/tools/tool-helpers.ts` no longer contains `loc.uri.replace('file://', '')`, now calls `fileUriToPath(loc.uri)` inside a try whose catch path is `continue` (either a new tight try/catch immediately above the cache lookup OR the existing try widened to include the conversion). Targeted vitest suites pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Full-suite regression check (UNIX-03 guard)</name>
  <files>(verification-only; no source edits)</files>
  <read_first>
    - .planning/phases/36-path-uri-handling-audit/36-VALIDATION.md §"Sampling Rate" (full suite command + estimated runtime)
    - .planning/REQUIREMENTS.md UNIX-03 (regression guard — all v1.5 tests must pass after the refactor)
    - .planning/phases/36-path-uri-handling-audit/36-CONTEXT.md D-25 (two-slash → three-slash on-the-wire change is expected; UNIX-02 is round-trip identity, not URI-string byte identity with v1.5)
  </read_first>
  <behavior>
    - After Tasks 1 + 2 of this plan land, the full vitest suite passes with zero new failures or skips.
    - No test asserts URI-string byte equality between this plan's output and v1.5 (such an assertion would be a false-positive UNIX-02 reading — D-25 documents the on-the-wire shape change is expected). If any existing test fails because it asserted the literal `'file://' + path` shape, surface in SUMMARY.md as a test-expectation update needed in this plan (not a regression).
    - The single grep gate `grep -rn "'file://'" src/` (excluding `gradle-parser.ts:36` and `uri-mapper.ts` which is Plan 03's scope) returns no matches — Phase 36's forward-sweep / reverse-sweep concern is closed.
  </behavior>
  <action>
    Run `pnpm test` and confirm it exits 0. Run `grep -rn "'file://'" src/ | grep -vE 'gradle-parser\\.ts|uri-mapper\\.ts'` and confirm the only remaining match is none (Plan 03 still owns `uri-mapper.ts` — that file may still have `'file://'` until Plan 03 lands; this check excludes it). If any v1.5 test fails because it pinned the literal two-slash wire shape, update its expectation to use `pathToFileUri(p)` (or equivalent regex) and note the change in SUMMARY.md per D-25. Do not modify production code in this task.
  </action>
  <verify>
    <automated>pnpm test 2>&1 | tail -10</automated>
    <source-assertion>
      Shell: `grep -rn "'file://'" src/ | grep -vE 'gradle-parser\\.ts|uri-mapper\\.ts'` returns no matches
      `pnpm test` exit code is 0
    </source-assertion>
  </verify>
  <done>
    Full vitest suite exits 0. The only remaining `'file://'` literals in `src/` are in `src/project/gradle-parser.ts:36` (out of scope) and `src/jdtls/uri-mapper.ts` (Plan 03's scope). UNIX-03 regression guard satisfied for this plan.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP server → JDT LS (LSP send) | The 7 forward sites construct URIs the MCP server sends to JDT LS. The URI is a path-shaped artifact derived from a workspace directory or temp directory the server itself created — no externally-supplied data crosses this boundary in this plan. |
| JDT LS → MCP server (LSP receive) | The reverse site (`tool-helpers.ts:350`) consumes `Location.uri` from JDT LS. JDT LS is treated as low-trust-but-not-adversarial (we run it; it runs against jars we extracted). Malformed URIs are now caught and `continue`d past — defensive posture per RESEARCH Open Question 4 (RESOLVED). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-02-01 | Tampering / DoS | `tool-helpers.ts:350` `fileUriToPath(loc.uri)` | mitigate | `fileUriToPath` is enclosed in a try/catch whose catch path is `continue` — a malformed URI from JDT LS does NOT abort the entire navigation result loop; the offending location is skipped and the remaining locations process normally. This preserves availability of the tool response even if one Location.uri is malformed. Per RESEARCH Open Question 4 (RESOLVED) "widen the try block" recommendation. |
| T-36-02-02 | Tampering | All 7 forward sites | accept | The MCP server constructs the URI from paths it controls (workspace directory; resolved temp directory from `realpathSync`). No path-traversal possibility at this boundary. The traversal threat surface is the ZIP-entry extraction in Plan 04. |

No new external input is handled by this plan. The package legitimacy audit is not triggered (no `pnpm install` in this plan).
</threat_model>

<verification>
- `pnpm test` exits 0 (full suite — UNIX-03 regression guard).
- `pnpm exec tsc --noEmit` exits 0.
- `grep -rn "'file://'" src/ | grep -vE 'gradle-parser\\.ts|uri-mapper\\.ts'` returns no matches.
- `grep -c "pathToFileUri\\|fileUriToPath" src/` returns at least 9 (1 import + 2 uses in client.ts; 1 import + 4 uses in workspace-sync.ts; 1 import + 1 use in remove-project-member.ts; 1 import + 1 use in tool-helpers.ts; 1 import + 2 exports in src/platform/uri.ts from Plan 01 = 14+).
- `src/project/gradle-parser.ts:36` is byte-identical to HEAD (verify with `git diff src/project/gradle-parser.ts` returning nothing).
- Lines 96 and 104 of `src/tools/remove-project-member.ts` are byte-identical to HEAD (no retry options added — that is outside CONTEXT.md D-17 scope).
- `fileUriToPath(loc.uri)` in `src/tools/tool-helpers.ts` is inside a try/catch with `continue` in the catch path (RESEARCH Open Question 4 (RESOLVED) compliance).
</verification>

<success_criteria>
1. 7 forward `'file://' + path` constructions migrated to `pathToFileUri(...)`.
2. 1 reverse `uri.replace('file://', '')` consumer migrated to `fileUriToPath(...)` AND enclosed in a try/catch whose catch path is `continue`.
3. All 4 `workspace-sync.ts` forward sites use `join(resolvedTempDir, '.classpath')` before passing to `pathToFileUri` (WIN-04 LSP-URI Pitfall 1 fix; ZIP-extract WIN-04 cases live in Plan 04).
4. `gradle-parser.ts` and `remove-project-member.ts:96,104` untouched.
5. `pnpm test` exits 0.
</success_criteria>

<output>
Create `.planning/phases/36-path-uri-handling-audit/36-02-SUMMARY.md` when done. SUMMARY MUST note: (a) the 12 in-place edits across 4 files (11 token swaps + 1 try-block widening); (b) explicit confirmation that `gradle-parser.ts:36` and `remove-project-member.ts:96,104` were NOT touched per scope locks; (c) the chosen shape for the widened try block in `tool-helpers.ts` (new tight try/catch vs merging into existing try); (d) any v1.5 test expectations updated due to the D-25 two-slash → three-slash wire-shape change (if applicable); (e) deferred-follow-up — `remove-project-member.ts:96,104` `rm` retry options not added (locked out of D-17 scope, candidate for a quick follow-up).
</output>
</content>
