---
phase: 37-smarter-java-discovery-cross-platform
plan: 05
type: execute
wave: 5
gap_closure: true
depends_on:
  - 37-04
files_modified:
  - src/jdtls/startup.ts
  - tests/jdtls/startup.test.ts
autonomous: true
requirements:
  - JAVA-01

must_haves:
  truths:
    - "After `retryDegradedJdtLsSessions` rescues a project's JDT LS session (newSession.available === true), every fabric-mod child under that project is re-synced into the new workspace via `syncFabricModToWorkspace(child, newSession, jarReader)` before the function returns to its caller."
    - "After `add_fabric_mod` / `refresh_project` / `refresh_project_members` complete, if any rescue occurred, the rescued session's `.classpath` is repopulated and a `find_definition` against a class belonging to a synced fabric-mod child returns real navigation results — observable via the new unit test that mocks `syncFabricModToWorkspace` and asserts it was called once per fabric-mod child of the rescued project (semantic equivalent is human UAT)."
    - "Error swallowing contract from Plan 02 (D-04) is preserved: any throw from `syncFabricModToWorkspace` during the post-rescue re-sync is logged via `logger.warn` and does NOT propagate past `retryDegradedJdtLsSessions` to the tool handler."
    - "Re-sync is gated on `newSession.available === true` — degraded reinit (newSession still failed) does NOT call `syncFabricModToWorkspace` (the existing Plan 02 behavior — sync is a no-op on degraded sessions — already covered this implicitly, but the new code MUST short-circuit explicitly so the sync loop only runs on rescue success)."
    - "Re-sync iterates `project.children.values()` and filters `child.kind === 'fabric-mod'` — study-jar children are skipped (matches the per-iteration projectRoot derivation pattern at startup.ts:131-136)."
    - "The `jarReader` argument passed to `syncFabricModToWorkspace` is the module singleton imported from `../tools/shared-jar-reader.js` — the SAME instance used by `add_fabric_mod` (line 7), `refresh_project`, and `refresh_project_members`, so the rescued workspace sees the same registered jars as the original tool-handler sync attempt would have."
    - "All 850 prior tests remain green (`pnpm test`); `pnpm tsc --noEmit` exits 0."
    - "`tests/jdtls/client.test.ts` is NOT modified (Plan 02 shim contract preserved; mandated by the gap-closure quality gate)."
  artifacts:
    - path: "src/jdtls/startup.ts"
      provides: "retryDegradedJdtLsSessions extended with post-rescue syncFabricModToWorkspace loop"
      contains: "syncFabricModToWorkspace"
    - path: "tests/jdtls/startup.test.ts"
      provides: "New it() under `describe('retryDegradedJdtLsSessions')` asserting `syncFabricModToWorkspace` is invoked once per fabric-mod child after a successful rescue, NOT invoked for study-jar children, NOT invoked when reinit stays degraded, and that a thrown sync error is swallowed + logged via logger.warn."
      contains: "syncFabricModToWorkspace"
  key_links:
    - from: "src/jdtls/startup.ts (retryDegradedJdtLsSessions)"
      to: "src/jdtls/workspace-sync.ts (syncFabricModToWorkspace)"
      via: "named import"
      pattern: "import \\{ syncFabricModToWorkspace \\} from '\\./workspace-sync\\.js'"
    - from: "src/jdtls/startup.ts (retryDegradedJdtLsSessions)"
      to: "src/tools/shared-jar-reader.ts (jarReader module singleton)"
      via: "named import"
      pattern: "import \\{ jarReader \\} from '\\.\\./tools/shared-jar-reader\\.js'"
    - from: "src/jdtls/startup.ts (retryDegradedJdtLsSessions)"
      to: "syncFabricModToWorkspace call site"
      via: "inline post-rescue loop gated on newSession.available === true"
      pattern: "syncFabricModToWorkspace\\(child, newSession, jarReader\\)"
---

<objective>
Close gap CR-01 from `37-REVIEW.md` / `37-VERIFICATION.md`: extend `retryDegradedJdtLsSessions` in `src/jdtls/startup.ts` so that after a degraded JDT LS session is successfully rescued, every `kind === 'fabric-mod'` child of that project is re-synced into the freshly-created workspace via `syncFabricModToWorkspace`. Without this, the rescued session reports `available: true` but has an empty `.classpath` and `find_definition` returns nothing — defeating Phase 37's user-visible promise ("newly-installed Java unlocks previously-degraded JDT LS sessions").

Purpose: Deliver JAVA-01's user-observable workflow. The discovery chain, async probe, and reinit hook are already in place from plans 37-01..37-04; the only remaining wire is the post-rescue workspace re-sync.

Output: Two files modified. `src/jdtls/startup.ts` gains two new imports and a `for (const child of project.children.values())` block after `project.jdtls = newSession;` (between lines 154 and 156 of the current file). `tests/jdtls/startup.test.ts` gains new `it()` cases under the existing `describe('retryDegradedJdtLsSessions')` block (lines 377-468) asserting the re-sync behavior. No other source files touched. `tests/jdtls/client.test.ts` is explicitly NOT modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/37-smarter-java-discovery-cross-platform/37-CONTEXT.md
@.planning/phases/37-smarter-java-discovery-cross-platform/37-REVIEW.md
@.planning/phases/37-smarter-java-discovery-cross-platform/37-VERIFICATION.md
@.planning/phases/37-smarter-java-discovery-cross-platform/37-02-SUMMARY.md
@.planning/phases/37-smarter-java-discovery-cross-platform/37-03-SUMMARY.md
@src/jdtls/startup.ts
@src/jdtls/workspace-sync.ts
@src/tools/add-fabric-mod.ts
@src/tools/shared-jar-reader.ts
@tests/jdtls/startup.test.ts

<interfaces>
<!-- Key contracts the executor needs. Extracted from src/. -->

From src/jdtls/workspace-sync.ts:165:
```typescript
export async function syncFabricModToWorkspace(
    fabricMod: FabricModChild,
    jdtls: JdtLsSession | undefined,
    jarReader: JarReader,
): Promise<{ synced: boolean; warning?: string }>;
```
Notes:
- Returns `{ synced: false, warning: 'Note: JDT LS unavailable -- semantic navigation disabled' }` when `jdtls?.available !== true` or `jdtls.endpoint` is missing (no-op on degraded).
- Returns `{ synced: true }` on success; may return `{ synced: true, warning: '...' }` for partial issues.
- Can throw (ZIP traversal rejection, fs errors) — caller must guard with try/catch.

From src/tools/shared-jar-reader.ts:
```typescript
export const jarReader = new JarReader();
```
The module singleton imported by add_fabric_mod / refresh_project / refresh_project_members.

From src/project/types.ts (already referenced via existing startup.ts imports):
- `Project.children: Map<string, FabricModChild | StudyJarChild>`
- `FabricModChild.kind === 'fabric-mod'`
- `StudyJarChild.kind === 'study-jar'`

From src/jdtls/startup.ts:123-164 (current state — point of edit):
```typescript
export async function retryDegradedJdtLsSessions(): Promise<void> {
    for (const project of projectStore.list()) {
        if (project.jdtls?.available !== false) continue;
        // ... per-iteration projectRoot derivation, cleanup, reinit ...
        try {
            const newSession = await initJdtLsSession({ projectRoot });
            project.jdtls = newSession;
            if (newSession.available === true) {
                logger.info(`JDT LS reinit succeeded for project '${project.name}'`);
            }
        } catch (err) {
            logger.warn(`JDT LS reinit failed for project '${project.name}'`, { error: String(err) });
        }
    }
}
```
The fix inserts the re-sync loop INSIDE the existing `if (newSession.available === true)` block, after the existing `logger.info` line (or before — order is not load-bearing, but doing it after the log preserves the existing "reinit succeeded" first-line semantics in test output).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire post-rescue syncFabricModToWorkspace into retryDegradedJdtLsSessions</name>
  <files>src/jdtls/startup.ts</files>
  <read_first>
    - src/jdtls/startup.ts (current file — the function to extend, lines 123-164)
    - src/jdtls/workspace-sync.ts (signature + return-shape of syncFabricModToWorkspace, lines 165-220)
    - src/tools/shared-jar-reader.ts (verify the `jarReader` named export is the module singleton)
    - src/tools/add-fabric-mod.ts (canonical caller pattern: import path, call shape, warning-log shape — lines 7, 73-76)
    - src/project/types.ts (FabricModChild / StudyJarChild discriminator union — confirm `kind` field shapes; do not modify)
    - .planning/phases/37-smarter-java-discovery-cross-platform/37-REVIEW.md (CR-01 fix sketch, lines 58-83)
    - .planning/phases/37-smarter-java-discovery-cross-platform/37-CONTEXT.md (D-04 unconditional-retry contract; the post-rescue sync must also swallow errors)
  </read_first>
  <action>
Add two named imports at the top of `src/jdtls/startup.ts` alongside the existing module imports:
- `import { syncFabricModToWorkspace } from './workspace-sync.js';`
- `import { jarReader } from '../tools/shared-jar-reader.js';`

Place them with the other `./` and `../` imports — order does not matter, but keep the `./` siblings (`./client.js`, `./workspace.js`, `./workspace-sync.js`) grouped, and the `../` external imports (`../logging/logger.js`, `../state/project-store.js`, `../tools/shared-jar-reader.js`) grouped, matching the existing block layout.

Then, inside `retryDegradedJdtLsSessions`, locate the existing block:

```
project.jdtls = newSession;
if (newSession.available === true) {
    logger.info(`JDT LS reinit succeeded for project '${project.name}'`);
}
```

Extend that `if` block to iterate `project.children.values()`, filter `child.kind === 'fabric-mod'`, and `await syncFabricModToWorkspace(child, newSession, jarReader)` for each. Wrap each per-child call in its own try/catch that logs via `logger.warn` (preserving the D-04 swallow-and-log contract): on a thrown error, log with shape `logger.warn('Workspace re-sync failed after JDT LS rescue', { project: project.name, child: child.name, error: String(err) })` and continue to the next child. If the returned `result.warning` is truthy, log via `logger.warn(\`Workspace re-sync after JDT LS rescue for '${child.name}': ${result.warning}\`)`. The `logger.info('JDT LS reinit succeeded ...')` line remains — emit it after the re-sync loop completes so the success message reflects the FULLY rescued state (workspace populated), matching the user-visible promise.

Do NOT introduce a top-level `for (const child of ...)` outside the `if (newSession.available === true)` guard — degraded reinits must not invoke sync (per the must_have gating truth and per workspace-sync.ts:170 short-circuit semantics).

Do NOT touch the existing `tempDir`/`dataDir` cleanup, the `let projectRoot` derivation, or the outer `try/catch (err)` that handles `initJdtLsSession` throws — those remain byte-identical. The new per-child try/catch is INSIDE the outer try block, so an unexpected throw in the sync loop is still caught by the outer logger.warn at line 161 if it ever escapes the inner catch (defense in depth, but the inner catch should always handle).

Tab indentation throughout (project convention; existing file already uses tabs). No fenced code blocks or implementation snippets — see CR-01 fix sketch in 37-REVIEW.md for the literal shape.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm test tests/jdtls/startup.test.ts tests/jdtls/client.test.ts tests/jdtls/java-discovery.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "syncFabricModToWorkspace" src/jdtls/startup.ts` returns at least 2 (1 import + 1 call site).
    - `grep -c "from '../tools/shared-jar-reader.js'" src/jdtls/startup.ts` returns 1.
    - `grep -c "from './workspace-sync.js'" src/jdtls/startup.ts` returns 1.
    - `grep -c "kind === 'fabric-mod'" src/jdtls/startup.ts` returns at least 2 (existing projectRoot derivation at line 132 + new re-sync loop).
    - `awk '/export async function retryDegradedJdtLsSessions/,/^}/' src/jdtls/startup.ts | grep -c "syncFabricModToWorkspace"` returns at least 1 (call is inside the sweep function, not a top-level helper).
    - `awk '/if \(newSession\.available === true\)/,/^			}/' src/jdtls/startup.ts | grep -c "syncFabricModToWorkspace"` returns at least 1 (call is inside the rescue-success gate, not in the degraded branch).
    - `grep -cE "logger\.warn.*[Ww]orkspace re-sync" src/jdtls/startup.ts` returns at least 1.
    - `pnpm tsc --noEmit` exits 0.
    - `git diff --name-only HEAD -- src/` returns ONLY `src/jdtls/startup.ts` (no other source file modified).
    - `git diff --name-only HEAD -- tests/jdtls/client.test.ts` returns empty (Plan 02 shim contract preserved; quality gate).
  </acceptance_criteria>
  <done>
    `retryDegradedJdtLsSessions` re-syncs every fabric-mod child after a successful rescue, errors are swallowed per D-04, only `src/jdtls/startup.ts` is touched, type-check passes, prior test suite still green (test extension follows in Task 2).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend tests/jdtls/startup.test.ts to assert post-rescue workspace re-sync</name>
  <files>tests/jdtls/startup.test.ts</files>
  <behavior>
    - Test 1: A degraded project with two fabric-mod children plus one study-jar child, after a successful `retryDegradedJdtLsSessions` rescue, sees `syncFabricModToWorkspace` invoked exactly twice — once per fabric-mod child, never for the study-jar child. Each call receives `(child, newSession, jarReader)` where `newSession` is the reassigned `project.jdtls` and is the SAME object across both calls.
    - Test 2: When `initJdtLsSession` returns a still-degraded session (`available: false`), `syncFabricModToWorkspace` is NOT invoked. (Wire by making `mockDiscoverJava` return `{ javaPath: null, error: '...' }` — initJdtLsSession then short-circuits to a degraded session.)
    - Test 3: A throw from `syncFabricModToWorkspace` for one child is swallowed via `logger.warn` (assertion on `mockLoggerWarn.mock.calls` containing a message that mentions "Workspace re-sync" or "rescue"), the second child's sync is still attempted, and `retryDegradedJdtLsSessions()` resolves without throwing.
    - Test 4: When `syncFabricModToWorkspace` returns `{ synced: true, warning: 'something' }`, the warning is logged via `logger.warn` (one warn call per warned child).
  </behavior>
  <read_first>
    - tests/jdtls/startup.test.ts (current file — full read; new `it`s slot into the existing `describe('retryDegradedJdtLsSessions')` at lines 377-468; understand `wireSuccessfulInit`, `seedProject`, `makeFabricModChild`, `makeStudyJarChild`, `makeDegradedSession`, `makeAvailableSession` helpers at lines 235-367)
    - src/jdtls/workspace-sync.ts (return-shape of syncFabricModToWorkspace — the mock's `mockResolvedValue` must match `{ synced: boolean; warning?: string }`)
    - src/jdtls/startup.ts (the just-modified file from Task 1 — to keep the test's mock target path aligned)
    - .planning/phases/37-smarter-java-discovery-cross-platform/37-REVIEW.md IN-05 (lines 281-288 — the explicit test-coverage gap this task closes)
  </read_first>
  <action>
Add a `vi.mock('../../src/jdtls/workspace-sync.js', () => ({ syncFabricModToWorkspace: vi.fn() }));` block alongside the existing `vi.mock('../../src/jdtls/client.js', ...)` block at the top of the file (lines 12-17). Add a `vi.mock('../../src/tools/shared-jar-reader.js', () => ({ jarReader: { /* opaque stub */ } }));` block too — the sweep only forwards the reference to `syncFabricModToWorkspace`, which is mocked, so the jarReader stub never has its methods called.

Import the mocked `syncFabricModToWorkspace` alongside the existing client imports at line 30, and bind a `vi.mocked()` reference (e.g. `const mockSyncFabricModToWorkspace = vi.mocked(syncFabricModToWorkspace);`) near the existing mock bindings (lines 38-42).

Default the mock to `mockResolvedValue({ synced: true })` inside each new `it` block — `vi.clearAllMocks()` in the `beforeEach` at line 381 already resets call history.

Add the four new `it()` cases listed in `<behavior>` INSIDE the existing `describe('retryDegradedJdtLsSessions', ...)` block at lines 377-468. Place them after the last existing `it` at line 459-468 (the "skips projects with no jdtls field" case), before the closing `});` at line 469. Use the existing helpers (`wireSuccessfulInit`, `seedProject`, `makeFabricModChild`, `makeStudyJarChild`, `makeDegradedSession`) — do NOT introduce new factories; the existing ones already produce the type-correct fixtures the sweep needs.

For Test 1, after `retryDegradedJdtLsSessions()`, assert:
- `expect(mockSyncFabricModToWorkspace).toHaveBeenCalledTimes(2)` — only fabric-mod children, not the study-jar.
- `expect(mockSyncFabricModToWorkspace.mock.calls.map(c => c[0].name)).toEqual(expect.arrayContaining(['mod-a', 'mod-b']))` — both fabric-mod children synced.
- The `c[1]` (second arg = newSession) reference equality across both calls (both should be the same `project.jdtls` post-rescue): `expect(mockSyncFabricModToWorkspace.mock.calls[0][1]).toBe(project.jdtls); expect(mockSyncFabricModToWorkspace.mock.calls[1][1]).toBe(project.jdtls);`.

For Test 2, use the existing "Java not found" mock pattern from line 81-93: `mockDiscoverJava.mockResolvedValue({ javaPath: null, error: 'Java not found. Tried: ...' })`. After the sweep, assert `mockSyncFabricModToWorkspace` was NOT called.

For Test 3, use `mockSyncFabricModToWorkspace.mockRejectedValueOnce(new Error('synthetic sync failure')).mockResolvedValue({ synced: true });` so the FIRST fabric-mod child throws and the SECOND still succeeds. Assert `retryDegradedJdtLsSessions()` resolves (does not throw), `mockSyncFabricModToWorkspace` was called twice (both children attempted), and `mockLoggerWarn` was called with a message containing the substring `re-sync` (lowercase or capitalized — match the message shape from Task 1).

For Test 4, `mockSyncFabricModToWorkspace.mockResolvedValue({ synced: true, warning: 'partial extraction skipped 2 entries' })`. Assert `mockLoggerWarn` was called and at least one call's first arg includes the warning text.

Do NOT modify any existing `it` block; do NOT modify tests/jdtls/client.test.ts (quality gate). Tab indentation throughout. No fenced code blocks outside the literal test-body lines (which are TypeScript, not pseudo-code).
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm test tests/jdtls/startup.test.ts && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "vi\\.mock\\('\\.\\./\\.\\./src/jdtls/workspace-sync\\.js'" tests/jdtls/startup.test.ts` returns 1.
    - `grep -c "vi\\.mock\\('\\.\\./\\.\\./src/tools/shared-jar-reader\\.js'" tests/jdtls/startup.test.ts` returns 1.
    - `grep -c "mockSyncFabricModToWorkspace" tests/jdtls/startup.test.ts` returns at least 4 (one per new `it`).
    - `awk '/describe\\('"'"'retryDegradedJdtLsSessions'"'"'/,/^}\\);$/' tests/jdtls/startup.test.ts | grep -cE "^\\s*it\\("` returns at least 10 (6 existing + 4 new).
    - `pnpm tsc --noEmit` exits 0.
    - `pnpm test tests/jdtls/startup.test.ts` passes (all describes green, including the four new `it`s).
    - `pnpm test` passes overall — at minimum 850 prior tests + the 4 new ones (= 854+), zero failures.
    - `git diff --name-only HEAD -- tests/` returns ONLY `tests/jdtls/startup.test.ts`.
    - `git diff --name-only HEAD -- tests/jdtls/client.test.ts` returns empty (Plan 02 shim contract preserved).
  </acceptance_criteria>
  <done>
    Four new `it` cases under `describe('retryDegradedJdtLsSessions')` assert: (1) re-sync invoked once per fabric-mod child, never for study-jar; (2) no sync when reinit stays degraded; (3) per-child sync throw is swallowed + logged + second child still attempted; (4) sync warnings are logged via `logger.warn`. All tests green. `tests/jdtls/client.test.ts` untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tool handler → retryDegradedJdtLsSessions | Tool args were already validated by Zod at the tool boundary; the sweep operates on in-memory `projectStore` state populated by prior validated tool calls. No fresh untrusted input crosses here. |
| retryDegradedJdtLsSessions → syncFabricModToWorkspace | `child.rootPath` and `dependencyJars` paths come from a previously-validated `loadFabricMod` call (same provenance as Plan 02's T-37-06 mitigation). No new trust boundary introduced. |
| syncFabricModToWorkspace → fs writes under jdtls.tempDir | Existing ZIP-traversal guard at workspace-sync.ts:194 rejects entry paths that escape `depDir`. Pre-existing mitigation; this plan does not change the boundary or surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-05-01 | Tampering | Post-rescue re-sync writes to `newSession.tempDir` | accept | tempDir is server-allocated via `tmpdir() + randomUUID()` at `startup.ts:66`; same trust profile as the existing initial-sync path. No external input crosses here. |
| T-37-05-02 | Denial of Service | Re-sync loop length is `count(fabric-mod children of project)` | accept | Bounded by the user's prior `add_fabric_mod` calls; the same count already gates the original `syncFabricModToWorkspace` call in the tool handler. No amplification — re-sync runs at most once per `retryDegradedJdtLsSessions` invocation, which is gated by the existing degraded-only filter (D-03) and the per-tool-handler trigger (D-04). |
| T-37-05-03 | Error-propagation | A throw from `syncFabricModToWorkspace` (ZIP traversal, fs error) | mitigate | Per-child try/catch wrapping the call, `logger.warn` with structured context, sweep continues to the next child. Outer try/catch at startup.ts:152-162 remains as defense-in-depth. Test 3 in Task 2 asserts this directly. |
| T-37-05-04 | Information-disclosure | Logged error strings via `String(err)` may include filesystem paths | accept | Same exposure profile as existing `logger.warn('JDT LS reinit failed ...')` at startup.ts:161. The MCP server runs under the user's own uid; paths in logs are already user-readable. No new sensitive info surfaces. |

Note: Phase 37's existing T-37-06/07/08 threats from Plan 02 remain in effect; no new trust boundaries are introduced by this gap closure. WR-05 (concurrency guard for `retryDegradedJdtLsSessions`) is OUT OF SCOPE per the planning context — developer ruled WR-01..WR-05 as not part of this gap closure.
</threat_model>

<verification>
- `pnpm tsc --noEmit` exits 0 (no type errors).
- `pnpm test tests/jdtls/startup.test.ts tests/jdtls/client.test.ts tests/jdtls/java-discovery.test.ts` — all three files pass; `client.test.ts` passes UNCHANGED (Plan 02 shim contract preserved).
- `pnpm test` (full suite) — 850 prior tests + the 4 new tests added by Task 2 = at least 854 tests pass, zero failures.
- `git diff --name-only HEAD~2..HEAD` (after the two task commits) shows ONLY: `src/jdtls/startup.ts`, `tests/jdtls/startup.test.ts`. No other source or test file modified.
- Manual inspection of `awk '/export async function retryDegradedJdtLsSessions/,/^}/' src/jdtls/startup.ts` shows the sequence `project.jdtls = newSession;` → `if (newSession.available === true) {` → `for (const child of project.children.values())` with `if (child.kind === 'fabric-mod')` → `await syncFabricModToWorkspace(child, newSession, jarReader)` wrapped in try/catch → `logger.info('JDT LS reinit succeeded ...')`.
</verification>

<success_criteria>
1. `retryDegradedJdtLsSessions` re-syncs every fabric-mod child of each rescued project against the new `JdtLsSession` before returning to its caller — verified by code-grep AC + Test 1 in Task 2.
2. The re-sync is gated on `newSession.available === true` — degraded reinits skip the sync loop — verified by Test 2.
3. Per-child sync errors are swallowed via `logger.warn` and do NOT abort the sweep — D-04 swallow-and-log contract preserved — verified by Test 3.
4. `syncFabricModToWorkspace` warnings (`result.warning`) are surfaced via `logger.warn` — verified by Test 4.
5. Only `src/jdtls/startup.ts` and `tests/jdtls/startup.test.ts` are modified; `tests/jdtls/client.test.ts` is untouched; no other source file is touched.
6. Full test suite remains green (850 prior + 4 new = 854+).
7. After this plan ships, the human UAT scenario from `37-VERIFICATION.md` Section "Workspace re-sync after JDT LS rescue (CR-01)" passes: `add_fabric_mod` against a project with a degraded default JDT LS session, then `find_definition` returns real navigation results without requiring a second `refresh_project` call.
</success_criteria>

<output>
Create `.planning/phases/37-smarter-java-discovery-cross-platform/37-05-SUMMARY.md` when done — summarize the two-task execution, ACs checked, and confirm the CR-01 gap closed.
</output>
