---
phase: 37-smarter-java-discovery-cross-platform
plan: 04
type: execute
wave: 4
depends_on:
  - 37-03
files_modified:
  - tests/jdtls/java-discovery.test.ts
  - tests/jdtls/startup.test.ts
autonomous: true
requirements:
  - JAVA-01
  - JAVA-02
  - JAVA-03
  - JAVA-04
  - JAVA-05

must_haves:
  truths:
    - "A new `tests/jdtls/java-discovery.test.ts` covers: priority-order across all 5 slots, version-skip continuation (slot-N returns Java <21, slot-N+1 still probed), `unescapePropertiesValue` for `\\\\`, `\\u`, `\\:`, unknown-escape, per-candidate 3s timeout simulation (SIGTERM/killed → `timed out after 3s`), vendor enumeration (macOS bundle, Homebrew openjdk* filter, Scoop current/, /opt prefix filter), version-hint sort (newest-first within parent), and the multi-line failureReason format starting with `Java not found.`."
    - "`tests/jdtls/startup.test.ts` is extended (NOT rewritten): existing 5 describes at lines 55-231 are converted from `mockDetectJava.mockReturnValue(...)` to `mockDiscoverJava.mockResolvedValue(...)`; the `toContain('Java not found')` assertion at line 78 keeps passing because Plan 01's failureReason prefix is `Java not found. Tried:`."
    - "New describes in `tests/jdtls/startup.test.ts` cover `initJdtLsSession({ projectRoot })` parameter passthrough (D-06: zero-arg → projectRoot: undefined; explicit value → projectRoot threaded to discoverJava) and `retryDegradedJdtLsSessions()` behavior (sweep degraded-only, derive rootPath from first fabric-mod child, replace project.jdtls atomically on success, swallow errors via logger.warn)."
    - "Existing `tests/jdtls/client.test.ts` Phase 35 describes for `detectJava` continue to pass UNCHANGED — the Plan 02 shim re-export preserves the byte-identical surface."
  artifacts:
    - path: "tests/jdtls/java-discovery.test.ts"
      provides: "Full coverage of discoverJava priority chain, vendor enumeration, unescape, multi-line errors"
      min_lines: 350
    - path: "tests/jdtls/startup.test.ts"
      provides: "Extended coverage of initJdtLsSession({ projectRoot }) + retryDegradedJdtLsSessions"
      contains: "discoverJava"
  key_links:
    - from: "tests/jdtls/java-discovery.test.ts"
      to: "src/jdtls/java-discovery.ts"
      via: "dynamic import after setPlatform + vi.resetModules"
      pattern: "await import\\('../../src/jdtls/java-discovery"
    - from: "tests/jdtls/startup.test.ts"
      to: "src/jdtls/client.js"
      via: "vi.mock target (the shim still re-exports discoverJava + findJdtLs + startJdtLs)"
      pattern: "vi\\.mock\\('../../src/jdtls/client\\.js'"
---

<objective>
Write the new `tests/jdtls/java-discovery.test.ts` for the Plan 01 module and extend `tests/jdtls/startup.test.ts` for the Plan 02 surface (D-17). The existing `tests/jdtls/client.test.ts` Phase 35 describes are NOT modified — the Plan 02 shim preserves their surface.

Purpose: Lock down all five JAVA-NN requirement behaviors at the unit-test layer. Without these, the chain logic, unescape spec, vendor map, and timeout semantics are unverified.

Output: One new test file + one extended test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-smarter-java-discovery-cross-platform/37-CONTEXT.md
@.planning/phases/37-smarter-java-discovery-cross-platform/37-PATTERNS.md
@.planning/phases/37-smarter-java-discovery-cross-platform/37-01-PLAN.md
@.planning/phases/37-smarter-java-discovery-cross-platform/37-02-PLAN.md
@CLAUDE.md

@src/jdtls/java-discovery.ts
@src/jdtls/startup.ts
@tests/jdtls/client.test.ts
@tests/jdtls/startup.test.ts
@tests/platform/index.test.ts

<interfaces>
<!-- Test contracts (the SUT exports the tests must exercise). -->

From src/jdtls/java-discovery.ts (Plan 01):
- `discoverJava(opts?: { projectRoot?: string }): Promise<JavaDetectResult>`
- `setJavaHome(javaHome: string | undefined): void`
- `unescapePropertiesValue(value: string): string`
- `parseJavaVersion(output: string): number | null`
- type `JavaDetectResult = { javaPath: string; version: number } | { javaPath: null; error: string }`

From src/jdtls/startup.ts (Plan 02):
- `initJdtLsSession(opts?: { projectRoot?: string }): Promise<JdtLsSession>`
- `retryDegradedJdtLsSessions(): Promise<void>`

Test patterns to reuse:
- `setPlatform(p: NodeJS.Platform)` helper from `tests/platform/index.test.ts:10-22`
- `vi.mock` spread pattern from `tests/jdtls/client.test.ts:6-20` (Pitfall 6: MANDATORY `{ ...actual, named: vi.fn() }`)
- `createMockProcess()` helper from `tests/jdtls/startup.test.ts:33-53`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author tests/jdtls/java-discovery.test.ts covering all five JAVA-NN requirements</name>
  <read_first>
    - tests/jdtls/client.test.ts (lines 1-340 — mock setup boilerplate, detectJava describes, Windows-mocked describes; the analog for boilerplate)
    - tests/platform/index.test.ts (lines 1-44 — setPlatform helper + dynamic import pattern)
    - src/jdtls/java-discovery.ts (the SUT — confirm exact exports and behaviors)
    - .planning/phases/37-smarter-java-discovery-cross-platform/37-PATTERNS.md ("`tests/jdtls/java-discovery.test.ts` (NEW)" section — composite analog + execFile-callback mocking pattern)
    - .planning/phases/37-smarter-java-discovery-cross-platform/37-CONTEXT.md (D-12..D-22 — vendor map, version-hint, error taxonomy)
    - src/platform/index.ts (commonJavaLocations return values per platform — to mock realistically)
  </read_first>
  <action>
    Create `tests/jdtls/java-discovery.test.ts` with tab indentation, ESM `.js` imports, and the vitest API. Compose the file from these blocks:

    1. **File-header boilerplate** (copy from PATTERNS.md, adapted): import `describe/it/expect/vi/beforeEach/afterEach` from `vitest`; import `execFile` from `node:child_process`; import `readdir`, `readFile` from `node:fs/promises`; import `existsSync` from `node:fs`. Mock all three with the MANDATORY spread idiom — `vi.mock('node:child_process', async () => { const actual = await vi.importActual<typeof import('node:child_process')>('node:child_process'); return { ...actual, execFile: vi.fn() }; })`. Same pattern for `node:fs/promises` (mocking `readdir` and `readFile`) and `node:fs` (mocking `existsSync`, preserving `vi.fn(actual.existsSync)` as the default). Capture `originalPlatform = process.platform` and `originalEnv = { ...process.env }`. Define the `setPlatform(p)` helper.

    2. **`describe('unescapePropertiesValue')`** — pure-function unit tests, NO platform/fs mocking needed:
       - unescapes `\\\\` → `\\` (input literal `'C:\\\\Users\\\\foo'` → expected `'C:\\Users\\foo'`)
       - unescapes `\\u0043` → `C` (UTF-16 hex; consume exactly 4 hex digits)
       - unescapes `\\:` → `:` and `\\=` → `=`
       - unescapes `\\t`, `\\n`, `\\r`, `\\f` to their control characters
       - unknown `\\X` (e.g. `\\q`) drops the backslash → `q`
       - `\\\\u0043` (literal backslash-u-0043) → `\\u0043` literal NOT `C` (escape applies only when single backslash precedes valid hex)
       - empty string returns empty string
       - string with no backslashes returns identical content

    3. **`describe('discoverJava priority order')`** — mock `execFile` to canned `--version` output; assert slot evaluation order. Use the `mockImplementation((file, args, opts, cb) => { (cb as any)(null, { stdout: 'openjdk 21.0.1 2023-10-17', stderr: '' }); return {} as any; })` shape because `promisify(execFile)` wraps the callback form.
       - Slot 1 wins: `setJavaHome('/cli/java')` and `process.env.JAVA_HOME = '/env/java'`; assert resolved javaPath starts with `/cli/java` and `mockExecFile.mock.calls[0][0]` includes `/cli/java`.
       - Slot 2 wins when slot 1 absent: `setJavaHome(undefined)`, mock `readFile` for `gradle.properties` to return `org.gradle.java.home=/gradle/java`, pass `{ projectRoot: '/work/proj' }`.
       - Slot 3 wins when slots 1+2 absent: `setJavaHome(undefined)`, no projectRoot, `process.env.JAVA_HOME = '/env/java'`.
       - Slot 4 wins when slots 1-3 absent: javaPath is the bare `javaBinaryName()` (resolveJavaExecutable passes through).
       - Slot 5 wins when 1-4 absent: mock `readdir` for a `commonJavaLocations` parent to return a temurin-21 entry; assert the constructed candidate path contains the parent + entry + bin/java.

    4. **`describe('discoverJava JAVA-02 version skip continuation')`** — slot returns Java 17 (parsed from `openjdk 17.0.8 2023-07-18`); next slot returns Java 21 (must be probed). Assert `result.version === 21` and that execFile was called for both slots (not short-circuited).

    5. **`describe('discoverJava JAVA-03 backslash unescape end-to-end')`** — Linux platform, mock `readFile('gradle.properties')` to return `org.gradle.java.home=/opt/jdk\\:21` (literal escape in properties syntax). Pass `{ projectRoot: '/work/proj' }`. Mock execFile to succeed. Assert the resolved path contains `/opt/jdk:21` (unescaped colon).

    6. **`describe('discoverJava JAVA-05 per-candidate 3s timeout')`** — mock `execFile` to invoke the callback with an error annotated `{ signal: 'SIGTERM' }` or `{ killed: true }`; assert that when this is the ONLY candidate's outcome, the synthesized failureReason contains `timed out after 3s`. Verify the `timeout: 3000` (or `3_000`) option was passed by inspecting `mockExecFile.mock.calls[0][2]`.

    7. **`describe('discoverJava JAVA-04 vendor enumeration')`** — three platform-mocked sub-describes:
       - **macOS bundle layout**: setPlatform('darwin'), mock readdir for `/Library/Java/JavaVirtualMachines` to return `['temurin-21.jdk', 'temurin-17.jdk']`; assert candidate paths include `Contents/Home/bin/java`; assert `temurin-21.jdk` is probed BEFORE `temurin-17.jdk` (version-hint sort — D-14).
       - **Homebrew openjdk filter**: setPlatform('darwin'), mock readdir for `/opt/homebrew/opt` to return `['openjdk@21', 'postgresql@16', 'openssl@3']`; assert ONLY `openjdk@21` becomes a candidate (D-16/homebrew filter); assert path includes `libexec/openjdk.jdk/Contents/Home/bin/java`.
       - **Scoop current/**: setPlatform('win32'), mock readdir for `~/scoop/apps` (use the actual path from `commonJavaLocations` after setPlatform) to return `['adoptium-jdk-21', 'firefox']`; assert candidate path includes `/current/bin/java.exe`.
       - **/opt prefix filter (Linux)**: setPlatform('linux'), mock readdir for `/opt` to return `['jdk-21', 'temurin-17', 'postgres', 'intellij-idea-community', 'corretto-21']`; assert ONLY `jdk-21`, `temurin-17`, `corretto-21` are probed; `postgres` and `intellij-idea-community` are filtered out (D-16).

    8. **`describe('discoverJava failureReason multi-line format')`** — every slot fails with a different reason. Mock execFile so JAVA_HOME returns Java 17, PATH returns Java 17, and scan candidates are empty (mock readdir to throw / return []). Pass `{ projectRoot: '/work/proj' }` with gradle.properties missing. Assert the returned `error` string:
       - starts with `'Java not found. Tried:'` (exact prefix per D-18).
       - contains `'--java-home: (not set)'` (D-21).
       - contains `'org.gradle.java.home: (not set in /work/proj/gradle.properties)'` (D-21 — projectRoot supplied but key absent).
       - contains `'JAVA_HOME='` followed by the value and `: Java 17 (need 21+)'`.
       - contains `'java on PATH:'` followed by a reason.
       - ends with `'Install Java 21+ (Adoptium / Microsoft / Zulu) or set JAVA_HOME / --java-home.'`.

    9. **`describe('discoverJava JAVA-05 zero-arg call')`** — call `discoverJava()` (no opts). Assert that the gradle.properties slot is silently skipped (`mockReadFile` is NOT called for any gradle.properties path) and the failureReason (on full failure) shows `'org.gradle.java.home: (not set)'` (the no-projectRoot branch per D-21).

    Every platform-flipping describe MUST use the `setPlatform(...) → vi.resetModules() → await import(...)` triplet in `beforeEach`, and reset platform in `afterEach`. Re-import the SUT inside each `it()` (or in `beforeEach`) so the post-reset module state is the one under test.
  </action>
  <verify>
    <automated>pnpm test tests/jdtls/java-discovery.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/jdtls/java-discovery.test.ts` exists.
    - `grep -c "describe(" tests/jdtls/java-discovery.test.ts` returns at least 7 (top-level describes per Task 1 outline).
    - `grep -c "unescapePropertiesValue" tests/jdtls/java-discovery.test.ts` returns at least 5 (multiple it() cases).
    - `grep -c "'Java not found\\. Tried:'\|toContain.*'Java not found'" tests/jdtls/java-discovery.test.ts` returns at least 1.
    - `grep -c "timed out after 3s" tests/jdtls/java-discovery.test.ts` returns at least 1.
    - `grep -c "setPlatform" tests/jdtls/java-discovery.test.ts` returns at least 4 (used in macOS/Linux/Windows describes).
    - `grep -c "vi\\.resetModules" tests/jdtls/java-discovery.test.ts` returns at least 4.
    - `grep -c "await import.*java-discovery\\.js" tests/jdtls/java-discovery.test.ts` returns at least 4 (dynamic import after resetModules).
    - `grep -c "openjdk@21\|temurin-21" tests/jdtls/java-discovery.test.ts` returns at least 2 (vendor-specific fixtures).
    - `pnpm test tests/jdtls/java-discovery.test.ts` exits 0 with all tests passing.
  </acceptance_criteria>
  <done>The new test file passes under `pnpm test tests/jdtls/java-discovery.test.ts` and covers every behavior locked by JAVA-01..JAVA-05.</done>
</task>

<task type="auto">
  <name>Task 2: Extend tests/jdtls/startup.test.ts for projectRoot + retryDegradedJdtLsSessions</name>
  <read_first>
    - tests/jdtls/startup.test.ts (ENTIRE file — lines 1-231; the existing 5 describes plus the mock setup at lines 10-14 AND the top-level `import { ... } from '../../src/jdtls/client.js'` statement — examine the exact named imports already present)
    - src/jdtls/startup.ts (post-Plan-02 version — confirms exact signature of `initJdtLsSession({ projectRoot? })` and `retryDegradedJdtLsSessions()`)
    - src/state/project-store.ts (projectStore.list / projectStore.set — used to seed test fixtures)
    - .planning/phases/37-smarter-java-discovery-cross-platform/37-PATTERNS.md ("`tests/jdtls/startup.test.ts` (extend)" section — exact describe shapes)
    - .planning/phases/37-smarter-java-discovery-cross-platform/37-CONTEXT.md (D-02, D-03, D-05, D-06 — retry sweep semantics)
  </read_first>
  <action>
    Edit `tests/jdtls/startup.test.ts`:

    1. **Extend the top-level import statement** that names symbols from `'../../src/jdtls/client.js'` (currently includes `detectJava`, `findJdtLs`, `startJdtLs` per the existing test file). Add `discoverJava` as a named import to that same statement. **This is mandatory**: `vi.mocked(discoverJava)` at runtime requires `discoverJava` to be a real binding in the test file's module scope, sourced from the same module path that vi.mock intercepts. Without this import line edit, `vi.mocked(discoverJava)` throws `ReferenceError: discoverJava is not defined`. Example shape (the executor must use the actual existing import line):

           // BEFORE (existing):
           import { detectJava, findJdtLs, startJdtLs } from '../../src/jdtls/client.js';
           // AFTER (add discoverJava):
           import { detectJava, discoverJava, findJdtLs, startJdtLs } from '../../src/jdtls/client.js';

       (The order of names within the braces doesn't matter; only that `discoverJava` is present as a named import from `'../../src/jdtls/client.js'`.)

    2. Extend the mock block at lines 10-14: add `discoverJava: vi.fn()` to the `'../../src/jdtls/client.js'` mock object (this still intercepts because Plan 02's client.ts re-exports `discoverJava` from java-discovery.ts — vitest hoist works against the import path the SUT uses).

    3. Update the existing 5 tests at lines 69-230: convert every `mockDetectJava.mockReturnValue({...})` to `mockDiscoverJava.mockResolvedValue({...})` (the return-shape is identical — `{ javaPath, version }` or `{ javaPath: null, error }` — but `discoverJava` is async). Add `const mockDiscoverJava = vi.mocked(discoverJava);` next to the existing `mockDetectJava` constant; you may delete `mockDetectJava` if no test still uses it.

       The `toContain('Java not found')` assertion at line 78 keeps passing without edit because Plan 01's failureReason starts with the literal `'Java not found. Tried:'`.

    4. Append a new `describe('initJdtLsSession with projectRoot')` block:
       - `it('passes projectRoot through to discoverJava')`: `mockDiscoverJava.mockResolvedValue({ javaPath: '/usr/bin/java', version: 21 })`; mock `findJdtLs` and `startJdtLs` per the existing successful-path test fixture; call `await initJdtLsSession({ projectRoot: '/work/my-mod' })`; assert `expect(mockDiscoverJava).toHaveBeenCalledWith({ projectRoot: '/work/my-mod' })`.
       - `it('zero-arg call passes projectRoot: undefined (D-06)')`: same fixture; call `await initJdtLsSession()`; assert `expect(mockDiscoverJava).toHaveBeenCalledWith({ projectRoot: undefined })`.

    5. Append a new `describe('retryDegradedJdtLsSessions')` block. These tests interact with the real `projectStore` singleton — clear it between tests via `beforeEach`. Add `import { projectStore } from '../../src/state/project-store.js';` if not already present; iterate and `delete` keys via the public API (or rely on `projectStore.set('default', { name: 'default', children: new Map() })` to overwrite). Required `it()` cases:
       - `it('sweeps all projects with jdtls.available === false')`: seed two projects; one has `jdtls: { available: false, failureReason: '...', tempDir: '', dataDir: '', jarIdToDirName: new Map() }`, one has `jdtls: { available: true, ... }`; mock `discoverJava` + `findJdtLs` + `startJdtLs` for success; call `retryDegradedJdtLsSessions()`; assert `mockDiscoverJava` was called exactly ONCE (the healthy project skipped).
       - `it('uses first fabric mod child rootPath as projectRoot (D-03)')`: seed a degraded project with two children — first is a fabric-mod with `rootPath: '/work/mod-a'`, second is a study-jar; call `retryDegradedJdtLsSessions()`; assert `mockDiscoverJava` was called with `{ projectRoot: '/work/mod-a' }`.
       - `it('passes each degraded project its own projectRoot (D-03/D-05)')`: seed TWO degraded projects each with a distinct fabric-mod rootPath (e.g., `/work/mod-a` and `/work/mod-b`); call `retryDegradedJdtLsSessions()`; assert `mockDiscoverJava` was called twice and that the two call argument objects are `{ projectRoot: '/work/mod-a' }` and `{ projectRoot: '/work/mod-b' }` respectively (order doesn't matter — assert via `expect(mockDiscoverJava.mock.calls.map(c => c[0])).toEqual(expect.arrayContaining([{ projectRoot: '/work/mod-a' }, { projectRoot: '/work/mod-b' }]))`). This proves the sweep does NOT pass a captured outer-scope value to all projects.
       - `it('replaces project.jdtls atomically on retry success')`: seed a degraded project with `jdtls.available === false` and no fabric-mod child (projectRoot will be undefined); mock discovery success; call retry; assert `project.jdtls.available === true` after the call (the field has been replaced, not mutated piecewise).
       - `it('logs warning but does not throw on retry failure')`: seed a degraded project; mock `discoverJava` to throw a synthetic Error; call `retryDegradedJdtLsSessions()`; assert the call resolves without throwing (i.e., `await expect(retryDegradedJdtLsSessions()).resolves.toBeUndefined()`); assert `logger.warn` was called with a message containing `'reinit failed'`.
       - `it('skips projects with no jdtls field')`: seed a project where `project.jdtls` is undefined; assert `mockDiscoverJava` is NOT called for that project.

    Use `createMockProcess()` (already at lines 33-53) when a test needs a real-looking ChildProcess for the `startJdtLs` mock return.

    Do NOT modify the file-header boilerplate beyond adding `discoverJava` to the import statement (step 1) and the mock object (step 2). Do NOT modify the existing logger mock, the `afterEach` cleanup of `tempDirs`, or `createMockProcess` itself.
  </action>
  <verify>
    <automated>pnpm test tests/jdtls/startup.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - **The top-level import statement contains `discoverJava` as a named import from `'../../src/jdtls/client.js'`**: `grep -E "^import \\{[^}]*\\bdiscoverJava\\b[^}]*\\} from '\\.\\./\\.\\./src/jdtls/client\\.js'" tests/jdtls/startup.test.ts` returns exactly 1 match.
    - `grep -c "discoverJava: vi.fn()" tests/jdtls/startup.test.ts` returns 1 (added to the mock block).
    - `grep -c "mockDiscoverJava" tests/jdtls/startup.test.ts` returns at least 8 (converted existing tests + new ones + the new D-03/D-05 multi-project test).
    - `grep -c "vi\\.mocked(discoverJava)" tests/jdtls/startup.test.ts` returns at least 1 (the `mockDiscoverJava` const initializer).
    - `grep -c "describe.*initJdtLsSession with projectRoot" tests/jdtls/startup.test.ts` returns 1.
    - `grep -c "describe.*retryDegradedJdtLsSessions" tests/jdtls/startup.test.ts` returns 1.
    - `grep -c "projectRoot: '/work/my-mod'" tests/jdtls/startup.test.ts` returns at least 1.
    - `grep -c "projectRoot: undefined" tests/jdtls/startup.test.ts` returns at least 1.
    - `grep -c "rootPath" tests/jdtls/startup.test.ts` returns at least 1 (fabric-mod fixture).
    - **D-03/D-05 multi-project test present**: `grep -c "projectRoot: '/work/mod-a'" tests/jdtls/startup.test.ts` returns at least 1 AND `grep -c "projectRoot: '/work/mod-b'" tests/jdtls/startup.test.ts` returns at least 1 (both per-project roots asserted in the new multi-project test).
    - The existing assertion `expect(session.failureReason).toContain('Java not found');` at line ~78 is PRESENT and UNCHANGED.
    - `pnpm test tests/jdtls/startup.test.ts` exits 0 with all tests passing.
  </acceptance_criteria>
  <done>startup.test.ts has its existing 5 tests converted to the async discoverJava mock (with `discoverJava` added as a top-level named import), plus new describes for `initJdtLsSession({ projectRoot })` and `retryDegradedJdtLsSessions()` including the D-03/D-05 per-project-projectRoot regression; `pnpm test tests/jdtls/startup.test.ts` passes.</done>
</task>

<task type="auto">
  <name>Task 3: End-to-end regression — full test suite passes including untouched client.test.ts</name>
  <read_first>
    - tests/jdtls/client.test.ts (ENTIRE file — confirm no modifications were made by Plan 02 or this plan)
    - tests/tools/get-project-info.test.ts (around line 177 — confirms `toContain('Java not found')` still matches the new multi-line message)
    - tests/tools/create-project.test.ts (around line 84 — same assertion site)
  </read_first>
  <action>
    Do NOT modify any source or test file in this task. This is a regression-only checkpoint: run the full vitest suite and confirm that EVERY existing test (Phase 35 client tests, Phase 36 path tests, tool tests using the `toContain('Java not found')` literal) continues to pass.

    If any test fails, do NOT patch the test — the failure indicates a regression in Plan 01/02/03 that must be fixed in the originating plan. Report the failure and STOP. The acceptance criteria below explicitly forbid blind test mutation as a fix.

    Specifically verify the UNIX-01 byte-identical commitment:
    - `tests/jdtls/client.test.ts` lines 64-131 (the v1.5 `detectJava` describes) must pass without edits.
    - Any test in `tests/tools/` that uses `toContain('Java not found')` must still match because Plan 01 keeps the literal prefix.
  </action>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --name-only HEAD -- tests/jdtls/client.test.ts` returns empty (UNIX-01 byte-identical surface).
    - `pnpm test` exits 0 with all tests passing.
    - The pass count includes every existing describe in `tests/jdtls/client.test.ts` (the Phase 35 Windows-mocked describes) — verify by `pnpm test tests/jdtls/client.test.ts --reporter=verbose 2>&amp;1 | grep -c "✓"` returning at least the same count as before this plan.
    - `pnpm test tests/tools/get-project-info.test.ts tests/tools/create-project.test.ts` exits 0 (Java-not-found assertion still matches).
  </acceptance_criteria>
  <done>The full `pnpm test` suite passes; no existing test file was edited by Plans 01-03 or this plan except `tests/jdtls/startup.test.ts`; UNIX-01 byte-identical commitment is held.</done>
</task>

</tasks>

<verification>
- `pnpm test` exits 0.
- `git diff --name-only HEAD -- tests/jdtls/client.test.ts` is empty.
- `git diff --name-only HEAD` for this plan touches exactly `tests/jdtls/java-discovery.test.ts` (new) and `tests/jdtls/startup.test.ts` (modified).
- No source file under `src/` is modified by this plan.
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures → SUT | Tests mock fs/child_process/platform; no real I/O escapes the test harness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-11 | Tampering | tests mutating process.platform / process.env leak into other test files | mitigate | Every platform-flipping describe restores `originalPlatform` and `originalEnv` in `afterEach` (pattern reused verbatim from Phase 35/36 tests) |
| T-37-12 | DoS | vi.mock spread omission makes downstream tests fail with `readdir is not a function` | mitigate | Pitfall 6 spread idiom `{ ...actual, named: vi.fn() }` enforced by acceptance criteria; PATTERNS.md documents the failure mode |
</threat_model>

<success_criteria>
- All `<acceptance_criteria>` of Tasks 1, 2, and 3 pass.
- `pnpm test` exits 0 with no test files skipped.
- `tests/jdtls/client.test.ts` is byte-identical to its pre-Phase-37 state (UNIX-01 commitment verifiable by `git diff`).
- `git log --name-only HEAD~3..HEAD -- tests/jdtls/client.test.ts` (or equivalent) shows no edits across Plans 01-04.
</success_criteria>

<output>
Create `.planning/phases/37-smarter-java-discovery-cross-platform/37-04-SUMMARY.md` when done.
</output>
